fragment_downloaded_cb({"url":"base/gstbytereader.html#gst_byte_reader_get_string_utf8","fragment":"\n\tgst_byte_reader_get_string_utf8\n\n\n\tGstBase.ByteReader.prototype.get_string_utf8\n\n\n\tGstBase.ByteReader.get_string_utf8\n\nReturns a constant pointer to the current data position if there is\na NUL-terminated string in the data (this could be just a NUL terminator),\nadvancing the current position to the byte after the string. This will work\nfor any NUL-terminated string with a character width of 8 bits, so ASCII,\nUTF-8, ISO-8859-N etc.\nNo input checking for valid UTF-8 is done.\nThis function will fail if no NUL-terminator was found in in the data.\nParameters:\na GstByteReader instance\naddress of a\ngchar pointer variable in which to store the result\nTRUE if a string could be found, FALSE otherwise.\nReturns a constant pointer to the current data position if there is\na NUL-terminated string in the data (this could be just a NUL terminator),\nadvancing the current position to the byte after the string. This will work\nfor any NUL-terminated string with a character width of 8 bits, so ASCII,\nUTF-8, ISO-8859-N etc.\nNo input checking for valid UTF-8 is done.\nThis function will fail if no NUL-terminator was found in in the data.\nParameters:\na GstBase.ByteReader instance\nReturns a tuple made of:\ntrue if a string could be found, false otherwise.\ntrue if a string could be found, false otherwise.\nReturns a constant pointer to the current data position if there is\na NUL-terminated string in the data (this could be just a NUL terminator),\nadvancing the current position to the byte after the string. This will work\nfor any NUL-terminated string with a character width of 8 bits, so ASCII,\nUTF-8, ISO-8859-N etc.\nNo input checking for valid UTF-8 is done.\nThis function will fail if no NUL-terminator was found in in the data.\nParameters:\na GstBase.ByteReader instance\nReturns a tuple made of:\nTrue if a string could be found, False otherwise.\nTrue if a string could be found, False otherwise.\n"});