fragment_downloaded_cb({"url":"base/gstbaseparse.html#gst_base_parse_set_pts_interpolation","fragment":"\n\tgst_base_parse_set_pts_interpolation\n\n\n\tGstBase.BaseParse.prototype.set_pts_interpolation\n\n\n\tGstBase.BaseParse.set_pts_interpolation\n\nBy default, the base class will guess PTS timestamps using a simple\ninterpolation (previous timestamp + duration), which is incorrect for\ndata streams with reordering, where PTS can go backward. Sub-classes\nimplementing such formats should disable PTS interpolation.\nParameters:\na GstBaseParse\nTRUE if parser should interpolate PTS timestamps\nBy default, the base class will guess PTS timestamps using a simple\ninterpolation (previous timestamp + duration), which is incorrect for\ndata streams with reordering, where PTS can go backward. Sub-classes\nimplementing such formats should disable PTS interpolation.\nParameters:\na GstBase.BaseParse\ntrue if parser should interpolate PTS timestamps\nBy default, the base class will guess PTS timestamps using a simple\ninterpolation (previous timestamp + duration), which is incorrect for\ndata streams with reordering, where PTS can go backward. Sub-classes\nimplementing such formats should disable PTS interpolation.\nParameters:\na GstBase.BaseParse\nTrue if parser should interpolate PTS timestamps\n"});