fragment_downloaded_cb({"url":"base/gstbaseparse.html#gst_base_parse_set_infer_ts","fragment":"\n\tgst_base_parse_set_infer_ts\n\n\n\tGstBase.BaseParse.prototype.set_infer_ts\n\n\n\tGstBase.BaseParse.set_infer_ts\n\nBy default, the base class might try to infer PTS from DTS and vice\nversa.  While this is generally correct for audio data, it may not\nbe otherwise. Sub-classes implementing such formats should disable\ntimestamp inferring.\nParameters:\na GstBaseParse\nTRUE if parser should infer DTS/PTS from each other\nBy default, the base class might try to infer PTS from DTS and vice\nversa.  While this is generally correct for audio data, it may not\nbe otherwise. Sub-classes implementing such formats should disable\ntimestamp inferring.\nParameters:\na GstBase.BaseParse\ntrue if parser should infer DTS/PTS from each other\nBy default, the base class might try to infer PTS from DTS and vice\nversa.  While this is generally correct for audio data, it may not\nbe otherwise. Sub-classes implementing such formats should disable\ntimestamp inferring.\nParameters:\na GstBase.BaseParse\nTrue if parser should infer DTS/PTS from each other\n"});