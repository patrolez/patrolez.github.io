fragment_downloaded_cb({"url":"base/gstadapter.html#gst_adapter_prev_pts_at_offset","fragment":"\n\tgst_adapter_prev_pts_at_offset\n\n\n\tGstBase.Adapter.prototype.prev_pts_at_offset\n\n\n\tGstBase.Adapter.prev_pts_at_offset\n\nGet the pts that was before the byte at offset offset in the adapter. When\ndistance is given, the amount of bytes between the pts and the current\nposition is returned.\nThe pts is reset to GST_CLOCK_TIME_NONE and the distance is set to 0 when\nthe adapter is first created or when it is cleared. This also means that before\nthe first byte with a pts is removed from the adapter, the pts\nand distance returned are GST_CLOCK_TIME_NONE and 0 respectively.\nParameters:\na GstAdapter\nthe offset in the adapter at which to get timestamp\npointer to location for distance, or NULL\nThe previously seen pts at given offset.\n\n\tSince\t\t\t: 1.2\n\t\t\nGet the pts that was before the byte at offset offset in the adapter. When\ndistance is given, the amount of bytes between the pts and the current\nposition is returned.\nThe pts is reset to GST_CLOCK_TIME_NONE and the distance is set to 0 when\nthe adapter is first created or when it is cleared. This also means that before\nthe first byte with a pts is removed from the adapter, the pts\nand distance returned are GST_CLOCK_TIME_NONE and 0 respectively.\nParameters:\na GstBase.Adapter\nthe offset in the adapter at which to get timestamp\nReturns a tuple made of:\nThe previously seen pts at given offset.\nThe previously seen pts at given offset.\n\n\tSince\t\t\t: 1.2\n\t\t\nGet the pts that was before the byte at offset offset in the adapter. When\ndistance is given, the amount of bytes between the pts and the current\nposition is returned.\nThe pts is reset to GST_CLOCK_TIME_NONE and the distance is set to 0 when\nthe adapter is first created or when it is cleared. This also means that before\nthe first byte with a pts is removed from the adapter, the pts\nand distance returned are GST_CLOCK_TIME_NONE and 0 respectively.\nParameters:\na GstBase.Adapter\nthe offset in the adapter at which to get timestamp\nReturns a tuple made of:\nThe previously seen pts at given offset.\nThe previously seen pts at given offset.\n\n\tSince\t\t\t: 1.2\n\t\t\n"});