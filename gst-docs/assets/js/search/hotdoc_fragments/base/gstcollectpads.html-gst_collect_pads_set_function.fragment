fragment_downloaded_cb({"url":"base/gstcollectpads.html#gst_collect_pads_set_function","fragment":"\n\tgst_collect_pads_set_function\n\n\n\tGstBase.CollectPads.prototype.set_function\n\n\n\tGstBase.CollectPads.set_function\n\nCollectPads provides a default collection algorithm that will determine\nthe oldest buffer available on all of its pads, and then delegate\nto a configured callback.\nHowever, if circumstances are more complicated and/or more control\nis desired, this sets a callback that will be invoked instead when\nall the pads added to the collection have buffers queued.\nEvidently, this callback is not compatible with\ngst_collect_pads_set_buffer_function callback.\nIf this callback is set, the former will be unset.\nMT safe.\nParameters:\nthe collectpads to use\nthe function to set\nuser data passed to the function\nCollectPads provides a default collection algorithm that will determine\nthe oldest buffer available on all of its pads, and then delegate\nto a configured callback.\nHowever, if circumstances are more complicated and/or more control\nis desired, this sets a callback that will be invoked instead when\nall the pads added to the collection have buffers queued.\nEvidently, this callback is not compatible with\nGstBase.CollectPads.prototype.set_buffer_function callback.\nIf this callback is set, the former will be unset.\nMT safe.\nParameters:\nthe collectpads to use\nthe function to set\nuser data passed to the function\nCollectPads provides a default collection algorithm that will determine\nthe oldest buffer available on all of its pads, and then delegate\nto a configured callback.\nHowever, if circumstances are more complicated and/or more control\nis desired, this sets a callback that will be invoked instead when\nall the pads added to the collection have buffers queued.\nEvidently, this callback is not compatible with\nGstBase.CollectPads.set_buffer_function callback.\nIf this callback is set, the former will be unset.\nMT safe.\nParameters:\nthe collectpads to use\nthe function to set\nuser data passed to the function\n"});