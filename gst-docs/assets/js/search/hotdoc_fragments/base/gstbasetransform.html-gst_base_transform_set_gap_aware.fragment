fragment_downloaded_cb({"url":"base/gstbasetransform.html#gst_base_transform_set_gap_aware","fragment":"\n\tgst_base_transform_set_gap_aware\n\n\n\tGstBase.BaseTransform.prototype.set_gap_aware\n\n\n\tGstBase.BaseTransform.set_gap_aware\n\nIf gap_aware is FALSE (the default), output buffers will have the\nGST_BUFFER_FLAG_GAP flag unset.\nIf set to TRUE, the element must handle output buffers with this flag set\ncorrectly, i.e. it can assume that the buffer contains neutral data but must\nunset the flag if the output is no neutral data.\nMT safe.\nParameters:\na GstBaseTransform\nNew state\nIf gap_aware is false (the default), output buffers will have the\nGst.BufferFlags.GAP flag unset.\nIf set to true, the element must handle output buffers with this flag set\ncorrectly, i.e. it can assume that the buffer contains neutral data but must\nunset the flag if the output is no neutral data.\nMT safe.\nParameters:\na GstBase.BaseTransform\nNew state\nIf gap_aware is False (the default), output buffers will have the\nGst.BufferFlags.GAP flag unset.\nIf set to True, the element must handle output buffers with this flag set\ncorrectly, i.e. it can assume that the buffer contains neutral data but must\nunset the flag if the output is no neutral data.\nMT safe.\nParameters:\na GstBase.BaseTransform\nNew state\n"});