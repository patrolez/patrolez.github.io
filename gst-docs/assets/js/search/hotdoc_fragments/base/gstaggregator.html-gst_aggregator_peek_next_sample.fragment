fragment_downloaded_cb({"url":"base/gstaggregator.html#gst_aggregator_peek_next_sample","fragment":"\n\tgst_aggregator_peek_next_sample\n\n\n\tGstBase.Aggregator.prototype.peek_next_sample\n\n\n\tGstBase.Aggregator.peek_next_sample\n\nUse this function to determine what input buffers will be aggregated\nto produce the next output buffer. This should only be called from\na samples-selected handler, and can be used to precisely\ncontrol aggregating parameters for a given set of input samples.\nParameters:\nThe sample that is about to be aggregated. It may hold a GstBuffer\nor a GstBufferList. The contents of its info structure is subclass-dependent,\nand documented on a subclass basis. The buffers held by the sample are\nnot writable.\n\n\tSince\t\t\t: 1.18\n\t\t\nUse this function to determine what input buffers will be aggregated\nto produce the next output buffer. This should only be called from\na samples-selected handler, and can be used to precisely\ncontrol aggregating parameters for a given set of input samples.\nParameters:\nThe sample that is about to be aggregated. It may hold a Gst.Buffer\nor a Gst.BufferList. The contents of its info structure is subclass-dependent,\nand documented on a subclass basis. The buffers held by the sample are\nnot writable.\n\n\tSince\t\t\t: 1.18\n\t\t\nUse this function to determine what input buffers will be aggregated\nto produce the next output buffer. This should only be called from\na samples-selected handler, and can be used to precisely\ncontrol aggregating parameters for a given set of input samples.\nParameters:\nThe sample that is about to be aggregated. It may hold a Gst.Buffer\nor a Gst.BufferList. The contents of its info structure is subclass-dependent,\nand documented on a subclass basis. The buffers held by the sample are\nnot writable.\n\n\tSince\t\t\t: 1.18\n\t\t\n"});