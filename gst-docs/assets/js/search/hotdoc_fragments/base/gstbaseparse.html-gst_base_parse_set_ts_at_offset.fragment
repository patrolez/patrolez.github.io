fragment_downloaded_cb({"url":"base/gstbaseparse.html#gst_base_parse_set_ts_at_offset","fragment":"\n\tgst_base_parse_set_ts_at_offset\n\n\n\tGstBase.BaseParse.prototype.set_ts_at_offset\n\n\n\tGstBase.BaseParse.set_ts_at_offset\n\nThis function should only be called from a handle_frame implementation.\nGstBaseParse creates initial timestamps for frames by using the last\ntimestamp seen in the stream before the frame starts.  In certain\ncases, the correct timestamps will occur in the stream after the\nstart of the frame, but before the start of the actual picture data.\nThis function can be used to set the timestamps based on the offset\ninto the frame data that the picture starts.\nParameters:\na GstBaseParse\noffset into current buffer\n\n\tSince\t\t\t: 1.2\n\t\t\nThis function should only be called from a handle_frame implementation.\nGstBase.BaseParse creates initial timestamps for frames by using the last\ntimestamp seen in the stream before the frame starts.  In certain\ncases, the correct timestamps will occur in the stream after the\nstart of the frame, but before the start of the actual picture data.\nThis function can be used to set the timestamps based on the offset\ninto the frame data that the picture starts.\nParameters:\na GstBase.BaseParse\noffset into current buffer\n\n\tSince\t\t\t: 1.2\n\t\t\nThis function should only be called from a handle_frame implementation.\nGstBase.BaseParse creates initial timestamps for frames by using the last\ntimestamp seen in the stream before the frame starts.  In certain\ncases, the correct timestamps will occur in the stream after the\nstart of the frame, but before the start of the actual picture data.\nThis function can be used to set the timestamps based on the offset\ninto the frame data that the picture starts.\nParameters:\na GstBase.BaseParse\noffset into current buffer\n\n\tSince\t\t\t: 1.2\n\t\t\n"});