fragment_downloaded_cb({"url":"base/gstbasesink.html#gst_base_sink_get_drop_out_of_segment","fragment":"\n\tgst_base_sink_get_drop_out_of_segment\n\n\n\tGstBase.BaseSink.prototype.get_drop_out_of_segment\n\n\n\tGstBase.BaseSink.get_drop_out_of_segment\n\nChecks if sink is currently configured to drop buffers which are outside\nthe current segment\nParameters:\nthe sink\nTRUE if the sink is configured to drop buffers outside the\ncurrent segment.\n\n\tSince\t\t\t: 1.12\n\t\t\nChecks if sink is currently configured to drop buffers which are outside\nthe current segment\nParameters:\nthe sink\ntrue if the sink is configured to drop buffers outside the\ncurrent segment.\n\n\tSince\t\t\t: 1.12\n\t\t\nChecks if sink is currently configured to drop buffers which are outside\nthe current segment\nParameters:\nthe sink\nTrue if the sink is configured to drop buffers outside the\ncurrent segment.\n\n\tSince\t\t\t: 1.12\n\t\t\n"});