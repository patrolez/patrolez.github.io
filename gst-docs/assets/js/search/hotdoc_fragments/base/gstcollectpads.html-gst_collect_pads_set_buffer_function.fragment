fragment_downloaded_cb({"url":"base/gstcollectpads.html#gst_collect_pads_set_buffer_function","fragment":"\n\tgst_collect_pads_set_buffer_function\n\n\n\tGstBase.CollectPads.prototype.set_buffer_function\n\n\n\tGstBase.CollectPads.set_buffer_function\n\nSet the callback function and user data that will be called with\nthe oldest buffer when all pads have been collected, or NULL on EOS.\nIf a buffer is passed, the callback owns a reference and must unref\nit.\nMT safe.\nParameters:\nthe collectpads to use\nthe function to set\nuser data passed to the function\nSet the callback function and user data that will be called with\nthe oldest buffer when all pads have been collected, or null on EOS.\nIf a buffer is passed, the callback owns a reference and must unref\nit.\nMT safe.\nParameters:\nthe collectpads to use\nthe function to set\nuser data passed to the function\nSet the callback function and user data that will be called with\nthe oldest buffer when all pads have been collected, or None on EOS.\nIf a buffer is passed, the callback owns a reference and must unref\nit.\nMT safe.\nParameters:\nthe collectpads to use\nthe function to set\nuser data passed to the function\n"});