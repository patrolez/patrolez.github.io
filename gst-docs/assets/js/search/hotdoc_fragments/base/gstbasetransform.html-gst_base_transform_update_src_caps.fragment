fragment_downloaded_cb({"url":"base/gstbasetransform.html#gst_base_transform_update_src_caps","fragment":"\n\tgst_base_transform_update_src_caps\n\n\n\tGstBase.BaseTransform.prototype.update_src_caps\n\n\n\tGstBase.BaseTransform.update_src_caps\n\nUpdates the srcpad caps and sends the caps downstream. This function\ncan be used by subclasses when they have already negotiated their caps\nbut found a change in them (or computed new information). This way,\nthey can notify downstream about that change without losing any\nbuffer.\nParameters:\na GstBaseTransform\nAn updated version of the srcpad caps to be pushed\ndownstream\nTRUE if the caps could be sent downstream FALSE otherwise\n\n\tSince\t\t\t: 1.6\n\t\t\nUpdates the srcpad caps and sends the caps downstream. This function\ncan be used by subclasses when they have already negotiated their caps\nbut found a change in them (or computed new information). This way,\nthey can notify downstream about that change without losing any\nbuffer.\nParameters:\na GstBase.BaseTransform\nAn updated version of the srcpad caps to be pushed\ndownstream\ntrue if the caps could be sent downstream false otherwise\n\n\tSince\t\t\t: 1.6\n\t\t\nUpdates the srcpad caps and sends the caps downstream. This function\ncan be used by subclasses when they have already negotiated their caps\nbut found a change in them (or computed new information). This way,\nthey can notify downstream about that change without losing any\nbuffer.\nParameters:\na GstBase.BaseTransform\nAn updated version of the srcpad caps to be pushed\ndownstream\nTrue if the caps could be sent downstream False otherwise\n\n\tSince\t\t\t: 1.6\n\t\t\n"});