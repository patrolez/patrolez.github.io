fragment_downloaded_cb({"url":"base/gstaggregator.html#gst_aggregator_set_latency","fragment":"\n\tgst_aggregator_set_latency\n\n\n\tGstBase.Aggregator.prototype.set_latency\n\n\n\tGstBase.Aggregator.set_latency\n\nLets GstAggregator sub-classes tell the baseclass what their internal\nlatency is. Will also post a LATENCY message on the bus so the pipeline\ncan reconfigure its global latency if the values changed.\nParameters:\na GstAggregator\nminimum latency\nmaximum latency\nLets GstBase.Aggregator sub-classes tell the baseclass what their internal\nlatency is. Will also post a LATENCY message on the bus so the pipeline\ncan reconfigure its global latency if the values changed.\nParameters:\na GstBase.Aggregator\nminimum latency\nmaximum latency\nLets GstBase.Aggregator sub-classes tell the baseclass what their internal\nlatency is. Will also post a LATENCY message on the bus so the pipeline\ncan reconfigure its global latency if the values changed.\nParameters:\na GstBase.Aggregator\nminimum latency\nmaximum latency\n"});