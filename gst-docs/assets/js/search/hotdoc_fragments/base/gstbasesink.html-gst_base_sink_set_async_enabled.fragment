fragment_downloaded_cb({"url":"base/gstbasesink.html#gst_base_sink_set_async_enabled","fragment":"\n\tgst_base_sink_set_async_enabled\n\n\n\tGstBase.BaseSink.prototype.set_async_enabled\n\n\n\tGstBase.BaseSink.set_async_enabled\n\nConfigures sink to perform all state changes asynchronously. When async is\ndisabled, the sink will immediately go to PAUSED instead of waiting for a\npreroll buffer. This feature is useful if the sink does not synchronize\nagainst the clock or when it is dealing with sparse streams.\nParameters:\nthe sink\nthe new async value.\nConfigures sink to perform all state changes asynchronously. When async is\ndisabled, the sink will immediately go to PAUSED instead of waiting for a\npreroll buffer. This feature is useful if the sink does not synchronize\nagainst the clock or when it is dealing with sparse streams.\nParameters:\nthe sink\nthe new async value.\nConfigures sink to perform all state changes asynchronously. When async is\ndisabled, the sink will immediately go to PAUSED instead of waiting for a\npreroll buffer. This feature is useful if the sink does not synchronize\nagainst the clock or when it is dealing with sparse streams.\nParameters:\nthe sink\nthe new async value.\n"});