fragment_downloaded_cb({"url":"base/gstadapter.html#gst_adapter_copy_bytes","fragment":"\n\tgst_adapter_copy_bytes\n\n\n\tGstBase.Adapter.prototype.copy_bytes\n\n\n\tGstBase.Adapter.copy_bytes\n\nSimilar to gst_adapter_copy, but more suitable for language bindings. size\nbytes of data starting at offset will be copied out of the buffers contained\nin adapter and into a new GBytes structure which is returned. Depending on\nthe value of the size argument an empty GBytes structure may be returned.\nParameters:\na GstAdapter\nthe bytes offset in the adapter to start from\nthe number of bytes to copy\nA new GBytes structure containing the copied data.\n\n\tSince\t\t\t: 1.4\n\t\t\nSimilar to gst_adapter_copy, but more suitable for language bindings. size\nbytes of data starting at offset will be copied out of the buffers contained\nin adapter and into a new GLib.Bytes structure which is returned. Depending on\nthe value of the size argument an empty GLib.Bytes structure may be returned.\nParameters:\na GstBase.Adapter\nthe bytes offset in the adapter to start from\nthe number of bytes to copy\nA new GLib.Bytes structure containing the copied data.\n\n\tSince\t\t\t: 1.4\n\t\t\nSimilar to gst_adapter_copy, but more suitable for language bindings. size\nbytes of data starting at offset will be copied out of the buffers contained\nin adapter and into a new GLib.Bytes structure which is returned. Depending on\nthe value of the size argument an empty GLib.Bytes structure may be returned.\nParameters:\na GstBase.Adapter\nthe bytes offset in the adapter to start from\nthe number of bytes to copy\nA new GLib.Bytes structure containing the copied data.\n\n\tSince\t\t\t: 1.4\n\t\t\n"});