fragment_downloaded_cb({"url":"base/gstbasetransform.html#gst_base_transform_reconfigure","fragment":"\n\tgst_base_transform_reconfigure\n\n\n\tGstBase.BaseTransform.prototype.reconfigure\n\n\n\tGstBase.BaseTransform.reconfigure\n\nNegotiates src pad caps with downstream elements if the source pad is\nmarked as needing reconfiguring. Unmarks GST_PAD_FLAG_NEED_RECONFIGURE in\nany case. But marks it again if negotiation fails.\nDo not call this in the transform or\ntransform_ip vmethod. Call this in\nsubmit_input_buffer,\nprepare_output_buffer or in\ngenerate_output before any output buffer is\nallocated.\nIt will be default be called when handling an ALLOCATION query or at the\nvery beginning of the default submit_input_buffer\nimplementation.\nParameters:\nthe GstBaseTransform to set\nTRUE if the negotiation succeeded, else FALSE.\n\n\tSince\t\t\t: 1.18\n\t\t\nNegotiates src pad caps with downstream elements if the source pad is\nmarked as needing reconfiguring. Unmarks GST_PAD_FLAG_NEED_RECONFIGURE in\nany case. But marks it again if negotiation fails.\nDo not call this in the vfunc_transform or\nvfunc_transform_ip vmethod. Call this in\nvfunc_submit_input_buffer,\nvfunc_prepare_output_buffer or in\nvfunc_generate_output before any output buffer is\nallocated.\nIt will be default be called when handling an ALLOCATION query or at the\nvery beginning of the default vfunc_submit_input_buffer\nimplementation.\nParameters:\nthe GstBase.BaseTransform to set\ntrue if the negotiation succeeded, else false.\n\n\tSince\t\t\t: 1.18\n\t\t\nNegotiates src pad caps with downstream elements if the source pad is\nmarked as needing reconfiguring. Unmarks GST_PAD_FLAG_NEED_RECONFIGURE in\nany case. But marks it again if negotiation fails.\nDo not call this in the do_transform or\ndo_transform_ip vmethod. Call this in\ndo_submit_input_buffer,\ndo_prepare_output_buffer or in\ndo_generate_output before any output buffer is\nallocated.\nIt will be default be called when handling an ALLOCATION query or at the\nvery beginning of the default do_submit_input_buffer\nimplementation.\nParameters:\nthe GstBase.BaseTransform to set\nTrue if the negotiation succeeded, else False.\n\n\tSince\t\t\t: 1.18\n\t\t\n"});