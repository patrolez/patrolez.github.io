fragment_downloaded_cb({"url":"base/gstaggregator.html#gst_aggregator_get_latency","fragment":"\n\tgst_aggregator_get_latency\n\n\n\tGstBase.Aggregator.prototype.get_latency\n\n\n\tGstBase.Aggregator.get_latency\n\nRetrieves the latency values reported by self in response to the latency\nquery, or GST_CLOCK_TIME_NONE if there is not live source connected and the element\nwill not wait for the clock.\nTypically only called by subclasses.\nParameters:\na GstAggregator\nThe latency or GST_CLOCK_TIME_NONE if the element does not sync\nRetrieves the latency values reported by self in response to the latency\nquery, or Gst.CLOCK_TIME_NONE if there is not live source connected and the element\nwill not wait for the clock.\nTypically only called by subclasses.\nParameters:\na GstBase.Aggregator\nThe latency or Gst.CLOCK_TIME_NONE if the element does not sync\nRetrieves the latency values reported by self in response to the latency\nquery, or Gst.CLOCK_TIME_NONE if there is not live source connected and the element\nwill not wait for the clock.\nTypically only called by subclasses.\nParameters:\na GstBase.Aggregator\nThe latency or Gst.CLOCK_TIME_NONE if the element does not sync\n"});