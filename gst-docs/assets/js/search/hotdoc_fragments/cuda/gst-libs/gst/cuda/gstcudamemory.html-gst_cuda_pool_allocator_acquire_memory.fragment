fragment_downloaded_cb({"url":"cuda/gst-libs/gst/cuda/gstcudamemory.html#gst_cuda_pool_allocator_acquire_memory","fragment":"\n\tgst_cuda_pool_allocator_acquire_memory\n\n\n\tGstCuda.CudaPoolAllocator.prototype.acquire_memory\n\n\n\tGstCuda.CudaPoolAllocator.acquire_memory\n\nAcquires a GstMemory from allocator. memory should point to a memory\nlocation that can hold a pointer to the new GstMemory.\nParameters:\na GstCudaPoolAllocator\na GstMemory\na GstFlowReturn such as GST_FLOW_FLUSHING when the allocator is\ninactive.\n\n\tSince\t\t\t: 1.24\n\t\t\nAcquires a Gst.Memory from allocator. memory should point to a memory\nlocation that can hold a pointer to the new Gst.Memory.\nParameters:\na GstCuda.CudaPoolAllocator\nReturns a tuple made of:\na Gst.FlowReturn such as Gst.FlowReturn.FLUSHING when the allocator is\ninactive.\na Gst.FlowReturn such as Gst.FlowReturn.FLUSHING when the allocator is\ninactive.\n\n\tSince\t\t\t: 1.24\n\t\t\nAcquires a Gst.Memory from allocator. memory should point to a memory\nlocation that can hold a pointer to the new Gst.Memory.\nParameters:\na GstCuda.CudaPoolAllocator\nReturns a tuple made of:\na Gst.FlowReturn such as Gst.FlowReturn.FLUSHING when the allocator is\ninactive.\na Gst.FlowReturn such as Gst.FlowReturn.FLUSHING when the allocator is\ninactive.\n\n\tSince\t\t\t: 1.24\n\t\t\n"});