fragment_downloaded_cb({"url":"valib/gstvaallocator.html#GstVaDmabufAllocator","fragment":"\n\tVaDmabufAllocator\n\n\n\tVaDmabufAllocator\n\n\n\tVaDmabufAllocator\n\nA pooled memory allocator backed by the DMABufs exported from a\nVASurfaceID. Also it is possible to import DMAbufs into a\nVASurfaceID.\n\n\tSince\t\t\t: 1.22\n\t\t\nA pooled memory allocator backed by the DMABufs exported from a\nVASurfaceID. Also it is possible to import DMAbufs into a\nVASurfaceID.\n\n\tSince\t\t\t: 1.22\n\t\t\nA pooled memory allocator backed by the DMABufs exported from a\nVASurfaceID. Also it is possible to import DMAbufs into a\nVASurfaceID.\n\n\tSince\t\t\t: 1.22\n\t\t\n"});