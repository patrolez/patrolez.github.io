fragment_downloaded_cb({"url":"nle/NleObject.html#NleObject::commit","fragment":"\n\tcommit\n\nAction signal to commit all the pending changes of the composition and\nits children timing properties\nParameters:\na NleObject\nWhether to commit recursiverly into (NleComposition) children of\nobject. This is used in case we have composition inside\na nlesource composition, telling it to commit the included\ncomposition state.\nTRUE if changes have been commited, FALSE if nothing had to\nbe commited\n\n\tFlags:\t\n\t\n\t\t\n\t\t\t\tRun Last\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tAction\n\t\t\t\n\n"});