fragment_downloaded_cb({"url":"video/video-format.html#gst_video_format_from_masks","fragment":"\n\tgst_video_format_from_masks\n\n\n\tGstVideo.prototype.video_format_from_masks\n\n\n\tGstVideo.video_format_from_masks\n\nFind the GstVideoFormat for the given parameters.\nParameters:\nthe amount of bits used for a pixel\nthe amount of bits used to store a pixel. This value is bigger than\ndepth\nthe endianness of the masks, G_LITTLE_ENDIAN or G_BIG_ENDIAN\nthe red mask\nthe green mask\nthe blue mask\nthe alpha mask, or 0 if no alpha mask\na GstVideoFormat or GST_VIDEO_FORMAT_UNKNOWN when the parameters to\nnot specify a known format.\nFind the GstVideo.VideoFormat for the given parameters.\nParameters:\nthe amount of bits used for a pixel\nthe amount of bits used to store a pixel. This value is bigger than\ndepth\nthe endianness of the masks, GLib.LITTLE_ENDIAN or GLib.BIG_ENDIAN\nthe red mask\nthe green mask\nthe blue mask\nthe alpha mask, or 0 if no alpha mask\na GstVideo.VideoFormat or GST_VIDEO_FORMAT_UNKNOWN when the parameters to\nnot specify a known format.\nFind the GstVideo.VideoFormat for the given parameters.\nParameters:\nthe amount of bits used for a pixel\nthe amount of bits used to store a pixel. This value is bigger than\ndepth\nthe endianness of the masks, GLib.LITTLE_ENDIAN or GLib.BIG_ENDIAN\nthe red mask\nthe green mask\nthe blue mask\nthe alpha mask, or 0 if no alpha mask\na GstVideo.VideoFormat or GST_VIDEO_FORMAT_UNKNOWN when the parameters to\nnot specify a known format.\n"});