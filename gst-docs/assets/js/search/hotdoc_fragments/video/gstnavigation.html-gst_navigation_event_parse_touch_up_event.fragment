fragment_downloaded_cb({"url":"video/gstnavigation.html#gst_navigation_event_parse_touch_up_event","fragment":"\n\tgst_navigation_event_parse_touch_up_event\n\n\n\tGstVideo.prototype.navigation_event_parse_touch_up_event\n\n\n\tGstVideo.navigation_event_parse_touch_up_event\n\nRetrieve the details of a GstNavigation touch-up event.\nParameters:\nA GstEvent to inspect.\nPointer to a guint that will receive the\nidentifier unique to this touch point.\nPointer to a gdouble that will receive the x\ncoordinate of the touch event.\nPointer to a gdouble that will receive the y\ncoordinate of the touch event.\nTRUE if all details could be extracted, otherwise FALSE.\n\n\tSince\t\t\t: 1.22\n\t\t\nRetrieve the details of a GstVideo.Navigation touch-up event.\nParameters:\nA Gst.Event to inspect.\nReturns a tuple made of:\nTRUE if all details could be extracted, otherwise FALSE.\nTRUE if all details could be extracted, otherwise FALSE.\nTRUE if all details could be extracted, otherwise FALSE.\nTRUE if all details could be extracted, otherwise FALSE.\n\n\tSince\t\t\t: 1.22\n\t\t\nRetrieve the details of a GstVideo.Navigation touch-up event.\nParameters:\nA Gst.Event to inspect.\nReturns a tuple made of:\nTRUE if all details could be extracted, otherwise FALSE.\nTRUE if all details could be extracted, otherwise FALSE.\nTRUE if all details could be extracted, otherwise FALSE.\nTRUE if all details could be extracted, otherwise FALSE.\n\n\tSince\t\t\t: 1.22\n\t\t\n"});