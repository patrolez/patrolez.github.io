fragment_downloaded_cb({"url":"video/gstvideometa.html#gst_buffer_add_video_meta","fragment":"\n\tgst_buffer_add_video_meta\n\n\n\tGstVideo.prototype.buffer_add_video_meta\n\n\n\tGstVideo.buffer_add_video_meta\n\nAttaches GstVideoMeta metadata to buffer with the given parameters and the\ndefault offsets and strides for format and width x height.\nThis function calculates the default offsets and strides and then calls\ngst_buffer_add_video_meta_full with them.\nParameters:\na GstBuffer\nGstVideoFrameFlags\na GstVideoFormat\nthe width\nthe height\nthe GstVideoMeta on buffer.\nAttaches GstVideoMeta metadata to buffer with the given parameters and the\ndefault offsets and strides for format and width x height.\nThis function calculates the default offsets and strides and then calls\nGstVideo.prototype.buffer_add_video_meta_full with them.\nParameters:\na Gst.Buffer\nGstVideo.VideoFrameFlags\na GstVideo.VideoFormat\nthe width\nthe height\nthe GstVideo.VideoMeta on buffer.\nAttaches GstVideoMeta metadata to buffer with the given parameters and the\ndefault offsets and strides for format and width x height.\nThis function calculates the default offsets and strides and then calls\nGstVideo.buffer_add_video_meta_full with them.\nParameters:\na Gst.Buffer\nGstVideo.VideoFrameFlags\na GstVideo.VideoFormat\nthe width\nthe height\nthe GstVideo.VideoMeta on buffer.\n"});