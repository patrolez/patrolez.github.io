fragment_downloaded_cb({"url":"video/video-multiview.html#gst_video_multiview_get_doubled_size_modes","fragment":"\n\tgst_video_multiview_get_doubled_size_modes\n\n\n\tGstVideo.prototype.video_multiview_get_doubled_size_modes\n\n\n\tGstVideo.video_multiview_get_doubled_size_modes\n\nA const GValue containing a list of stereo video modes\nUtility function that returns a GValue with a GstList of packed\nstereo video modes that have double the width/height of a single\nview for use in caps negotiation. Currently this is just\n'checkerboard' layout.\n\n\tSince\t\t\t: 1.6\n\t\t\nA const GObject.Value containing a list of stereo video modes\nUtility function that returns a GObject.Value with a GstList of packed\nstereo video modes that have double the width/height of a single\nview for use in caps negotiation. Currently this is just\n'checkerboard' layout.\n\n\tSince\t\t\t: 1.6\n\t\t\nA const GObject.Value containing a list of stereo video modes\nUtility function that returns a GObject.Value with a GstList of packed\nstereo video modes that have double the width/height of a single\nview for use in caps negotiation. Currently this is just\n'checkerboard' layout.\n\n\tSince\t\t\t: 1.6\n\t\t\n"});