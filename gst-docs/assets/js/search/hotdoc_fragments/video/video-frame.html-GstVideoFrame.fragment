fragment_downloaded_cb({"url":"video/video-frame.html#GstVideoFrame","fragment":"\n\tGstVideoFrame\n\nMembers\n\n\tGstVideo.VideoFrame\n\nMembers\n\n\tGstVideo.VideoFrame\n\nMembers\nA video frame obtained from gst_video_frame_map\nthe GstVideoInfo\nGstVideoFrameFlags for the frame\nthe mapped buffer\npointer to metadata if any\nid of the mapped frame. the id can for example be used to\nidentify the frame in case of multiview video.\npointers to the plane data\nmappings of the planes\nA video frame obtained from GstVideo.prototype.video_frame_map\nthe GstVideo.VideoInfo\nGstVideo.VideoFrameFlags for the frame\nthe mapped buffer\npointer to metadata if any\nid of the mapped frame. the id can for example be used to\nidentify the frame in case of multiview video.\npointers to the plane data\nmappings of the planes\nA video frame obtained from GstVideo.video_frame_map\nthe GstVideo.VideoInfo\nGstVideo.VideoFrameFlags for the frame\nthe mapped buffer\npointer to metadata if any\nid of the mapped frame. the id can for example be used to\nidentify the frame in case of multiview video.\npointers to the plane data\nmappings of the planes\n"});