fragment_downloaded_cb({"url":"video/video-multiview.html#gst_video_multiview_get_doubled_width_modes","fragment":"\n\tgst_video_multiview_get_doubled_width_modes\n\n\n\tGstVideo.prototype.video_multiview_get_doubled_width_modes\n\n\n\tGstVideo.video_multiview_get_doubled_width_modes\n\nA const GValue containing a list of stereo video modes\nUtility function that returns a GValue with a GstList of packed stereo\nvideo modes with double the width of a single view for use in\ncaps negotiations. Currently this is side-by-side, side-by-side-quincunx\nand column-interleaved.\n\n\tSince\t\t\t: 1.6\n\t\t\nA const GObject.Value containing a list of stereo video modes\nUtility function that returns a GObject.Value with a GstList of packed stereo\nvideo modes with double the width of a single view for use in\ncaps negotiations. Currently this is side-by-side, side-by-side-quincunx\nand column-interleaved.\n\n\tSince\t\t\t: 1.6\n\t\t\nA const GObject.Value containing a list of stereo video modes\nUtility function that returns a GObject.Value with a GstList of packed stereo\nvideo modes with double the width of a single view for use in\ncaps negotiations. Currently this is side-by-side, side-by-side-quincunx\nand column-interleaved.\n\n\tSince\t\t\t: 1.6\n\t\t\n"});