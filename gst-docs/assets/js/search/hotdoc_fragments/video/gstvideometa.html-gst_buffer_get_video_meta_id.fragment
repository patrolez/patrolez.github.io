fragment_downloaded_cb({"url":"video/gstvideometa.html#gst_buffer_get_video_meta_id","fragment":"\n\tgst_buffer_get_video_meta_id\n\n\n\tGstVideo.prototype.buffer_get_video_meta_id\n\n\n\tGstVideo.buffer_get_video_meta_id\n\nFind the GstVideoMeta on buffer with the given id.\nBuffers can contain multiple GstVideoMeta metadata items when dealing with\nmultiview buffers.\nParameters:\na GstBuffer\na metadata id\nthe GstVideoMeta with id or NULL when there is no such metadata\non buffer.\nFind the GstVideo.VideoMeta on buffer with the given id.\nBuffers can contain multiple GstVideo.VideoMeta metadata items when dealing with\nmultiview buffers.\nParameters:\na Gst.Buffer\na metadata id\nthe GstVideo.VideoMeta with id or null when there is no such metadata\non buffer.\nFind the GstVideo.VideoMeta on buffer with the given id.\nBuffers can contain multiple GstVideo.VideoMeta metadata items when dealing with\nmultiview buffers.\nParameters:\na Gst.Buffer\na metadata id\nthe GstVideo.VideoMeta with id or None when there is no such metadata\non buffer.\n"});