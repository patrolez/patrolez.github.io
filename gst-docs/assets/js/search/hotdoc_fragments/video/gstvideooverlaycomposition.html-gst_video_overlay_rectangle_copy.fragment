fragment_downloaded_cb({"url":"video/gstvideooverlaycomposition.html#gst_video_overlay_rectangle_copy","fragment":"\n\tgst_video_overlay_rectangle_copy\n\n\n\tGstVideo.VideoOverlayRectangle.prototype.copy\n\n\n\tGstVideo.VideoOverlayRectangle.copy\n\nMakes a copy of rectangle, so that it is possible to modify it\n(e.g. to change the render co-ordinates or render dimension). The\nactual overlay pixel data buffers contained in the rectangle are not\ncopied.\nParameters:\na GstVideoOverlayRectangle to copy\na new GstVideoOverlayRectangle equivalent\nto rectangle.\nMakes a copy of rectangle, so that it is possible to modify it\n(e.g. to change the render co-ordinates or render dimension). The\nactual overlay pixel data buffers contained in the rectangle are not\ncopied.\nParameters:\na GstVideo.VideoOverlayRectangle to copy\na new GstVideo.VideoOverlayRectangle equivalent\nto rectangle.\nMakes a copy of rectangle, so that it is possible to modify it\n(e.g. to change the render co-ordinates or render dimension). The\nactual overlay pixel data buffers contained in the rectangle are not\ncopied.\nParameters:\na GstVideo.VideoOverlayRectangle to copy\na new GstVideo.VideoOverlayRectangle equivalent\nto rectangle.\n"});