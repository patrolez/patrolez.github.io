fragment_downloaded_cb({"url":"video/video-frame.html#GstVideoFrameMapFlags","fragment":"\n\tGstVideoFrameMapFlags\n\nMembers\n\n\tGstVideo.VideoFrameMapFlags\n\nMembers\n\n\tGstVideo.VideoFrameMapFlags\n\nMembers\nAdditional mapping flags for gst_video_frame_map.\nDon't take another reference of the buffer and store it in\nthe GstVideoFrame. This makes sure that the buffer stays\nwritable while the frame is mapped, but requires that the\nbuffer reference stays valid until the frame is unmapped again.\nOffset to define more flags\n\n\tSince\t\t\t: 1.6\n\t\t\nAdditional mapping flags for GstVideo.prototype.video_frame_map.\nDon't take another reference of the buffer and store it in\nthe GstVideoFrame. This makes sure that the buffer stays\nwritable while the frame is mapped, but requires that the\nbuffer reference stays valid until the frame is unmapped again.\nOffset to define more flags\n\n\tSince\t\t\t: 1.6\n\t\t\nAdditional mapping flags for GstVideo.video_frame_map.\nDon't take another reference of the buffer and store it in\nthe GstVideoFrame. This makes sure that the buffer stays\nwritable while the frame is mapped, but requires that the\nbuffer reference stays valid until the frame is unmapped again.\nOffset to define more flags\n\n\tSince\t\t\t: 1.6\n\t\t\n"});