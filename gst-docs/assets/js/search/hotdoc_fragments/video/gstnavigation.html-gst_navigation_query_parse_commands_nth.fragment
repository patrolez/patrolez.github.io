fragment_downloaded_cb({"url":"video/gstnavigation.html#gst_navigation_query_parse_commands_nth","fragment":"\n\tgst_navigation_query_parse_commands_nth\n\n\n\tGstVideo.prototype.navigation_query_parse_commands_nth\n\n\n\tGstVideo.navigation_query_parse_commands_nth\n\nParse the GstNavigation command query and retrieve the nth command from\nit into cmd. If the list contains less elements than nth, cmd will be\nset to GST_NAVIGATION_COMMAND_INVALID.\nParameters:\na GstQuery\nthe nth command to retrieve.\na pointer to store the nth command into.\nTRUE if the query could be successfully parsed. FALSE if not.\nParse the GstVideo.Navigation command query and retrieve the nth command from\nit into cmd. If the list contains less elements than nth, cmd will be\nset to GstVideo.NavigationCommand.INVALID.\nParameters:\na Gst.Query\nthe nth command to retrieve.\nReturns a tuple made of:\ntrue if the query could be successfully parsed. false if not.\ntrue if the query could be successfully parsed. false if not.\nParse the GstVideo.Navigation command query and retrieve the nth command from\nit into cmd. If the list contains less elements than nth, cmd will be\nset to GstVideo.NavigationCommand.INVALID.\nParameters:\na Gst.Query\nthe nth command to retrieve.\nReturns a tuple made of:\nTrue if the query could be successfully parsed. False if not.\nTrue if the query could be successfully parsed. False if not.\n"});