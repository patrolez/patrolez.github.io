fragment_downloaded_cb({"url":"video/gstvideoencoder.html#gst_video_encoder_set_latency","fragment":"\n\tgst_video_encoder_set_latency\n\n\n\tGstVideo.VideoEncoder.prototype.set_latency\n\n\n\tGstVideo.VideoEncoder.set_latency\n\nInforms baseclass of encoding latency. If the provided values changed from\npreviously provided ones, this will also post a LATENCY message on the bus\nso the pipeline can reconfigure its global latency.\nParameters:\na GstVideoEncoder\nminimum latency\nmaximum latency\nInforms baseclass of encoding latency. If the provided values changed from\npreviously provided ones, this will also post a LATENCY message on the bus\nso the pipeline can reconfigure its global latency.\nParameters:\na GstVideo.VideoEncoder\nminimum latency\nmaximum latency\nInforms baseclass of encoding latency. If the provided values changed from\npreviously provided ones, this will also post a LATENCY message on the bus\nso the pipeline can reconfigure its global latency.\nParameters:\na GstVideo.VideoEncoder\nminimum latency\nmaximum latency\n"});