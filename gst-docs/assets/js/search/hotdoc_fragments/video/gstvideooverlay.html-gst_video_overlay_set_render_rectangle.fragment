fragment_downloaded_cb({"url":"video/gstvideooverlay.html#gst_video_overlay_set_render_rectangle","fragment":"\n\tgst_video_overlay_set_render_rectangle\n\n\n\tGstVideo.VideoOverlay.prototype.set_render_rectangle\n\n\n\tGstVideo.VideoOverlay.set_render_rectangle\n\nConfigure a subregion as a video target within the window set by\ngst_video_overlay_set_window_handle. If this is not used or not supported\nthe video will fill the area of the window set as the overlay to 100%.\nBy specifying the rectangle, the video can be overlayed to a specific region\nof that window only. After setting the new rectangle one should call\ngst_video_overlay_expose to force a redraw. To unset the region pass -1 for\nthe width and height parameters.\nThis method is needed for non fullscreen video overlay in UI toolkits that\ndo not support subwindows.\nParameters:\na GstVideoOverlay\nthe horizontal offset of the render area inside the window\nthe vertical offset of the render area inside the window\nthe width of the render area inside the window\nthe height of the render area inside the window\nFALSE if not supported by the sink.\nConfigure a subregion as a video target within the window set by\nGstVideo.VideoOverlay.prototype.set_window_handle. If this is not used or not supported\nthe video will fill the area of the window set as the overlay to 100%.\nBy specifying the rectangle, the video can be overlayed to a specific region\nof that window only. After setting the new rectangle one should call\nGstVideo.VideoOverlay.prototype.expose to force a redraw. To unset the region pass -1 for\nthe width and height parameters.\nThis method is needed for non fullscreen video overlay in UI toolkits that\ndo not support subwindows.\nParameters:\na GstVideo.VideoOverlay\nthe horizontal offset of the render area inside the window\nthe vertical offset of the render area inside the window\nthe width of the render area inside the window\nthe height of the render area inside the window\nfalse if not supported by the sink.\nConfigure a subregion as a video target within the window set by\nGstVideo.VideoOverlay.set_window_handle. If this is not used or not supported\nthe video will fill the area of the window set as the overlay to 100%.\nBy specifying the rectangle, the video can be overlayed to a specific region\nof that window only. After setting the new rectangle one should call\nGstVideo.VideoOverlay.expose to force a redraw. To unset the region pass -1 for\nthe width and height parameters.\nThis method is needed for non fullscreen video overlay in UI toolkits that\ndo not support subwindows.\nParameters:\na GstVideo.VideoOverlay\nthe horizontal offset of the render area inside the window\nthe vertical offset of the render area inside the window\nthe width of the render area inside the window\nthe height of the render area inside the window\nFalse if not supported by the sink.\n"});