fragment_downloaded_cb({"url":"video/gstvideotimecode.html#gst_video_time_code_compare","fragment":"\n\tgst_video_time_code_compare\n\n\n\tGstVideo.VideoTimeCode.prototype.compare\n\n\n\tGstVideo.VideoTimeCode.compare\n\nCompares tc1 and tc2. If both have latest daily jam information, it is\ntaken into account. Otherwise, it is assumed that the daily jam of both\ntc1 and tc2 was at the same time. Both time codes must be valid.\nParameters:\na valid GstVideoTimeCode\nanother valid GstVideoTimeCode\n1 if tc1 is after tc2, -1 if tc1 is before tc2, 0 otherwise.\n\n\tSince\t\t\t: 1.10\n\t\t\nCompares tc1 and tc2. If both have latest daily jam information, it is\ntaken into account. Otherwise, it is assumed that the daily jam of both\ntc1 and tc2 was at the same time. Both time codes must be valid.\nParameters:\na valid GstVideo.VideoTimeCode\nanother valid GstVideo.VideoTimeCode\n1 if tc1 is after tc2, -1 if tc1 is before tc2, 0 otherwise.\n\n\tSince\t\t\t: 1.10\n\t\t\nCompares tc1 and tc2. If both have latest daily jam information, it is\ntaken into account. Otherwise, it is assumed that the daily jam of both\ntc1 and tc2 was at the same time. Both time codes must be valid.\nParameters:\na valid GstVideo.VideoTimeCode\nanother valid GstVideo.VideoTimeCode\n1 if tc1 is after tc2, -1 if tc1 is before tc2, 0 otherwise.\n\n\tSince\t\t\t: 1.10\n\t\t\n"});