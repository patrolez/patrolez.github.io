fragment_downloaded_cb({"url":"video/gstvideoencoder.html#gst_video_encoder_negotiate","fragment":"\n\tgst_video_encoder_negotiate\n\n\n\tGstVideo.VideoEncoder.prototype.negotiate\n\n\n\tGstVideo.VideoEncoder.negotiate\n\nNegotiate with downstream elements to currently configured GstVideoCodecState.\nUnmark GST_PAD_FLAG_NEED_RECONFIGURE in any case. But mark it again if\nnegotiate fails.\nParameters:\na GstVideoEncoder\nTRUE if the negotiation succeeded, else FALSE.\nNegotiate with downstream elements to currently configured GstVideo.VideoCodecState.\nUnmark GST_PAD_FLAG_NEED_RECONFIGURE in any case. But mark it again if\nnegotiate fails.\nParameters:\na GstVideo.VideoEncoder\ntrue if the negotiation succeeded, else false.\nNegotiate with downstream elements to currently configured GstVideo.VideoCodecState.\nUnmark GST_PAD_FLAG_NEED_RECONFIGURE in any case. But mark it again if\nnegotiate fails.\nParameters:\na GstVideo.VideoEncoder\nTrue if the negotiation succeeded, else False.\n"});