fragment_downloaded_cb({"url":"video/gstvideoutils.html#gst_video_codec_frame_set_user_data","fragment":"\n\tgst_video_codec_frame_set_user_data\n\n\n\tGstVideo.VideoCodecFrame.prototype.set_user_data\n\n\n\tGstVideo.VideoCodecFrame.set_user_data\n\nSets user_data on the frame and the GDestroyNotify that will be called when\nthe frame is freed. Allows to attach private data by the subclass to frames.\nIf a user_data was previously set, then the previous set notify will be called\nbefore the user_data is replaced.\nParameters:\na GstVideoCodecFrame\nprivate data\na GDestroyNotify\nSets user_data on the frame and the GLib.DestroyNotify that will be called when\nthe frame is freed. Allows to attach private data by the subclass to frames.\nIf a user_data was previously set, then the previous set notify will be called\nbefore the user_data is replaced.\nParameters:\na GstVideo.VideoCodecFrame\nprivate data\na GLib.DestroyNotify\nSets user_data on the frame and the GLib.DestroyNotify that will be called when\nthe frame is freed. Allows to attach private data by the subclass to frames.\nIf a user_data was previously set, then the previous set notify will be called\nbefore the user_data is replaced.\nParameters:\na GstVideo.VideoCodecFrame\nprivate data\na GLib.DestroyNotify\n"});