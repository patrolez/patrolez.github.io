fragment_downloaded_cb({"url":"video/gstvideoencoder.html#gst_video_encoder_proxy_getcaps","fragment":"\n\tgst_video_encoder_proxy_getcaps\n\n\n\tGstVideo.VideoEncoder.prototype.proxy_getcaps\n\n\n\tGstVideo.VideoEncoder.proxy_getcaps\n\nReturns caps that express caps (or sink template caps if caps == NULL)\nrestricted to resolution/format/... combinations supported by downstream\nelements (e.g. muxers).\nParameters:\na GstVideoEncoder\ninitial caps\nfilter caps\na GstCaps owned by caller\nReturns caps that express caps (or sink template caps if caps == NULL)\nrestricted to resolution/format/... combinations supported by downstream\nelements (e.g. muxers).\nParameters:\na GstVideo.VideoEncoder\ninitial caps\nfilter caps\na Gst.Caps owned by caller\nReturns caps that express caps (or sink template caps if caps == NULL)\nrestricted to resolution/format/... combinations supported by downstream\nelements (e.g. muxers).\nParameters:\na GstVideo.VideoEncoder\ninitial caps\nfilter caps\na Gst.Caps owned by caller\n"});