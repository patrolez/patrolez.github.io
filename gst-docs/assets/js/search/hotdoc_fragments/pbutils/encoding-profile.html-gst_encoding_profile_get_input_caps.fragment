fragment_downloaded_cb({"url":"pbutils/encoding-profile.html#gst_encoding_profile_get_input_caps","fragment":"\n\tgst_encoding_profile_get_input_caps\n\n\n\tGstPbutils.EncodingProfile.prototype.get_input_caps\n\n\n\tGstPbutils.EncodingProfile.get_input_caps\n\nComputes the full output caps that this profile will be able to consume.\nParameters:\na GstEncodingProfile\nThe full caps the given profile can consume. Call\ngst_caps_unref when you are done with the caps.\nComputes the full output caps that this profile will be able to consume.\nParameters:\na GstPbutils.EncodingProfile\nThe full caps the given profile can consume. Call\ngst_caps_unref (not introspectable) when you are done with the caps.\nComputes the full output caps that this profile will be able to consume.\nParameters:\na GstPbutils.EncodingProfile\nThe full caps the given profile can consume. Call\ngst_caps_unref (not introspectable) when you are done with the caps.\n"});