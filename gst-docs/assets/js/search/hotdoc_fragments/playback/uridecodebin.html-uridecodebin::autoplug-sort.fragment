fragment_downloaded_cb({"url":"playback/uridecodebin.html#uridecodebin::autoplug-sort","fragment":"\n\tautoplug-sort\n\nOnce decodebin has found the possible GstElementFactory objects to try\nfor caps on pad, this signal is emitted. The purpose of the signal is for\nthe application to perform additional sorting or filtering on the element\nfactory array.\nThe callee should copy and modify factories or return NULL if the\norder should not change.\nInvocation of signal handlers stops after one signal handler has\nreturned something else than NULL. Signal handlers are invoked in\nthe order they were connected in.\nDon't connect signal handlers with the G_CONNECT_AFTER flag to this\nsignal, they will never be invoked!\nParameters:\nThe uridecodebin.\nThe GstPad.\nThe GstCaps.\nA GValueArray of possible GstElementFactory to use.\nA new sorted array of GstElementFactory objects.\n\n\tFlags:\t\n\t\n\t\t\n\t\t\t\tRun Last\n\t\t\t\n\n"});