fragment_downloaded_cb({"url":"gl/gstglbasememory.html#gst_gl_base_memory_alloc_data","fragment":"\n\tgst_gl_base_memory_alloc_data\n\n\n\tGstGL.GLBaseMemory.prototype.alloc_data\n\n\n\tGstGL.GLBaseMemory.alloc_data\n\nNote: only intended for subclass usage to allocate the system memory buffer\non demand.  If there is already a non-NULL data pointer in gl_mem->data,\nthen this function imply returns TRUE.\nParameters:\na GstGLBaseMemory\nwhether the system memory could be allocated\nNote: only intended for subclass usage to allocate the system memory buffer\non demand.  If there is already a non-NULL data pointer in gl_mem->data,\nthen this function imply returns TRUE.\nParameters:\na GstGL.GLBaseMemory\nwhether the system memory could be allocated\nNote: only intended for subclass usage to allocate the system memory buffer\non demand.  If there is already a non-NULL data pointer in gl_mem->data,\nthen this function imply returns TRUE.\nParameters:\na GstGL.GLBaseMemory\nwhether the system memory could be allocated\n"});