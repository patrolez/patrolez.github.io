fragment_downloaded_cb({"url":"gl/gstglframebuffer.html#gst_gl_framebuffer_draw_to_texture","fragment":"\n\tgst_gl_framebuffer_draw_to_texture\n\n\n\tGstGL.GLFramebuffer.prototype.draw_to_texture\n\n\n\tGstGL.GLFramebuffer.draw_to_texture\n\nPerform the steps necessary to have the output of a glDraw* command in\nfunc update the contents of mem.\nNote: this function does not map mem for writing with OpenGL and that must\nbe done manually by the caller using any of the mapping functions such as\ngst_memory_map with the map flags GST_MAP_WRITE | GST_MAP_GL.\nMust be called with the same OpenGL context current that fb was created\nwith.\nParameters:\na GstGLFramebuffer\nthe GstGLMemory to draw to\nthe function to run\ndata to pass to func\nthe result of executing func\n\n\tSince\t\t\t: 1.10\n\t\t\nPerform the steps necessary to have the output of a glDraw* command in\nfunc update the contents of mem.\nNote: this function does not map mem for writing with OpenGL and that must\nbe done manually by the caller using any of the mapping functions such as\nGst.Memory.prototype.map with the map flags Gst.MapFlags.WRITE | GstGL.MAP_GL.\nMust be called with the same OpenGL context current that fb was created\nwith.\nParameters:\na GstGL.GLFramebuffer\nthe GstGL.GLMemory to draw to\nthe function to run\ndata to pass to func\nthe result of executing func\n\n\tSince\t\t\t: 1.10\n\t\t\nPerform the steps necessary to have the output of a glDraw* command in\nfunc update the contents of mem.\nNote: this function does not map mem for writing with OpenGL and that must\nbe done manually by the caller using any of the mapping functions such as\nGst.Memory.map with the map flags Gst.MapFlags.WRITE | GstGL.MAP_GL.\nMust be called with the same OpenGL context current that fb was created\nwith.\nParameters:\na GstGL.GLFramebuffer\nthe GstGL.GLMemory to draw to\nthe function to run\ndata to pass to func\nthe result of executing func\n\n\tSince\t\t\t: 1.10\n\t\t\n"});