fragment_downloaded_cb({"url":"gst-rtsp-server/rtsp-auth.html#GstRTSPAuth::accept-certificate","fragment":"\n\taccept-certificate\n\n\n\taccept-certificate\n\n\n\taccept-certificate\n\nEmitted during the TLS handshake after the client certificate has\nbeen received. See also gst_rtsp_auth_set_tls_authentication_mode.\nParameters:\na GTlsConnection\nthe peer's GTlsCertificate\nthe problems with peer_cert.\nTRUE to accept peer_cert (which will also\nimmediately end the signal emission). FALSE to allow the signal\nemission to continue, which will cause the handshake to fail if\nno one else overrides it.\n\n\tFlags:\t\n\t\n\t\t\n\t\t\t\tRun Last\n\t\t\t\n\n\n\tSince\t\t\t: 1.6\n\t\t\nEmitted during the TLS handshake after the client certificate has\nbeen received. See also GstRtspServer.RTSPAuth.prototype.set_tls_authentication_mode.\nParameters:\na Gio.TlsConnection\nthe peer's Gio.TlsCertificate\nthe problems with peer_cert.\ntrue to accept peer_cert (which will also\nimmediately end the signal emission). false to allow the signal\nemission to continue, which will cause the handshake to fail if\nno one else overrides it.\n\n\tFlags:\t\n\t\n\t\t\n\t\t\t\tRun Last\n\t\t\t\n\n\n\tSince\t\t\t: 1.6\n\t\t\nEmitted during the TLS handshake after the client certificate has\nbeen received. See also GstRtspServer.RTSPAuth.set_tls_authentication_mode.\nParameters:\na Gio.TlsConnection\nthe peer's Gio.TlsCertificate\nthe problems with peer_cert.\nTrue to accept peer_cert (which will also\nimmediately end the signal emission). False to allow the signal\nemission to continue, which will cause the handshake to fail if\nno one else overrides it.\n\n\tFlags:\t\n\t\n\t\t\n\t\t\t\tRun Last\n\t\t\t\n\n\n\tSince\t\t\t: 1.6\n\t\t\n"});