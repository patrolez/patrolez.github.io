fragment_downloaded_cb({"url":"gst-rtsp-server/rtsp-stream-transport.html#gst_rtsp_stream_transport_set_keepalive","fragment":"\n\tgst_rtsp_stream_transport_set_keepalive\n\n\n\tGstRtspServer.RTSPStreamTransport.prototype.set_keepalive\n\n\n\tGstRtspServer.RTSPStreamTransport.set_keepalive\n\nInstall callbacks that will be called when RTCP packets are received from the\nreceiver of trans.\nParameters:\na GstRTSPStreamTransport\na callback called when the receiver is active\nuser data passed to callback\ncalled with the user_data when no longer needed.\nInstall callbacks that will be called when RTCP packets are received from the\nreceiver of trans.\nParameters:\na GstRtspServer.RTSPStreamTransport\na callback called when the receiver is active\nuser data passed to callback\nInstall callbacks that will be called when RTCP packets are received from the\nreceiver of trans.\nParameters:\na GstRtspServer.RTSPStreamTransport\na callback called when the receiver is active\nuser data passed to callback\n"});