fragment_downloaded_cb({"url":"gst-rtsp-server/rtsp-media.html#GstRTSPMediaClass::prepare","fragment":"\n\tprepare\n\n\n\tvfunc_prepare\n\n\n\tdo_prepare\n\nthe default implementation adds all elements and sets the\npipeline's state to GST_STATE_PAUSED (or GST_STATE_PLAYING\nin case of NO_PREROLL elements).\nParameters:\nthe default implementation adds all elements and sets the\npipeline's state to GST_STATE_PAUSED (or GST_STATE_PLAYING\nin case of NO_PREROLL elements).\nParameters:\nthe default implementation adds all elements and sets the\npipeline's state to GST_STATE_PAUSED (or GST_STATE_PLAYING\nin case of NO_PREROLL elements).\nParameters:\n"});