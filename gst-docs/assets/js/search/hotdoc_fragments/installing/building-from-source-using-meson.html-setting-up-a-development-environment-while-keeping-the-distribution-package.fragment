fragment_downloaded_cb({"url":"installing/building-from-source-using-meson.html#setting-up-a-development-environment-while-keeping-the-distribution-package","fragment":"Setting up a development environment while keeping the distribution package\nThis case is very simple all you have to do is either:\n\n\n./gst-env.py from the project root\n\nninja -C build devenv (build is the generated meson build directory)\n\nmeson devenv from the meson build directory (e.g. build) within the\nproject root\n\n"});