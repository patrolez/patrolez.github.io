fragment_downloaded_cb({"url":"vulkanlib/vkoperation.html#gst_vulkan_operation_begin","fragment":"\n\tgst_vulkan_operation_begin\n\n\n\tGstVulkan.VulkanOperation.prototype.begin\n\n\n\tGstVulkan.VulkanOperation.begin\n\nSee also: gst_vulkan_operation_end and gst_vulkan_operation_reset\nAttempts to set the operation ready to work. It instantiates the common\ncommand buffer in self and calls vkBeginCommandBuffer.\nAfter calling this function you can register commands in the command buffer,\nand finally call gst_vulkan_operation_end. gst_vulkan_operation_reset is\ncalled internally if something failed.\nParameters:\na GstVulkanOperation\na GError\nwhether the operation started. It might fill error.\nSee also: GstVulkan.VulkanOperation.prototype.end and GstVulkan.VulkanOperation.prototype.reset\nAttempts to set the operation ready to work. It instantiates the common\ncommand buffer in self and calls vkBeginCommandBuffer.\nAfter calling this function you can register commands in the command buffer,\nand finally call GstVulkan.VulkanOperation.prototype.end. GstVulkan.VulkanOperation.prototype.reset is\ncalled internally if something failed.\nParameters:\na GstVulkan.VulkanOperation\nwhether the operation started. It might fill error.\nSee also: GstVulkan.VulkanOperation.end and GstVulkan.VulkanOperation.reset\nAttempts to set the operation ready to work. It instantiates the common\ncommand buffer in self and calls vkBeginCommandBuffer.\nAfter calling this function you can register commands in the command buffer,\nand finally call GstVulkan.VulkanOperation.end. GstVulkan.VulkanOperation.reset is\ncalled internally if something failed.\nParameters:\na GstVulkan.VulkanOperation\nwhether the operation started. It might fill error.\n"});