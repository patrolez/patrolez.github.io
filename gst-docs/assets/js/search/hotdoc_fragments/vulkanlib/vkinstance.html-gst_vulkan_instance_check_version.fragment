fragment_downloaded_cb({"url":"vulkanlib/vkinstance.html#gst_vulkan_instance_check_version","fragment":"\n\tgst_vulkan_instance_check_version\n\n\n\tGstVulkan.VulkanInstance.prototype.check_version\n\n\n\tGstVulkan.VulkanInstance.check_version\n\nCheck if the configured vulkan instance supports the specified version.\nWill not work prior to opening the instance with gst_vulkan_instance_open.\nIf a specific version is requested, the patch level is ignored.\nParameters:\na GstVulkanInstance\nmajor version\nminor version\npatch version\nwhether instance is at least the requested version.\n\n\tSince\t\t\t: 1.18\n\t\t\nCheck if the configured vulkan instance supports the specified version.\nWill not work prior to opening the instance with GstVulkan.VulkanInstance.prototype.open.\nIf a specific version is requested, the patch level is ignored.\nParameters:\na GstVulkan.VulkanInstance\nmajor version\nminor version\npatch version\nwhether instance is at least the requested version.\n\n\tSince\t\t\t: 1.18\n\t\t\nCheck if the configured vulkan instance supports the specified version.\nWill not work prior to opening the instance with GstVulkan.VulkanInstance.open.\nIf a specific version is requested, the patch level is ignored.\nParameters:\na GstVulkan.VulkanInstance\nmajor version\nminor version\npatch version\nwhether instance is at least the requested version.\n\n\tSince\t\t\t: 1.18\n\t\t\n"});