fragment_downloaded_cb({"url":"gst-editing-services/gestimelineelement.html#ges_timeline_element_get_layer_priority","fragment":"\n\tges_timeline_element_get_layer_priority\n\n\n\tGES.TimelineElement.prototype.get_layer_priority\n\n\n\tGES.TimelineElement.get_layer_priority\n\nGets the priority of the layer the element is in. A GESGroup may span\nseveral layers, so this would return the highest priority (numerically,\nthe smallest) amongst them.\nParameters:\nA GESTimelineElement\nThe priority of the layer self is in, or\nGES_TIMELINE_ELEMENT_NO_LAYER_PRIORITY if self does not exist in a\nlayer.\n\n\tSince\t\t\t: 1.16\n\t\t\nGets the priority of the layer the element is in. A GES.Group may span\nseveral layers, so this would return the highest priority (numerically,\nthe smallest) amongst them.\nParameters:\nA GES.TimelineElement\nThe priority of the layer self is in, or\nGES.TIMELINE_ELEMENT_NO_LAYER_PRIORITY if self does not exist in a\nlayer.\n\n\tSince\t\t\t: 1.16\n\t\t\nGets the priority of the layer the element is in. A GES.Group may span\nseveral layers, so this would return the highest priority (numerically,\nthe smallest) amongst them.\nParameters:\nA GES.TimelineElement\nThe priority of the layer self is in, or\nGES.TIMELINE_ELEMENT_NO_LAYER_PRIORITY if self does not exist in a\nlayer.\n\n\tSince\t\t\t: 1.16\n\t\t\n"});