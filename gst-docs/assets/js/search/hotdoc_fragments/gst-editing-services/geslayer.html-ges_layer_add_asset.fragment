fragment_downloaded_cb({"url":"gst-editing-services/geslayer.html#ges_layer_add_asset","fragment":"\n\tges_layer_add_asset\n\n\n\tGES.Layer.prototype.add_asset\n\n\n\tGES.Layer.add_asset\n\nSee ges_layer_add_asset_full, which also gives an error.\nParameters:\nThe GESLayer\nThe asset to extract the new clip from\nThe start value to set on the new clip\nIf start == #GST_CLOCK_TIME_NONE, it will be added to the end\nof layer, i.e. it will be set to layer's duration\nThe in-point value to set on the new\nclip\nThe duration value to set on the new\nclip\nThe supported-formats to set on the the new\nclip, or GES_TRACK_TYPE_UNKNOWN to use the default\nThe newly created clip.\nSee GES.Layer.prototype.add_asset_full, which also gives an error.\nParameters:\nThe GES.Layer\nThe asset to extract the new clip from\nThe start value to set on the new clip\nIf start == #GST_CLOCK_TIME_NONE, it will be added to the end\nof layer, i.e. it will be set to layer's duration\nThe in-point value to set on the new\nclip\nThe duration value to set on the new\nclip\nThe supported-formats to set on the the new\nclip, or GES.TrackType.UNKNOWN to use the default\nThe newly created clip.\nSee GES.Layer.add_asset_full, which also gives an error.\nParameters:\nThe GES.Layer\nThe asset to extract the new clip from\nThe start value to set on the new clip\nIf start == #GST_CLOCK_TIME_NONE, it will be added to the end\nof layer, i.e. it will be set to layer's duration\nThe in_point value to set on the new\nclip\nThe duration value to set on the new\nclip\nThe supported_formats to set on the the new\nclip, or GES.TrackType.UNKNOWN to use the default\nThe newly created clip.\n"});