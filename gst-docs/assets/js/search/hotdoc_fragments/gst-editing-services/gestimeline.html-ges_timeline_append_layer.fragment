fragment_downloaded_cb({"url":"gst-editing-services/gestimeline.html#ges_timeline_append_layer","fragment":"\n\tges_timeline_append_layer\n\n\n\tGES.Timeline.prototype.append_layer\n\n\n\tGES.Timeline.append_layer\n\nAppend a newly created layer to the timeline. The layer will\nbe added at the lowest priority (numerically, the highest).\nParameters:\nThe GESTimeline\nThe newly created layer.\nAppend a newly created layer to the timeline. The layer will\nbe added at the lowest priority (numerically, the highest).\nParameters:\nThe GES.Timeline\nThe newly created layer.\nAppend a newly created layer to the timeline. The layer will\nbe added at the lowest priority (numerically, the highest).\nParameters:\nThe GES.Timeline\nThe newly created layer.\n"});