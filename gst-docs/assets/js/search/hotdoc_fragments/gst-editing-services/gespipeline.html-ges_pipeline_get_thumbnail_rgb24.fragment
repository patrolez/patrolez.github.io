fragment_downloaded_cb({"url":"gst-editing-services/gespipeline.html#ges_pipeline_get_thumbnail_rgb24","fragment":"\n\tges_pipeline_get_thumbnail_rgb24\n\n\n\tGES.Pipeline.prototype.get_thumbnail_rgb24\n\n\n\tGES.Pipeline.get_thumbnail_rgb24\n\nGets a sample from the pipeline of the currently displayed image in\npreview, in the 24-bit \"RGB\" format and of the desired width and\nheight.\nSee ges_pipeline_get_thumbnail.\nParameters:\nA GESPipeline in GST_STATE_PLAYING or GST_STATE_PAUSED\nThe requested pixel width of the image, or -1 to use the native\nsize\nThe requested pixel height of the image, or -1 to use the\nnative size\nA sample of self's current image preview in\nthe \"RGB\" format, scaled to width and height, or NULL if an error\nprevented fetching the sample.\nGets a sample from the pipeline of the currently displayed image in\npreview, in the 24-bit \"RGB\" format and of the desired width and\nheight.\nSee GES.Pipeline.prototype.get_thumbnail.\nParameters:\nA GES.Pipeline in Gst.State.PLAYING or Gst.State.PAUSED\nThe requested pixel width of the image, or -1 to use the native\nsize\nThe requested pixel height of the image, or -1 to use the\nnative size\nA sample of self's current image preview in\nthe \"RGB\" format, scaled to width and height, or null if an error\nprevented fetching the sample.\nGets a sample from the pipeline of the currently displayed image in\npreview, in the 24-bit \"RGB\" format and of the desired width and\nheight.\nSee GES.Pipeline.get_thumbnail.\nParameters:\nA GES.Pipeline in Gst.State.PLAYING or Gst.State.PAUSED\nThe requested pixel width of the image, or -1 to use the native\nsize\nThe requested pixel height of the image, or -1 to use the\nnative size\nA sample of self's current image preview in\nthe \"RGB\" format, scaled to width and height, or None if an error\nprevented fetching the sample.\n"});