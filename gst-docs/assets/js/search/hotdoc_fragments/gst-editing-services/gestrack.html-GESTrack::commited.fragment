fragment_downloaded_cb({"url":"gst-editing-services/gestrack.html#GESTrack::commited","fragment":"\n\tcommited\n\n\n\tcommited\n\n\n\tcommited\n\nThis signal will be emitted once the changes initiated by\nges_track_commit have been executed in the backend. In particular,\nthis will be emitted whenever the underlying nlecomposition has been\ncommitted (see commited).\nParameters:\n\n\tFlags:\t\n\t\n\t\t\n\t\t\t\tRun Last\n\t\t\t\n\nThis signal will be emitted once the changes initiated by\nGES.Track.prototype.commit have been executed in the backend. In particular,\nthis will be emitted whenever the underlying nlecomposition (not introspectable) has been\ncommitted (see commited (not introspectable)).\nParameters:\n\n\tFlags:\t\n\t\n\t\t\n\t\t\t\tRun Last\n\t\t\t\n\nThis signal will be emitted once the changes initiated by\nGES.Track.commit have been executed in the backend. In particular,\nthis will be emitted whenever the underlying nlecomposition (not introspectable) has been\ncommitted (see commited (not introspectable)).\nParameters:\n\n\tFlags:\t\n\t\n\t\t\n\t\t\t\tRun Last\n\t\t\t\n\n"});