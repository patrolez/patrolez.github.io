fragment_downloaded_cb({"url":"gst-editing-services/gestrack.html#GESTrack:restriction-caps","fragment":"\n\trestriction-caps\n\n\n\trestriction-caps\n\n\n\trestriction_caps\n\nThe capabilities that specifies the final output format of the\nGESTrack. For example, for a video track, it would specify the\nheight, width, framerate and other properties of the stream.\nYou may change this property after the track has been added to a\nGESTimeline, but it must remain compatible with the track's\ncaps.\nDefault value: GST_CAPS_ANY.\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\nThe capabilities that specifies the final output format of the\nGES.Track. For example, for a video track, it would specify the\nheight, width, framerate and other properties of the stream.\nYou may change this property after the track has been added to a\nGES.Timeline, but it must remain compatible with the track's\ncaps.\nDefault value: GST_CAPS_ANY (not introspectable).\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\nThe capabilities that specifies the final output format of the\nGES.Track. For example, for a video track, it would specify the\nheight, width, framerate and other properties of the stream.\nYou may change this property after the track has been added to a\nGES.Timeline, but it must remain compatible with the track's\ncaps.\nDefault value: GST_CAPS_ANY (not introspectable).\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\n"});