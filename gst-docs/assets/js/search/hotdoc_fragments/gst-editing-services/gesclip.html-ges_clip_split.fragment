fragment_downloaded_cb({"url":"gst-editing-services/gesclip.html#ges_clip_split","fragment":"\n\tges_clip_split\n\n\n\tGES.Clip.prototype.split\n\n\n\tGES.Clip.split\n\nSee ges_clip_split_full, which also gives an error.\nParameters:\nThe GESClip to split\nThe timeline position at which to perform the split\nThe newly created clip resulting\nfrom the splitting clip, or NULL if clip can't be split.\nSee GES.Clip.prototype.split_full, which also gives an error.\nParameters:\nThe GES.Clip to split\nThe timeline position at which to perform the split\nThe newly created clip resulting\nfrom the splitting clip, or null if clip can't be split.\nSee GES.Clip.split_full, which also gives an error.\nParameters:\nThe GES.Clip to split\nThe timeline position at which to perform the split\nThe newly created clip resulting\nfrom the splitting clip, or None if clip can't be split.\n"});