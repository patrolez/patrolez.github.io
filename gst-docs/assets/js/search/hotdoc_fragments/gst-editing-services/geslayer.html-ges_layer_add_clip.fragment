fragment_downloaded_cb({"url":"gst-editing-services/geslayer.html#ges_layer_add_clip","fragment":"\n\tges_layer_add_clip\n\n\n\tGES.Layer.prototype.add_clip\n\n\n\tGES.Layer.add_clip\n\nSee ges_layer_add_clip_full, which also gives an error.\nParameters:\nThe GESLayer\nThe clip to add\nTRUE if clip was properly added to layer, or FALSE\nif layer refused to add clip.\nSee GES.Layer.prototype.add_clip_full, which also gives an error.\nParameters:\nThe GES.Layer\nThe clip to add\ntrue if clip was properly added to layer, or false\nif layer refused to add clip.\nSee GES.Layer.add_clip_full, which also gives an error.\nParameters:\nThe GES.Layer\nThe clip to add\nTrue if clip was properly added to layer, or False\nif layer refused to add clip.\n"});