fragment_downloaded_cb({"url":"gst-editing-services/geslayer.html#GESLayer:auto-transition","fragment":"\n\tauto-transition\n\n\n\tauto-transition\n\n\n\tauto_transition\n\nWhether to automatically create a GESTransitionClip whenever two\nGESSource-s that both belong to a GESClip in the layer overlap.\nSee GESTimeline for what counts as an overlap.\nWhen a layer is added to a GESTimeline, if this property is left as\nFALSE, but the timeline's auto-transition is TRUE, it\nwill be set to TRUE as well.\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\nWhether to automatically create a GES.TransitionClip whenever two\nGES.Source-s that both belong to a GES.Clip in the layer overlap.\nSee GES.Timeline for what counts as an overlap.\nWhen a layer is added to a GES.Timeline, if this property is left as\nfalse, but the timeline's auto-transition is true, it\nwill be set to true as well.\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\nWhether to automatically create a GES.TransitionClip whenever two\nGES.Source-s that both belong to a GES.Clip in the layer overlap.\nSee GES.Timeline for what counts as an overlap.\nWhen a layer is added to a GES.Timeline, if this property is left as\nFalse, but the timeline's auto_transition is True, it\nwill be set to True as well.\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\n"});