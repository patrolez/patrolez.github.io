fragment_downloaded_cb({"url":"gst-editing-services/gesasset.html#GESAsset:proxy","fragment":"\n\tproxy\n\n\n\tproxy\n\n\n\tproxy\n\nThe default proxy for this asset, or NULL if it has no proxy. A\nproxy will act as a substitute for the original asset when the\noriginal is requested (see ges_asset_request).\nSetting this property will not usually remove the existing proxy, but\nwill replace it as the default (see ges_asset_set_proxy).\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\nThe default proxy for this asset, or null if it has no proxy. A\nproxy will act as a substitute for the original asset when the\noriginal is requested (see GES.Asset.prototype.request).\nSetting this property will not usually remove the existing proxy, but\nwill replace it as the default (see GES.Asset.prototype.set_proxy).\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\nThe default proxy for this asset, or None if it has no proxy. A\nproxy will act as a substitute for the original asset when the\noriginal is requested (see GES.Asset.request).\nSetting this property will not usually remove the existing proxy, but\nwill replace it as the default (see GES.Asset.set_proxy).\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\n"});