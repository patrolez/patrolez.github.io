fragment_downloaded_cb({"url":"gst-editing-services/gesasset.html#ges_asset_list_proxies","fragment":"\n\tges_asset_list_proxies\n\n\n\tGES.Asset.prototype.list_proxies\n\n\n\tGES.Asset.list_proxies\n\nGet all the proxies that the asset has. The first item of the list will\nbe the default proxy. The second will be the proxy that is\n'next in line' to be default, and so on.\nParameters:\nA GESAsset\nThe list of proxies\nthat asset has.\nGet all the proxies that the asset has. The first item of the list will\nbe the default proxy. The second will be the proxy that is\n'next in line' to be default, and so on.\nParameters:\nA GES.Asset\nThe list of proxies\nthat asset has.\nGet all the proxies that the asset has. The first item of the list will\nbe the default proxy. The second will be the proxy that is\n'next in line' to be default, and so on.\nParameters:\nA GES.Asset\nThe list of proxies\nthat asset has.\n"});