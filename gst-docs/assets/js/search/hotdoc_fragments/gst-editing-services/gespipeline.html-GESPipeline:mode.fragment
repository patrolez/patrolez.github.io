fragment_downloaded_cb({"url":"gst-editing-services/gespipeline.html#GESPipeline:mode","fragment":"\n\tmode\n\n\n\tmode\n\n\n\tmode\n\nThe pipeline's mode. In preview mode (for audio or video, or both)\nthe pipeline can display the timeline's content to an end user. In\nrendering mode the pipeline can encode the timeline's content and\nsave it to a file.\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\nThe pipeline's mode. In preview mode (for audio or video, or both)\nthe pipeline can display the timeline's content to an end user. In\nrendering mode the pipeline can encode the timeline's content and\nsave it to a file.\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\nThe pipeline's mode. In preview mode (for audio or video, or both)\nthe pipeline can display the timeline's content to an end user. In\nrendering mode the pipeline can encode the timeline's content and\nsave it to a file.\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\n"});