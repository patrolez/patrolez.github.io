fragment_downloaded_cb({"url":"gst-editing-services/geslayer.html#ges_layer_add_clip_full","fragment":"\n\tges_layer_add_clip_full\n\n\n\tGES.Layer.prototype.add_clip_full\n\n\n\tGES.Layer.add_clip_full\n\nAdds the given clip to the layer. If the method succeeds, the layer\nwill take ownership of the clip.\nThis method will fail and return FALSE if clip already resides in\nsome layer. It can also fail if the additional clip breaks some\ncompositional rules (see GESTimelineElement).\nParameters:\nThe GESLayer\nThe clip to add\nReturn location for an error\nTRUE if clip was properly added to layer, or FALSE\nif layer refused to add clip.\n\n\tSince\t\t\t: 1.18\n\t\t\nAdds the given clip to the layer. If the method succeeds, the layer\nwill take ownership of the clip.\nThis method will fail and return false if clip already resides in\nsome layer. It can also fail if the additional clip breaks some\ncompositional rules (see GES.TimelineElement).\nParameters:\nThe GES.Layer\nThe clip to add\ntrue if clip was properly added to layer, or false\nif layer refused to add clip.\n\n\tSince\t\t\t: 1.18\n\t\t\nAdds the given clip to the layer. If the method succeeds, the layer\nwill take ownership of the clip.\nThis method will fail and return False if clip already resides in\nsome layer. It can also fail if the additional clip breaks some\ncompositional rules (see GES.TimelineElement).\nParameters:\nThe GES.Layer\nThe clip to add\nTrue if clip was properly added to layer, or False\nif layer refused to add clip.\n\n\tSince\t\t\t: 1.18\n\t\t\n"});