fragment_downloaded_cb({"url":"gst-editing-services/gesclip.html#ges_clip_set_supported_formats","fragment":"\n\tges_clip_set_supported_formats\n\n\n\tGES.Clip.prototype.set_supported_formats\n\n\n\tGES.Clip.set_supported_formats\n\nSets the supported-formats of the clip. This should normally\nonly be called by subclasses, which should be responsible for updating\nits value, rather than the user.\nParameters:\nA GESClip\nThe GESTrackType-s supported by clip\nSets the supported-formats of the clip. This should normally\nonly be called by subclasses, which should be responsible for updating\nits value, rather than the user.\nParameters:\nA GES.Clip\nThe GES.TrackType-s supported by clip\nSets the supported_formats of the clip. This should normally\nonly be called by subclasses, which should be responsible for updating\nits value, rather than the user.\nParameters:\nA GES.Clip\nThe GES.TrackType-s supported by clip\n"});