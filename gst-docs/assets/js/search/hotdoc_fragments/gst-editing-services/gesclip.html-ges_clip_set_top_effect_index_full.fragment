fragment_downloaded_cb({"url":"gst-editing-services/gesclip.html#ges_clip_set_top_effect_index_full","fragment":"\n\tges_clip_set_top_effect_index_full\n\n\n\tGES.Clip.prototype.set_top_effect_index_full\n\n\n\tGES.Clip.set_top_effect_index_full\n\nSet the index of an effect within the clip. See\nges_clip_get_top_effect_index. The new index must be an existing\nindex of the clip. The effect is moved to the new index, and the other\neffects may be shifted in index accordingly to otherwise maintain the\nordering.\nParameters:\nA GESClip\nAn effect within clip to move\nThe index for effect in clip\nReturn location for an error\nTRUE if effect was successfully moved to newindex.\n\n\tSince\t\t\t: 1.18\n\t\t\nSet the index of an effect within the clip. See\nGES.Clip.prototype.get_top_effect_index. The new index must be an existing\nindex of the clip. The effect is moved to the new index, and the other\neffects may be shifted in index accordingly to otherwise maintain the\nordering.\nParameters:\nA GES.Clip\nAn effect within clip to move\nThe index for effect in clip\ntrue if effect was successfully moved to newindex.\n\n\tSince\t\t\t: 1.18\n\t\t\nSet the index of an effect within the clip. See\nGES.Clip.get_top_effect_index. The new index must be an existing\nindex of the clip. The effect is moved to the new index, and the other\neffects may be shifted in index accordingly to otherwise maintain the\nordering.\nParameters:\nA GES.Clip\nAn effect within clip to move\nThe index for effect in clip\nTrue if effect was successfully moved to newindex.\n\n\tSince\t\t\t: 1.18\n\t\t\n"});