fragment_downloaded_cb({"url":"gst-editing-services/geslayer.html#ges_layer_is_empty","fragment":"\n\tges_layer_is_empty\n\n\n\tGES.Layer.prototype.is_empty\n\n\n\tGES.Layer.is_empty\n\nConvenience method to check if the layer is empty (doesn't contain\nany GESClip), or not.\nParameters:\nThe GESLayer to check\nTRUE if layer is empty, FALSE if it contains at least\none clip.\nConvenience method to check if the layer is empty (doesn't contain\nany GES.Clip), or not.\nParameters:\nThe GES.Layer to check\ntrue if layer is empty, false if it contains at least\none clip.\nConvenience method to check if the layer is empty (doesn't contain\nany GES.Clip), or not.\nParameters:\nThe GES.Layer to check\nTrue if layer is empty, False if it contains at least\none clip.\n"});