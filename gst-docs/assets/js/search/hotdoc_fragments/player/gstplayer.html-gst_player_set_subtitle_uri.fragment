fragment_downloaded_cb({"url":"player/gstplayer.html#gst_player_set_subtitle_uri","fragment":"\n\tgst_player_set_subtitle_uri\n\n\n\tGstPlayer.Player.prototype.set_subtitle_uri\n\n\n\tGstPlayer.Player.set_subtitle_uri\n\nSets the external subtitle URI. This should be combined with a call to\ngst_player_set_subtitle_track_enabled(@player, TRUE) so the subtitles are actually\nrendered.\nParameters:\nGstPlayer instance\nsubtitle URI\nSets the external subtitle URI. This should be combined with a call to\ngst_player_set_subtitle_track_enabled(@player, TRUE) so the subtitles are actually\nrendered.\nParameters:\nGstPlayer.Player instance\nsubtitle URI\nSets the external subtitle URI. This should be combined with a call to\ngst_player_set_subtitle_track_enabled(@player, TRUE) so the subtitles are actually\nrendered.\nParameters:\nGstPlayer.Player instance\nsubtitle URI\n"});