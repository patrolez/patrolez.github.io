fragment_downloaded_cb({"url":"tcp/multisocketsink.html#multisocketsink::client-removed","fragment":"\n\tclient-removed\n\nThe given socket is about to be removed from multisocketsink. This\nsignal will be emitted from the streaming thread so applications should\nbe prepared for that.\ngstmultisocketsink still holds a handle to socket so it is possible to call\nthe get-stats signal from this callback. For the same reason it is\nnot safe to close() and reuse socket in this callback.\nParameters:\nthe multisocketsink element that emitted this signal\nthe socket that is to be removed from multisocketsink\nthe reason why the client was removed\n\n\tFlags:\t\n\t\n\t\t\n\t\t\t\tRun Last\n\t\t\t\n\n"});