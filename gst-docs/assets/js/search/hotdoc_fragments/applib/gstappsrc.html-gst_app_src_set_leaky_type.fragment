fragment_downloaded_cb({"url":"applib/gstappsrc.html#gst_app_src_set_leaky_type","fragment":"\n\tgst_app_src_set_leaky_type\n\n\n\tGstApp.AppSrc.prototype.set_leaky_type\n\n\n\tGstApp.AppSrc.set_leaky_type\n\nWhen set to any other value than GST_APP_LEAKY_TYPE_NONE then the appsrc\nwill drop any buffers that are pushed into it once its internal queue is\nfull. The selected type defines whether to drop the oldest or new\nbuffers.\nParameters:\na GstAppSrc\nthe GstAppLeakyType\n\n\tSince\t\t\t: 1.20\n\t\t\nWhen set to any other value than GST_APP_LEAKY_TYPE_NONE then the appsrc\nwill drop any buffers that are pushed into it once its internal queue is\nfull. The selected type defines whether to drop the oldest or new\nbuffers.\nParameters:\na GstApp.AppSrc\nthe GstApp.AppLeakyType\n\n\tSince\t\t\t: 1.20\n\t\t\nWhen set to any other value than GST_APP_LEAKY_TYPE_NONE then the appsrc\nwill drop any buffers that are pushed into it once its internal queue is\nfull. The selected type defines whether to drop the oldest or new\nbuffers.\nParameters:\na GstApp.AppSrc\nthe GstApp.AppLeakyType\n\n\tSince\t\t\t: 1.20\n\t\t\n"});