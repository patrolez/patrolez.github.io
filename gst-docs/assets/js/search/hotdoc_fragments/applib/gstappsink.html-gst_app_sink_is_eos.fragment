fragment_downloaded_cb({"url":"applib/gstappsink.html#gst_app_sink_is_eos","fragment":"\n\tgst_app_sink_is_eos\n\n\n\tGstApp.AppSink.prototype.is_eos\n\n\n\tGstApp.AppSink.is_eos\n\nCheck if appsink is EOS, which is when no more samples can be pulled because\nan EOS event was received.\nThis function also returns TRUE when the appsink is not in the PAUSED or\nPLAYING state.\nParameters:\na GstAppSink\nTRUE if no more samples can be pulled and the appsink is EOS.\nCheck if appsink is EOS, which is when no more samples can be pulled because\nan EOS event was received.\nThis function also returns true when the appsink is not in the PAUSED or\nPLAYING state.\nParameters:\na GstApp.AppSink\ntrue if no more samples can be pulled and the appsink is EOS.\nCheck if appsink is EOS, which is when no more samples can be pulled because\nan EOS event was received.\nThis function also returns True when the appsink is not in the PAUSED or\nPLAYING state.\nParameters:\na GstApp.AppSink\nTrue if no more samples can be pulled and the appsink is EOS.\n"});