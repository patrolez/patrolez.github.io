fragment_downloaded_cb({"url":"applib/gstappsink.html#gst_app_sink_get_wait_on_eos","fragment":"\n\tgst_app_sink_get_wait_on_eos\n\n\n\tGstApp.AppSink.prototype.get_wait_on_eos\n\n\n\tGstApp.AppSink.get_wait_on_eos\n\nCheck if appsink will wait for all buffers to be consumed when an EOS is\nreceived.\nParameters:\na GstAppSink\nTRUE if appsink will wait for all buffers to be consumed when an\nEOS is received.\nCheck if appsink will wait for all buffers to be consumed when an EOS is\nreceived.\nParameters:\na GstApp.AppSink\ntrue if appsink will wait for all buffers to be consumed when an\nEOS is received.\nCheck if appsink will wait for all buffers to be consumed when an EOS is\nreceived.\nParameters:\na GstApp.AppSink\nTrue if appsink will wait for all buffers to be consumed when an\nEOS is received.\n"});