fragment_downloaded_cb({"url":"audio/gstaudioencoder.html#gst_audio_encoder_negotiate","fragment":"\n\tgst_audio_encoder_negotiate\n\n\n\tGstAudio.AudioEncoder.prototype.negotiate\n\n\n\tGstAudio.AudioEncoder.negotiate\n\nNegotiate with downstream elements to currently configured GstCaps.\nUnmark GST_PAD_FLAG_NEED_RECONFIGURE in any case. But mark it again if\nnegotiate fails.\nParameters:\na GstAudioEncoder\nTRUE if the negotiation succeeded, else FALSE.\nNegotiate with downstream elements to currently configured Gst.Caps.\nUnmark GST_PAD_FLAG_NEED_RECONFIGURE in any case. But mark it again if\nnegotiate fails.\nParameters:\na GstAudio.AudioEncoder\ntrue if the negotiation succeeded, else false.\nNegotiate with downstream elements to currently configured Gst.Caps.\nUnmark GST_PAD_FLAG_NEED_RECONFIGURE in any case. But mark it again if\nnegotiate fails.\nParameters:\na GstAudio.AudioEncoder\nTrue if the negotiation succeeded, else False.\n"});