fragment_downloaded_cb({"url":"audio/audio-buffer.html#gst_audio_buffer_clip","fragment":"\n\tgst_audio_buffer_clip\n\n\n\tGstAudio.prototype.audio_buffer_clip\n\n\n\tGstAudio.audio_buffer_clip\n\nClip the buffer to the given GstSegment.\nAfter calling this function the caller does not own a reference to\nbuffer anymore.\nParameters:\nThe buffer to clip.\nSegment in GST_FORMAT_TIME or GST_FORMAT_DEFAULT to which\nthe buffer should be clipped.\nsample rate.\nsize of one audio frame in bytes. This is the size of one sample *\nnumber of channels.\nNULL if the buffer is completely outside the configured segment,\notherwise the clipped buffer is returned.\nIf the buffer has no timestamp, it is assumed to be inside the segment and\nis not clipped\nClip the buffer to the given Gst.Segment.\nAfter calling this function the caller does not own a reference to\nbuffer anymore.\nParameters:\nThe buffer to clip.\nSegment in Gst.Format.TIME or Gst.Format.DEFAULT to which\nthe buffer should be clipped.\nsample rate.\nsize of one audio frame in bytes. This is the size of one sample *\nnumber of channels.\nnull if the buffer is completely outside the configured segment,\notherwise the clipped buffer is returned.\nIf the buffer has no timestamp, it is assumed to be inside the segment and\nis not clipped\nClip the buffer to the given Gst.Segment.\nAfter calling this function the caller does not own a reference to\nbuffer anymore.\nParameters:\nThe buffer to clip.\nSegment in Gst.Format.TIME or Gst.Format.DEFAULT to which\nthe buffer should be clipped.\nsample rate.\nsize of one audio frame in bytes. This is the size of one sample *\nnumber of channels.\nNone if the buffer is completely outside the configured segment,\notherwise the clipped buffer is returned.\nIf the buffer has no timestamp, it is assumed to be inside the segment and\nis not clipped\n"});