fragment_downloaded_cb({"url":"audio/gstaudioencoder.html#gst_audio_encoder_set_latency","fragment":"\n\tgst_audio_encoder_set_latency\n\n\n\tGstAudio.AudioEncoder.prototype.set_latency\n\n\n\tGstAudio.AudioEncoder.set_latency\n\nSets encoder latency. If the provided values changed from\npreviously provided ones, this will also post a LATENCY message on the bus\nso the pipeline can reconfigure its global latency.\nParameters:\na GstAudioEncoder\nminimum latency\nmaximum latency\nSets encoder latency. If the provided values changed from\npreviously provided ones, this will also post a LATENCY message on the bus\nso the pipeline can reconfigure its global latency.\nParameters:\na GstAudio.AudioEncoder\nminimum latency\nmaximum latency\nSets encoder latency. If the provided values changed from\npreviously provided ones, this will also post a LATENCY message on the bus\nso the pipeline can reconfigure its global latency.\nParameters:\na GstAudio.AudioEncoder\nminimum latency\nmaximum latency\n"});