fragment_downloaded_cb({"url":"audio/gstaudioclock.html#gst_audio_clock_new","fragment":"\n\tgst_audio_clock_new\n\n\n\tGstAudio.AudioClock.prototype.new\n\n\n\tGstAudio.AudioClock.new\n\nCreate a new GstAudioClock instance. Whenever the clock time should be\ncalculated it will call func with user_data. When func returns\nGST_CLOCK_TIME_NONE, the clock will return the last reported time.\nParameters:\nthe name of the clock\na function\nuser data\nGDestroyNotify for user_data\na new GstAudioClock casted to a GstClock.\nCreate a new GstAudio.AudioClock instance. Whenever the clock time should be\ncalculated it will call func with user_data. When func returns\nGst.CLOCK_TIME_NONE, the clock will return the last reported time.\nParameters:\nthe name of the clock\na function\nuser data\na new GstAudio.AudioClock casted to a Gst.Clock.\nCreate a new GstAudio.AudioClock instance. Whenever the clock time should be\ncalculated it will call func with user_data. When func returns\nGst.CLOCK_TIME_NONE, the clock will return the last reported time.\nParameters:\nthe name of the clock\na function\nuser data\na new GstAudio.AudioClock casted to a Gst.Clock.\n"});