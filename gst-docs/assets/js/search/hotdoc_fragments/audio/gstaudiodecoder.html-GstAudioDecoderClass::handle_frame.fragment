fragment_downloaded_cb({"url":"audio/gstaudiodecoder.html#GstAudioDecoderClass::handle_frame","fragment":"\n\thandle_frame\n\n\n\tvfunc_handle_frame\n\n\n\tdo_handle_frame\n\nProvides input data (or NULL to clear any remaining data)\nto subclass.  Input data ref management is performed by\nbase class, subclass should not care or intervene,\nand input data is only valid until next call to base class,\nmost notably a call to gst_audio_decoder_finish_frame.\nParameters:\nProvides input data (or NULL to clear any remaining data)\nto subclass.  Input data ref management is performed by\nbase class, subclass should not care or intervene,\nand input data is only valid until next call to base class,\nmost notably a call to GstAudio.AudioDecoder.prototype.finish_frame.\nParameters:\nProvides input data (or NULL to clear any remaining data)\nto subclass.  Input data ref management is performed by\nbase class, subclass should not care or intervene,\nand input data is only valid until next call to base class,\nmost notably a call to GstAudio.AudioDecoder.finish_frame.\nParameters:\n"});