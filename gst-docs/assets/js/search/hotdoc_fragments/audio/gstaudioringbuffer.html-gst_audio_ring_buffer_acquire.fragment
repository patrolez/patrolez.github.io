fragment_downloaded_cb({"url":"audio/gstaudioringbuffer.html#gst_audio_ring_buffer_acquire","fragment":"\n\tgst_audio_ring_buffer_acquire\n\n\n\tGstAudio.AudioRingBuffer.prototype.acquire\n\n\n\tGstAudio.AudioRingBuffer.acquire\n\nAllocate the resources for the ringbuffer. This function fills\nin the data pointer of the ring buffer with a valid GstBuffer\nto which samples can be written.\nParameters:\nthe GstAudioRingBuffer to acquire\nthe specs of the buffer\nTRUE if the device could be acquired, FALSE on error.\nMT safe.\nAllocate the resources for the ringbuffer. This function fills\nin the data pointer of the ring buffer with a valid Gst.Buffer\nto which samples can be written.\nParameters:\nthe GstAudio.AudioRingBuffer to acquire\nthe specs of the buffer\nTRUE if the device could be acquired, FALSE on error.\nMT safe.\nAllocate the resources for the ringbuffer. This function fills\nin the data pointer of the ring buffer with a valid Gst.Buffer\nto which samples can be written.\nParameters:\nthe GstAudio.AudioRingBuffer to acquire\nthe specs of the buffer\nTRUE if the device could be acquired, FALSE on error.\nMT safe.\n"});