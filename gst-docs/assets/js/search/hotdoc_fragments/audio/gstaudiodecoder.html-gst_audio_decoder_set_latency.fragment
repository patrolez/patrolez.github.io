fragment_downloaded_cb({"url":"audio/gstaudiodecoder.html#gst_audio_decoder_set_latency","fragment":"\n\tgst_audio_decoder_set_latency\n\n\n\tGstAudio.AudioDecoder.prototype.set_latency\n\n\n\tGstAudio.AudioDecoder.set_latency\n\nSets decoder latency. If the provided values changed from\npreviously provided ones, this will also post a LATENCY message on the bus\nso the pipeline can reconfigure its global latency.\nParameters:\na GstAudioDecoder\nminimum latency\nmaximum latency\nSets decoder latency. If the provided values changed from\npreviously provided ones, this will also post a LATENCY message on the bus\nso the pipeline can reconfigure its global latency.\nParameters:\na GstAudio.AudioDecoder\nminimum latency\nmaximum latency\nSets decoder latency. If the provided values changed from\npreviously provided ones, this will also post a LATENCY message on the bus\nso the pipeline can reconfigure its global latency.\nParameters:\na GstAudio.AudioDecoder\nminimum latency\nmaximum latency\n"});