fragment_downloaded_cb({"url":"audio/gstaudioresampler.html#GstAudioResamplerFilterMode","fragment":"\n\tGstAudioResamplerFilterMode\n\nMembers\n\n\tGstAudio.AudioResamplerFilterMode\n\nMembers\n\n\tGstAudio.AudioResamplerFilterMode\n\nMembers\nSelect for the filter tables should be set up.\nUse interpolated filter tables. This\nuses less memory but more CPU and is slightly less accurate but it allows for more\nefficient variable rate resampling with gst_audio_resampler_update.\nUse full filter table. This uses more memory\nbut less CPU.\nAutomatically choose between interpolated\nand full filter tables.\n\n\tSince\t\t\t: 1.10\n\t\t\nSelect for the filter tables should be set up.\nUse interpolated filter tables. This\nuses less memory but more CPU and is slightly less accurate but it allows for more\nefficient variable rate resampling with GstAudio.AudioResampler.prototype.update.\nUse full filter table. This uses more memory\nbut less CPU.\nAutomatically choose between interpolated\nand full filter tables.\n\n\tSince\t\t\t: 1.10\n\t\t\nSelect for the filter tables should be set up.\nUse interpolated filter tables. This\nuses less memory but more CPU and is slightly less accurate but it allows for more\nefficient variable rate resampling with GstAudio.AudioResampler.update.\nUse full filter table. This uses more memory\nbut less CPU.\nAutomatically choose between interpolated\nand full filter tables.\n\n\tSince\t\t\t: 1.10\n\t\t\n"});