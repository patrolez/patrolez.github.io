fragment_downloaded_cb({"url":"audio/gstaudioconverter.html#gst_audio_converter_get_config","fragment":"\n\tgst_audio_converter_get_config\n\n\n\tGstAudio.AudioConverter.prototype.get_config\n\n\n\tGstAudio.AudioConverter.get_config\n\nGet the current configuration of convert.\nParameters:\na GstAudioConverter\nresult input rate\nresult output rate\n(transfer none):\na GstStructure that remains valid for as long as convert is valid\nor until gst_audio_converter_update_config is called.\nGet the current configuration of convert.\nParameters:\na GstAudio.AudioConverter\nReturns a tuple made of:\n(transfer none):\na Gst.Structure that remains valid for as long as convert is valid\nor until GstAudio.AudioConverter.prototype.update_config is called.\n(transfer none):\na Gst.Structure that remains valid for as long as convert is valid\nor until GstAudio.AudioConverter.prototype.update_config is called.\n(transfer none):\na Gst.Structure that remains valid for as long as convert is valid\nor until GstAudio.AudioConverter.prototype.update_config is called.\nGet the current configuration of convert.\nParameters:\na GstAudio.AudioConverter\nReturns a tuple made of:\n(transfer none):\na Gst.Structure that remains valid for as long as convert is valid\nor until GstAudio.AudioConverter.update_config is called.\n(transfer none):\na Gst.Structure that remains valid for as long as convert is valid\nor until GstAudio.AudioConverter.update_config is called.\n(transfer none):\na Gst.Structure that remains valid for as long as convert is valid\nor until GstAudio.AudioConverter.update_config is called.\n"});