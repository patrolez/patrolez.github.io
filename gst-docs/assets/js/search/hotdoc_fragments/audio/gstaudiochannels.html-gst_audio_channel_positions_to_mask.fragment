fragment_downloaded_cb({"url":"audio/gstaudiochannels.html#gst_audio_channel_positions_to_mask","fragment":"\n\tgst_audio_channel_positions_to_mask\n\n\n\tGstAudio.prototype.audio_channel_positions_to_mask\n\n\n\tGstAudio.audio_channel_positions_to_mask\n\nConvert the position array of channels channels to a bitmask.\nIf force_order is TRUE it additionally checks if the channels are\nin the order required by GStreamer.\nParameters:\nThe GstAudioChannelPosition\nThe number of channels.\nOnly consider the GStreamer channel order.\nthe output channel mask\nTRUE if the channel positions are valid and could be converted.\nConvert the position array of channels channels to a bitmask.\nIf force_order is true it additionally checks if the channels are\nin the order required by GStreamer.\nParameters:\nThe GstAudio.AudioChannelPosition\nThe number of channels.\nOnly consider the GStreamer channel order.\nReturns a tuple made of:\ntrue if the channel positions are valid and could be converted.\ntrue if the channel positions are valid and could be converted.\nConvert the position array of channels channels to a bitmask.\nIf force_order is True it additionally checks if the channels are\nin the order required by GStreamer.\nParameters:\nThe GstAudio.AudioChannelPosition\nThe number of channels.\nOnly consider the GStreamer channel order.\nReturns a tuple made of:\nTrue if the channel positions are valid and could be converted.\nTrue if the channel positions are valid and could be converted.\n"});