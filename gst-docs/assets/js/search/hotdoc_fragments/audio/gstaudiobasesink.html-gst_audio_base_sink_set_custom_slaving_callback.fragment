fragment_downloaded_cb({"url":"audio/gstaudiobasesink.html#gst_audio_base_sink_set_custom_slaving_callback","fragment":"\n\tgst_audio_base_sink_set_custom_slaving_callback\n\n\n\tGstAudio.AudioBaseSink.prototype.set_custom_slaving_callback\n\n\n\tGstAudio.AudioBaseSink.set_custom_slaving_callback\n\nSets the custom slaving callback. This callback will\nbe invoked if the slave-method property is set to\nGST_AUDIO_BASE_SINK_SLAVE_CUSTOM and the audio sink\nreceives and plays samples.\nSetting the callback to NULL causes the sink to\nbehave as if the GST_AUDIO_BASE_SINK_SLAVE_NONE\nmethod were used.\nParameters:\na GstAudioBaseSink\na GstAudioBaseSinkCustomSlavingCallback\nuser data passed to the callback\ncalled when user_data becomes unused\n\n\tSince\t\t\t: 1.6\n\t\t\nSets the custom slaving callback. This callback will\nbe invoked if the slave-method property is set to\nGST_AUDIO_BASE_SINK_SLAVE_CUSTOM and the audio sink\nreceives and plays samples.\nSetting the callback to NULL causes the sink to\nbehave as if the GST_AUDIO_BASE_SINK_SLAVE_NONE\nmethod were used.\nParameters:\na GstAudio.AudioBaseSink\na GstAudio.AudioBaseSinkCustomSlavingCallback\nuser data passed to the callback\n\n\tSince\t\t\t: 1.6\n\t\t\nSets the custom slaving callback. This callback will\nbe invoked if the slave-method property is set to\nGST_AUDIO_BASE_SINK_SLAVE_CUSTOM and the audio sink\nreceives and plays samples.\nSetting the callback to NULL causes the sink to\nbehave as if the GST_AUDIO_BASE_SINK_SLAVE_NONE\nmethod were used.\nParameters:\na GstAudio.AudioBaseSink\na GstAudio.AudioBaseSinkCustomSlavingCallback\nuser data passed to the callback\n\n\tSince\t\t\t: 1.6\n\t\t\n"});