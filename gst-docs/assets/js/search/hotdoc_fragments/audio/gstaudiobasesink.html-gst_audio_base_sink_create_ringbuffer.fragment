fragment_downloaded_cb({"url":"audio/gstaudiobasesink.html#gst_audio_base_sink_create_ringbuffer","fragment":"\n\tgst_audio_base_sink_create_ringbuffer\n\n\n\tGstAudio.AudioBaseSink.prototype.create_ringbuffer\n\n\n\tGstAudio.AudioBaseSink.create_ringbuffer\n\nCreate and return the GstAudioRingBuffer for sink. This function will\ncall the ::create_ringbuffer vmethod and will set sink as the parent of\nthe returned buffer (see gst_object_set_parent).\nParameters:\na GstAudioBaseSink.\nThe new ringbuffer of sink.\nCreate and return the GstAudio.AudioRingBuffer for sink. This function will\ncall the ::create_ringbuffer vmethod and will set sink as the parent of\nthe returned buffer (see Gst.Object.prototype.set_parent).\nParameters:\na GstAudio.AudioBaseSink.\nThe new ringbuffer of sink.\nCreate and return the GstAudio.AudioRingBuffer for sink. This function will\ncall the ::create_ringbuffer vmethod and will set sink as the parent of\nthe returned buffer (see Gst.Object.set_parent).\nParameters:\na GstAudio.AudioBaseSink.\nThe new ringbuffer of sink.\n"});