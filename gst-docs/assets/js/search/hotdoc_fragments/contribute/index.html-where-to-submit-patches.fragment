fragment_downloaded_cb({"url":"contribute/index.html#where-to-submit-patches","fragment":"Where to Submit Patches\nPatches need to be submitted through GitLab in form of a\n\"Merge Request\" (MR), which is the same as a \"Pull Request\" (PR)\nin GitHub, and uses the same workflow.\nIn a nutshell, you will:\n"});