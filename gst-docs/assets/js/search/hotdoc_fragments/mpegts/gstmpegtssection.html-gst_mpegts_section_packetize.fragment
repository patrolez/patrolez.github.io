fragment_downloaded_cb({"url":"mpegts/gstmpegtssection.html#gst_mpegts_section_packetize","fragment":"\n\tgst_mpegts_section_packetize\n\n\n\tGstMpegts.Section.prototype.packetize\n\n\n\tGstMpegts.Section.packetize\n\nPacketize (i.e. serialize) the section. If the data in section has already\nbeen packetized, the data pointer is returned immediately. Otherwise, the\ndata field is allocated and populated.\nParameters:\nthe GstMpegtsSection that holds the data\ngsize to hold the size of the data\npointer to section data, or NULL on failure.\nPacketize (i.e. serialize) the section. If the data in section has already\nbeen packetized, the data pointer is returned immediately. Otherwise, the\ndata field is allocated and populated.\nParameters:\nthe GstMpegts.Section that holds the data\nReturns a tuple made of:\npointer to section data, or null on failure.\npointer to section data, or null on failure.\nPacketize (i.e. serialize) the section. If the data in section has already\nbeen packetized, the data pointer is returned immediately. Otherwise, the\ndata field is allocated and populated.\nParameters:\nthe GstMpegts.Section that holds the data\nReturns a tuple made of:\npointer to section data, or None on failure.\npointer to section data, or None on failure.\n"});