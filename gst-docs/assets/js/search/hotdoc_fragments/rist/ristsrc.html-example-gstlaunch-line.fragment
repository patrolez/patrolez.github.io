fragment_downloaded_cb({"url":"rist/ristsrc.html#example-gstlaunch-line","fragment":"Example gst-launch line\nIn order to use a dynamic payload type the element needs to be able\nto set the correct caps. It can be done through setting the caps\nproperty or one can use the encoding-name property and the\nelement can work out the caps from it.\n"});