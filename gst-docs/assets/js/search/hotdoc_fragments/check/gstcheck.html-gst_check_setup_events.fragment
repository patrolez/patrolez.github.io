fragment_downloaded_cb({"url":"check/gstcheck.html#gst_check_setup_events","fragment":"\n\tgst_check_setup_events\n\n\n\tGstCheck.prototype.check_setup_events\n\n\n\tGstCheck.check_setup_events\n\nPush stream-start, caps and segment event, which consist of the minimum\nrequired events to allow streaming. Caps is optional to allow raw src\ntesting. If element has more than one src or sink pad, use\ngst_check_setup_events_with_stream_id instead.\nParameters:\nThe src GstPad to push on\nThe GstElement use to create the stream id\nGstCaps in case caps event must be sent\nThe GstFormat of the default segment to send\nPush stream-start, caps and segment event, which consist of the minimum\nrequired events to allow streaming. Caps is optional to allow raw src\ntesting. If element has more than one src or sink pad, use\nGstCheck.prototype.check_setup_events_with_stream_id instead.\nParameters:\nThe src Gst.Pad to push on\nThe Gst.Element use to create the stream id\nGst.Caps in case caps event must be sent\nThe Gst.Format of the default segment to send\nPush stream-start, caps and segment event, which consist of the minimum\nrequired events to allow streaming. Caps is optional to allow raw src\ntesting. If element has more than one src or sink pad, use\nGstCheck.check_setup_events_with_stream_id instead.\nParameters:\nThe src Gst.Pad to push on\nThe Gst.Element use to create the stream id\nGst.Caps in case caps event must be sent\nThe Gst.Format of the default segment to send\n"});