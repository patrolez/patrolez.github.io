fragment_downloaded_cb({"url":"check/gstharness.html#gst_harness_wait_for_clock_id_waits","fragment":"\n\tgst_harness_wait_for_clock_id_waits\n\n\n\tGstCheck.Harness.prototype.wait_for_clock_id_waits\n\n\n\tGstCheck.Harness.wait_for_clock_id_waits\n\nWaits for timeout seconds until waits number of GstClockID waits is\nregistered with the GstTestClock. Useful for writing deterministic tests,\nwhere you want to make sure that an expected number of waits have been\nreached.\nMT safe.\nParameters:\na GstHarness\na guint describing the numbers of GstClockID registered with\nthe GstTestClock\na guint describing how many seconds to wait for waits to be true\na gboolean TRUE if the waits have been registered, FALSE if not.\n(Could be that it timed out waiting or that more waits than waits was found)\n\n\tSince\t\t\t: 1.6\n\t\t\nWaits for timeout seconds until waits number of Object waits is\nregistered with the GstCheck.TestClock. Useful for writing deterministic tests,\nwhere you want to make sure that an expected number of waits have been\nreached.\nMT safe.\nParameters:\na GstCheck.Harness\na Number describing the numbers of Object registered with\nthe GstCheck.TestClock\na Number describing how many seconds to wait for waits to be true\na gboolean true if the waits have been registered, false if not.\n(Could be that it timed out waiting or that more waits than waits was found)\n\n\tSince\t\t\t: 1.6\n\t\t\nWaits for timeout seconds until waits number of object waits is\nregistered with the GstCheck.TestClock. Useful for writing deterministic tests,\nwhere you want to make sure that an expected number of waits have been\nreached.\nMT safe.\nParameters:\na GstCheck.Harness\na int describing the numbers of object registered with\nthe GstCheck.TestClock\na int describing how many seconds to wait for waits to be true\na gboolean True if the waits have been registered, False if not.\n(Could be that it timed out waiting or that more waits than waits was found)\n\n\tSince\t\t\t: 1.6\n\t\t\n"});