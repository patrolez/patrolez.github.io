fragment_downloaded_cb({"url":"check/gstharness.html#gst_harness_crank_multiple_clock_waits","fragment":"\n\tgst_harness_crank_multiple_clock_waits\n\n\n\tGstCheck.Harness.prototype.crank_multiple_clock_waits\n\n\n\tGstCheck.Harness.crank_multiple_clock_waits\n\nSimilar to gst_harness_crank_single_clock_wait, this is the function to use\nif your harnessed element(s) are using more then one gst_clock_id_wait.\nFailing to do so can (and will) make it racy which GstClockID you actually\nare releasing, where as this function will process all the waits at the\nsame time, ensuring that one thread can't register another wait before\nboth are released.\nMT safe.\nParameters:\na GstHarness\na guint describing the number of GstClockID to crank\na gboolean TRUE if the \"crank\" was successful, FALSE if not.\n\n\tSince\t\t\t: 1.6\n\t\t\nSimilar to GstCheck.Harness.prototype.crank_single_clock_wait, this is the function to use\nif your harnessed element(s) are using more then one gst_clock_id_wait.\nFailing to do so can (and will) make it racy which Object you actually\nare releasing, where as this function will process all the waits at the\nsame time, ensuring that one thread can't register another wait before\nboth are released.\nMT safe.\nParameters:\na GstCheck.Harness\na Number describing the number of Object to crank\na gboolean true if the \"crank\" was successful, false if not.\n\n\tSince\t\t\t: 1.6\n\t\t\nSimilar to GstCheck.Harness.crank_single_clock_wait, this is the function to use\nif your harnessed element(s) are using more then one gst_clock_id_wait.\nFailing to do so can (and will) make it racy which object you actually\nare releasing, where as this function will process all the waits at the\nsame time, ensuring that one thread can't register another wait before\nboth are released.\nMT safe.\nParameters:\na GstCheck.Harness\na int describing the number of object to crank\na gboolean True if the \"crank\" was successful, False if not.\n\n\tSince\t\t\t: 1.6\n\t\t\n"});