fragment_downloaded_cb({"url":"check/gsttestclock.html#gst_test_clock_timed_wait_for_multiple_pending_ids","fragment":"\n\tgst_test_clock_timed_wait_for_multiple_pending_ids\n\n\n\tGstCheck.TestClock.prototype.timed_wait_for_multiple_pending_ids\n\n\n\tGstCheck.TestClock.timed_wait_for_multiple_pending_ids\n\nBlocks until at least count clock notifications have been requested from\ntest_clock, or the timeout expires.\nMT safe.\nParameters:\nGstTestClock for which to await having enough pending clock\nthe number of pending clock notifications to wait for\nthe timeout in milliseconds\nAddress\nof a GList pointer variable to store the list of pending GstClockID\nthat expired, or NULL\na gboolean TRUE if the waits have been registered, FALSE if not.\n(Could be that it timed out waiting or that more waits than waits was found)\n\n\tSince\t\t\t: 1.16\n\t\t\nBlocks until at least count clock notifications have been requested from\ntest_clock, or the timeout expires.\nMT safe.\nParameters:\nGstCheck.TestClock for which to await having enough pending clock\nthe number of pending clock notifications to wait for\nthe timeout in milliseconds\nReturns a tuple made of:\na gboolean true if the waits have been registered, false if not.\n(Could be that it timed out waiting or that more waits than waits was found)\na gboolean true if the waits have been registered, false if not.\n(Could be that it timed out waiting or that more waits than waits was found)\n\n\tSince\t\t\t: 1.16\n\t\t\nBlocks until at least count clock notifications have been requested from\ntest_clock, or the timeout expires.\nMT safe.\nParameters:\nGstCheck.TestClock for which to await having enough pending clock\nthe number of pending clock notifications to wait for\nthe timeout in milliseconds\nReturns a tuple made of:\na gboolean True if the waits have been registered, False if not.\n(Could be that it timed out waiting or that more waits than waits was found)\na gboolean True if the waits have been registered, False if not.\n(Could be that it timed out waiting or that more waits than waits was found)\n\n\tSince\t\t\t: 1.16\n\t\t\n"});