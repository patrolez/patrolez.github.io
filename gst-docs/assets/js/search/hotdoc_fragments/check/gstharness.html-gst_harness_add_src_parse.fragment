fragment_downloaded_cb({"url":"check/gstharness.html#gst_harness_add_src_parse","fragment":"\n\tgst_harness_add_src_parse\n\n\n\tGstCheck.Harness.prototype.add_src_parse\n\n\n\tGstCheck.Harness.add_src_parse\n\nSimilar to gst_harness_add_src, this allows you to specify a launch-line,\nwhich can be useful for both having more then one GstElement acting as your\nsrc (Like a src producing raw buffers, and then an encoder, providing encoded\ndata), but also by allowing you to set properties like \"is-live\" directly on\nthe elements.\nMT safe.\nParameters:\na GstHarness\na gchar describing a gst-launch type line\na gboolean specifying if the GstElement uses\ngst_clock_wait_id internally.\n\n\tSince\t\t\t: 1.6\n\t\t\nSimilar to gst_harness_add_src, this allows you to specify a launch-line,\nwhich can be useful for both having more then one Gst.Element acting as your\nsrc (Like a src producing raw buffers, and then an encoder, providing encoded\ndata), but also by allowing you to set properties like \"is-live\" directly on\nthe elements.\nMT safe.\nParameters:\na GstCheck.Harness\na String describing a gst-launch type line\na Number specifying if the Gst.Element uses\ngst_clock_wait_id internally.\n\n\tSince\t\t\t: 1.6\n\t\t\nSimilar to gst_harness_add_src, this allows you to specify a launch-line,\nwhich can be useful for both having more then one Gst.Element acting as your\nsrc (Like a src producing raw buffers, and then an encoder, providing encoded\ndata), but also by allowing you to set properties like \"is-live\" directly on\nthe elements.\nMT safe.\nParameters:\na GstCheck.Harness\na int describing a gst-launch type line\na bool specifying if the Gst.Element uses\ngst_clock_wait_id internally.\n\n\tSince\t\t\t: 1.6\n\t\t\n"});