fragment_downloaded_cb({"url":"check/gstcheck.html#gst_check_drop_buffers","fragment":"\n\tgst_check_drop_buffers\n\n\n\tGstCheck.prototype.check_drop_buffers\n\n\n\tGstCheck.check_drop_buffers\n\nUnref and remove all buffers that are in the global buffers GList,\nemptying the list.\nUnref and remove all buffers that are in the global buffers GList,\nemptying the list.\nUnref and remove all buffers that are in the global buffers GList,\nemptying the list.\n"});