fragment_downloaded_cb({"url":"coreelements/valve.html#valve-page","fragment":"valve\nThe valve is a simple element that drops buffers when the drop\nproperty is set to TRUE and lets then through otherwise.\nAny downstream error received while the drop property is TRUE\nis ignored. So downstream element can be set to  GST_STATE_NULL and removed,\nwithout using pad blocking.\n"});