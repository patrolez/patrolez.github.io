fragment_downloaded_cb({"url":"application-development/basics/bins.html#bins","fragment":"Bins\nA bin is a container element. You can add elements to a bin. Since a bin\nis an element itself, a bin can be handled in the same way as any other\nelement. Therefore, the whole previous chapter (Elements) applies\nto bins as well.\n"});