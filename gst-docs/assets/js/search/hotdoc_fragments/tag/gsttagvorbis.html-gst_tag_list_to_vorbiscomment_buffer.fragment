fragment_downloaded_cb({"url":"tag/gsttagvorbis.html#gst_tag_list_to_vorbiscomment_buffer","fragment":"\n\tgst_tag_list_to_vorbiscomment_buffer\n\n\n\tGstTag.prototype.tag_list_to_vorbiscomment_buffer\n\n\n\tGstTag.tag_list_to_vorbiscomment_buffer\n\nCreates a new vorbiscomment buffer from a tag list.\nParameters:\ntag list to convert\nidentification data at start of stream\nlength of identification data, may be 0 if id_data is NULL\nstring that describes the vendor string or NULL\nA new GstBuffer containing a vorbiscomment buffer with all tags\nthat could be converted from the given tag list.\nCreates a new vorbiscomment buffer from a tag list.\nParameters:\ntag list to convert\nidentification data at start of stream\nlength of identification data, may be 0 if id_data is NULL\nstring that describes the vendor string or NULL\nA new Gst.Buffer containing a vorbiscomment buffer with all tags\nthat could be converted from the given tag list.\nCreates a new vorbiscomment buffer from a tag list.\nParameters:\ntag list to convert\nidentification data at start of stream\nlength of identification data, may be 0 if id_data is NULL\nstring that describes the vendor string or NULL\nA new Gst.Buffer containing a vorbiscomment buffer with all tags\nthat could be converted from the given tag list.\n"});