fragment_downloaded_cb({"url":"tag/gsttagexif.html#gst_tag_list_from_exif_buffer","fragment":"\n\tgst_tag_list_from_exif_buffer\n\n\n\tGstTag.prototype.tag_list_from_exif_buffer\n\n\n\tGstTag.tag_list_from_exif_buffer\n\nParses the IFD and IFD tags data contained in the buffer and puts it\non a taglist. The base_offset is used to subtract from the offset in\nthe tag entries and be able to get the offset relative to the buffer\nstart\nParameters:\nThe exif buffer\nbyte order of the data\nOffset from the tiff header to this buffer\nThe parsed taglist\nParses the IFD and IFD tags data contained in the buffer and puts it\non a taglist. The base_offset is used to subtract from the offset in\nthe tag entries and be able to get the offset relative to the buffer\nstart\nParameters:\nThe exif buffer\nbyte order of the data\nOffset from the tiff header to this buffer\nThe parsed taglist\nParses the IFD and IFD tags data contained in the buffer and puts it\non a taglist. The base_offset is used to subtract from the offset in\nthe tag entries and be able to get the offset relative to the buffer\nstart\nParameters:\nThe exif buffer\nbyte order of the data\nOffset from the tiff header to this buffer\nThe parsed taglist\n"});