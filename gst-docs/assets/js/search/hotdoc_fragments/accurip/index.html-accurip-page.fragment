fragment_downloaded_cb({"url":"accurip/index.html#accurip-page","fragment":"accurip\nThe accurip element calculates a CRC for an audio stream which can be used\nto match the audio stream to a database hosted on\nAccurateRip. This database is used to check for a\nCD rip accuracy.\n"});