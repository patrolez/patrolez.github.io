fragment_downloaded_cb({"url":"gstreamer/gstmemory.html#GstMemoryMapFullFunction","fragment":"\n\tGstMemoryMapFullFunction\n\n\n\tGst.MemoryMapFullFunction\n\n\n\tGst.MemoryMapFullFunction\n\nGet the memory of mem that can be accessed according to the mode specified\nin info's flags. The function should return a pointer that contains at least\nmaxsize bytes.\nParameters:\na GstMemory\nthe GstMapInfo to map with\nsize to map\na pointer to memory of which at least maxsize bytes can be\naccessed according to the access pattern in info's flags.\nGet the memory of mem that can be accessed according to the mode specified\nin info's flags. The function should return a pointer that contains at least\nmaxsize bytes.\nParameters:\na Gst.Memory\nthe Gst.MapInfo to map with\nsize to map\na pointer to memory of which at least maxsize bytes can be\naccessed according to the access pattern in info's flags.\nGet the memory of mem that can be accessed according to the mode specified\nin info's flags. The function should return a pointer that contains at least\nmaxsize bytes.\nParameters:\na Gst.Memory\nthe Gst.MapInfo to map with\nsize to map\na pointer to memory of which at least maxsize bytes can be\naccessed according to the access pattern in info's flags.\n"});