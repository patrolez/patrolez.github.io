fragment_downloaded_cb({"url":"gstreamer/gstbuffer.html#gst_buffer_resize_range","fragment":"\n\tgst_buffer_resize_range\n\n\n\tGst.Buffer.prototype.resize_range\n\n\n\tGst.Buffer.resize_range\n\nSets the total size of the length memory blocks starting at idx in\nbuffer\nParameters:\na GstBuffer.\nan index\na length\nthe offset adjustment\nthe new size or -1 to just adjust the offset\nTRUE if resizing succeeded, FALSE otherwise.\nSets the total size of the length memory blocks starting at idx in\nbuffer\nParameters:\na Gst.Buffer.\nan index\na length\nthe offset adjustment\nthe new size or -1 to just adjust the offset\ntrue if resizing succeeded, false otherwise.\nSets the total size of the length memory blocks starting at idx in\nbuffer\nParameters:\na Gst.Buffer.\nan index\na length\nthe offset adjustment\nthe new size or -1 to just adjust the offset\nTrue if resizing succeeded, False otherwise.\n"});