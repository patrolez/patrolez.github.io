fragment_downloaded_cb({"url":"gstreamer/gstbufferlist.html#gst_buffer_list_take","fragment":"\n\tgst_buffer_list_take\n\n\n\tGst.prototype.buffer_list_take\n\n\n\tGst.buffer_list_take\n\nModifies a pointer to a GstBufferList to point to a different\nGstBufferList. This function is similar to gst_buffer_list_replace except\nthat it takes ownership of new_list.\nParameters:\npointer to a pointer to a GstBufferList\nto be replaced.\npointer to a GstBufferList\nthat will replace the bufferlist pointed to by old_list.\nTRUE if new_list was different from old_list\n\n\tSince\t\t\t: 1.16\n\t\t\nModifies a pointer to a Gst.BufferList to point to a different\nGst.BufferList. This function is similar to Gst.prototype.buffer_list_replace except\nthat it takes ownership of new_list.\nParameters:\npointer to a pointer to a Gst.BufferList\nto be replaced.\npointer to a Gst.BufferList\nthat will replace the bufferlist pointed to by old_list.\nReturns a tuple made of:\ntrue if new_list was different from old_list\ntrue if new_list was different from old_list\n\n\tSince\t\t\t: 1.16\n\t\t\nModifies a pointer to a Gst.BufferList to point to a different\nGst.BufferList. This function is similar to Gst.buffer_list_replace except\nthat it takes ownership of new_list.\nParameters:\npointer to a pointer to a Gst.BufferList\nto be replaced.\npointer to a Gst.BufferList\nthat will replace the bufferlist pointed to by old_list.\nReturns a tuple made of:\nTrue if new_list was different from old_list\nTrue if new_list was different from old_list\n\n\tSince\t\t\t: 1.16\n\t\t\n"});