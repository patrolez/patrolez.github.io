fragment_downloaded_cb({"url":"gstreamer/gststructure.html#gst_structure_set_parent_refcount","fragment":"\n\tgst_structure_set_parent_refcount\n\n\n\tGst.Structure.prototype.set_parent_refcount\n\n\n\tGst.Structure.set_parent_refcount\n\nSets the parent_refcount field of GstStructure. This field is used to\ndetermine whether a structure is mutable or not. This function should only be\ncalled by code implementing parent objects of GstStructure, as described in\nthe MT Refcounting section of the design documents.\nParameters:\na GstStructure\na pointer to the parent's refcount\nTRUE if the parent refcount could be set.\nSets the parent_refcount field of Gst.Structure. This field is used to\ndetermine whether a structure is mutable or not. This function should only be\ncalled by code implementing parent objects of Gst.Structure, as described in\nthe MT Refcounting section of the design documents.\nParameters:\na Gst.Structure\na pointer to the parent's refcount\ntrue if the parent refcount could be set.\nSets the parent_refcount field of Gst.Structure. This field is used to\ndetermine whether a structure is mutable or not. This function should only be\ncalled by code implementing parent objects of Gst.Structure, as described in\nthe MT Refcounting section of the design documents.\nParameters:\na Gst.Structure\na pointer to the parent's refcount\nTrue if the parent refcount could be set.\n"});