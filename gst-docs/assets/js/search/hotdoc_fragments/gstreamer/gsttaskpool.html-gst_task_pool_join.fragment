fragment_downloaded_cb({"url":"gstreamer/gsttaskpool.html#gst_task_pool_join","fragment":"\n\tgst_task_pool_join\n\n\n\tGst.TaskPool.prototype.join\n\n\n\tGst.TaskPool.join\n\nJoin a task and/or return it to the pool. id is the id obtained from\ngst_task_pool_push. The default implementation does nothing, as the\ndefault push implementation always returns NULL.\nThis method should only be called with the same pool instance that provided\nid.\nParameters:\na GstTaskPool\nthe id\nJoin a task and/or return it to the pool. id is the id obtained from\nGst.TaskPool.prototype.push. The default implementation does nothing, as the\ndefault vfunc_push implementation always returns null.\nThis method should only be called with the same pool instance that provided\nid.\nParameters:\na Gst.TaskPool\nthe id\nJoin a task and/or return it to the pool. id is the id obtained from\nGst.TaskPool.push. The default implementation does nothing, as the\ndefault do_push implementation always returns None.\nThis method should only be called with the same pool instance that provided\nid.\nParameters:\na Gst.TaskPool\nthe id\n"});