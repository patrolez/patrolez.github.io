fragment_downloaded_cb({"url":"gstreamer/gstchildproxy.html#gst_child_proxy_lookup","fragment":"\n\tgst_child_proxy_lookup\n\n\n\tGst.ChildProxy.prototype.lookup\n\n\n\tGst.ChildProxy.lookup\n\nLooks up which object and GParamSpec would be effected by the given name.\nParameters:\nchild proxy object to lookup the property in\nname of the property to look up\npointer to a GObject that\ntakes the real object to set property on\npointer to take the GParamSpec\ndescribing the property\nTRUE if target and pspec could be found. FALSE otherwise. In that\ncase the values for pspec and target are not modified. Unref target after\nusage. For plain GObject target is the same as object.\nLooks up which object and GObject.ParamSpec would be effected by the given name.\nParameters:\nchild proxy object to lookup the property in\nname of the property to look up\nReturns a tuple made of:\ntrue if target and pspec could be found. false otherwise. In that\ncase the values for pspec and target are not modified. Unref target after\nusage. For plain GObject.Object target is the same as object.\ntrue if target and pspec could be found. false otherwise. In that\ncase the values for pspec and target are not modified. Unref target after\nusage. For plain GObject.Object target is the same as object.\ntrue if target and pspec could be found. false otherwise. In that\ncase the values for pspec and target are not modified. Unref target after\nusage. For plain GObject.Object target is the same as object.\nLooks up which object and GObject.ParamSpec would be effected by the given name.\nParameters:\nchild proxy object to lookup the property in\nname of the property to look up\nReturns a tuple made of:\nTrue if target and pspec could be found. False otherwise. In that\ncase the values for pspec and target are not modified. Unref target after\nusage. For plain GObject.Object target is the same as object.\nTrue if target and pspec could be found. False otherwise. In that\ncase the values for pspec and target are not modified. Unref target after\nusage. For plain GObject.Object target is the same as object.\nTrue if target and pspec could be found. False otherwise. In that\ncase the values for pspec and target are not modified. Unref target after\nusage. For plain GObject.Object target is the same as object.\n"});