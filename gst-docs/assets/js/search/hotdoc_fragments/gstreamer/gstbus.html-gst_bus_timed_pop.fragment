fragment_downloaded_cb({"url":"gstreamer/gstbus.html#gst_bus_timed_pop","fragment":"\n\tgst_bus_timed_pop\n\n\n\tGst.Bus.prototype.timed_pop\n\n\n\tGst.Bus.timed_pop\n\nGets a message from the bus, waiting up to the specified timeout.\nIf timeout is 0, this function behaves like gst_bus_pop. If timeout is\nGST_CLOCK_TIME_NONE, this function will block forever until a message was\nposted on the bus.\nParameters:\na GstBus to pop\na timeout\nthe GstMessage that is on the\nbus after the specified timeout or NULL if the bus is empty\nafter the timeout expired.\nGets a message from the bus, waiting up to the specified timeout.\nIf timeout is 0, this function behaves like Gst.Bus.prototype.pop. If timeout is\nGst.CLOCK_TIME_NONE, this function will block forever until a message was\nposted on the bus.\nParameters:\na Gst.Bus to pop\na timeout\nthe Gst.Message that is on the\nbus after the specified timeout or null if the bus is empty\nafter the timeout expired.\nGets a message from the bus, waiting up to the specified timeout.\nIf timeout is 0, this function behaves like Gst.Bus.pop. If timeout is\nGst.CLOCK_TIME_NONE, this function will block forever until a message was\nposted on the bus.\nParameters:\na Gst.Bus to pop\na timeout\nthe Gst.Message that is on the\nbus after the specified timeout or None if the bus is empty\nafter the timeout expired.\n"});