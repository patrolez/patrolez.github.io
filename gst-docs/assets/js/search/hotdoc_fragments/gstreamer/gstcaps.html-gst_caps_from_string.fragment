fragment_downloaded_cb({"url":"gstreamer/gstcaps.html#gst_caps_from_string","fragment":"\n\tgst_caps_from_string\n\n\n\tGst.prototype.caps_from_string\n\n\n\tGst.caps_from_string\n\nConverts caps from a string representation.\nThe implementation of serialization up to 1.20 would lead to unexpected results\nwhen there were nested GstCaps / GstStructure deeper than one level.\nParameters:\na string to convert to GstCaps\na newly allocated GstCaps\nConverts caps from a string representation.\nThe implementation of serialization up to 1.20 would lead to unexpected results\nwhen there were nested Gst.Caps / Gst.Structure deeper than one level.\nParameters:\na string to convert to Gst.Caps\na newly allocated Gst.Caps\nConverts caps from a string representation.\nThe implementation of serialization up to 1.20 would lead to unexpected results\nwhen there were nested Gst.Caps / Gst.Structure deeper than one level.\nParameters:\na string to convert to Gst.Caps\na newly allocated Gst.Caps\n"});