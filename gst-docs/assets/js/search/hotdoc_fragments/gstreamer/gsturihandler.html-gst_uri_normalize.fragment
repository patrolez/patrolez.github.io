fragment_downloaded_cb({"url":"gstreamer/gsturihandler.html#gst_uri_normalize","fragment":"\n\tgst_uri_normalize\n\n\n\tGst.Uri.prototype.normalize\n\n\n\tGst.Uri.normalize\n\nNormalization will remove extra path segments (\".\" and \"..\") from the URI. It\nwill also convert the scheme and host name to lower case and any\npercent-encoded values to uppercase.\nThe GstUri object must be writable. Check with gst_uri_is_writable or use\ngst_uri_make_writable first.\nParameters:\nThe GstUri to normalize.\nTRUE if the URI was modified.\n\n\tSince\t\t\t: 1.6\n\t\t\nNormalization will remove extra path segments (\".\" and \"..\") from the URI. It\nwill also convert the scheme and host name to lower case and any\npercent-encoded values to uppercase.\nThe Gst.Uri object must be writable. Check with Gst.Uri.prototype.is_writable or use\nGst.Uri.prototype.make_writable first.\nParameters:\nThe Gst.Uri to normalize.\nTRUE if the URI was modified.\n\n\tSince\t\t\t: 1.6\n\t\t\nNormalization will remove extra path segments (\".\" and \"..\") from the URI. It\nwill also convert the scheme and host name to lower case and any\npercent-encoded values to uppercase.\nThe Gst.Uri object must be writable. Check with Gst.Uri.is_writable or use\nGst.Uri.make_writable first.\nParameters:\nThe Gst.Uri to normalize.\nTRUE if the URI was modified.\n\n\tSince\t\t\t: 1.6\n\t\t\n"});