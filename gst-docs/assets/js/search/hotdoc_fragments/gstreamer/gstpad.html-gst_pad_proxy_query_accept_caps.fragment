fragment_downloaded_cb({"url":"gstreamer/gstpad.html#gst_pad_proxy_query_accept_caps","fragment":"\n\tgst_pad_proxy_query_accept_caps\n\n\n\tGst.Pad.prototype.proxy_query_accept_caps\n\n\n\tGst.Pad.proxy_query_accept_caps\n\nChecks if all internally linked pads of pad accepts the caps in query and\nreturns the intersection of the results.\nThis function is useful as a default accept caps query function for an element\nthat can handle any stream format, but requires caps that are acceptable for\nall opposite pads.\nParameters:\na GstPad to proxy.\nan ACCEPT_CAPS GstQuery.\nTRUE if query could be executed\nChecks if all internally linked pads of pad accepts the caps in query and\nreturns the intersection of the results.\nThis function is useful as a default accept caps query function for an element\nthat can handle any stream format, but requires caps that are acceptable for\nall opposite pads.\nParameters:\na Gst.Pad to proxy.\nan ACCEPT_CAPS Gst.Query.\ntrue if query could be executed\nChecks if all internally linked pads of pad accepts the caps in query and\nreturns the intersection of the results.\nThis function is useful as a default accept caps query function for an element\nthat can handle any stream format, but requires caps that are acceptable for\nall opposite pads.\nParameters:\na Gst.Pad to proxy.\nan ACCEPT_CAPS Gst.Query.\nTrue if query could be executed\n"});