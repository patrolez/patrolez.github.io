fragment_downloaded_cb({"url":"gstreamer/gstdeviceprovider.html#gst_device_provider_hide_provider","fragment":"\n\tgst_device_provider_hide_provider\n\n\n\tGst.DeviceProvider.prototype.hide_provider\n\n\n\tGst.DeviceProvider.hide_provider\n\nMake provider hide the devices from the factory with name.\nThis function is used when provider will also provide the devices reported\nby provider factory name. A monitor should stop monitoring the\ndevice provider with name to avoid duplicate devices.\nParameters:\na GstDeviceProvider\na provider factory name\n\n\tSince\t\t\t: 1.6\n\t\t\nMake provider hide the devices from the factory with name.\nThis function is used when provider will also provide the devices reported\nby provider factory name. A monitor should stop monitoring the\ndevice provider with name to avoid duplicate devices.\nParameters:\na Gst.DeviceProvider\na provider factory name\n\n\tSince\t\t\t: 1.6\n\t\t\nMake provider hide the devices from the factory with name.\nThis function is used when provider will also provide the devices reported\nby provider factory name. A monitor should stop monitoring the\ndevice provider with name to avoid duplicate devices.\nParameters:\na Gst.DeviceProvider\na provider factory name\n\n\tSince\t\t\t: 1.6\n\t\t\n"});