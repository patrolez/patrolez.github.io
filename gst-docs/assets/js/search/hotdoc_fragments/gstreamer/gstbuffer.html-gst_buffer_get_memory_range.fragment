fragment_downloaded_cb({"url":"gstreamer/gstbuffer.html#gst_buffer_get_memory_range","fragment":"\n\tgst_buffer_get_memory_range\n\n\n\tGst.Buffer.prototype.get_memory_range\n\n\n\tGst.Buffer.get_memory_range\n\nGets length memory blocks in buffer starting at idx. The memory blocks will\nbe merged into one large GstMemory.\nIf length is -1, all memory starting from idx is merged.\nParameters:\na GstBuffer.\nan index\na length\na GstMemory that contains the merged data of length\nblocks starting at idx.\nGets length memory blocks in buffer starting at idx. The memory blocks will\nbe merged into one large Gst.Memory.\nIf length is -1, all memory starting from idx is merged.\nParameters:\na Gst.Buffer.\nan index\na length\na Gst.Memory that contains the merged data of length\nblocks starting at idx.\nGets length memory blocks in buffer starting at idx. The memory blocks will\nbe merged into one large Gst.Memory.\nIf length is -1, all memory starting from idx is merged.\nParameters:\na Gst.Buffer.\nan index\na length\na Gst.Memory that contains the merged data of length\nblocks starting at idx.\n"});