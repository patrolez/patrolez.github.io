fragment_downloaded_cb({"url":"gstreamer/gstbufferpool.html#GstBufferPoolClass::acquire_buffer","fragment":"\n\tacquire_buffer\n\n\n\tvfunc_acquire_buffer\n\n\n\tdo_acquire_buffer\n\nGet a new buffer from the pool. The default implementation\nwill take a buffer from the queue and optionally wait for a buffer to\nbe released when there are no buffers available.\nParameters:\nthe GstBufferPool\na location for a GstBuffer\nparameters.\na GstFlowReturn such as GST_FLOW_FLUSHING when the pool is\ninactive.\nGet a new buffer from the pool. The default implementation\nwill take a buffer from the queue and optionally wait for a buffer to\nbe released when there are no buffers available.\nParameters:\nthe Gst.BufferPool\nparameters.\nReturns a tuple made of:\na Gst.FlowReturn such as Gst.FlowReturn.FLUSHING when the pool is\ninactive.\na Gst.FlowReturn such as Gst.FlowReturn.FLUSHING when the pool is\ninactive.\nGet a new buffer from the pool. The default implementation\nwill take a buffer from the queue and optionally wait for a buffer to\nbe released when there are no buffers available.\nParameters:\nthe Gst.BufferPool\nparameters.\nReturns a tuple made of:\na Gst.FlowReturn such as Gst.FlowReturn.FLUSHING when the pool is\ninactive.\na Gst.FlowReturn such as Gst.FlowReturn.FLUSHING when the pool is\ninactive.\n"});