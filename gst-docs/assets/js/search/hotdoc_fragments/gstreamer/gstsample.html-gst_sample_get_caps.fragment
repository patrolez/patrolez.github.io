fragment_downloaded_cb({"url":"gstreamer/gstsample.html#gst_sample_get_caps","fragment":"\n\tgst_sample_get_caps\n\n\n\tGst.Sample.prototype.get_caps\n\n\n\tGst.Sample.get_caps\n\nGet the caps associated with sample\nParameters:\na GstSample\nthe caps of sample or NULL\nwhen there is no caps. The caps remain valid as long as sample is\nvalid.  If you need to hold on to the caps for longer than that,\ntake a ref to the caps with gst_caps_ref.\nGet the caps associated with sample\nParameters:\na Gst.Sample\nthe caps of sample or null\nwhen there is no caps. The caps remain valid as long as sample is\nvalid.  If you need to hold on to the caps for longer than that,\ntake a ref to the caps with gst_caps_ref (not introspectable).\nGet the caps associated with sample\nParameters:\na Gst.Sample\nthe caps of sample or None\nwhen there is no caps. The caps remain valid as long as sample is\nvalid.  If you need to hold on to the caps for longer than that,\ntake a ref to the caps with gst_caps_ref (not introspectable).\n"});