fragment_downloaded_cb({"url":"gstreamer/gst.html#gst_init_check","fragment":"\n\tgst_init_check\n\n\n\tGst.prototype.init_check\n\n\n\tGst.init_check\n\nInitializes the GStreamer library, setting up internal path lists,\nregistering built-in elements, and loading standard plugins.\nThis function will return FALSE if GStreamer could not be initialized\nfor some reason.  If you want your program to fail fatally,\nuse gst_init instead.\nParameters:\npointer to application's argc\npointer to application's argv\npointer to a GError to which a message will be posted on error\nTRUE if GStreamer could be initialized.\nInitializes the GStreamer library, setting up internal path lists,\nregistering built-in elements, and loading standard plugins.\nThis function will return false if GStreamer could not be initialized\nfor some reason.  If you want your program to fail fatally,\nuse Gst.prototype.init instead.\nParameters:\npointer to application's argc\npointer to application's argv\nReturns a tuple made of:\ntrue if GStreamer could be initialized.\ntrue if GStreamer could be initialized.\ntrue if GStreamer could be initialized.\nInitializes the GStreamer library, setting up internal path lists,\nregistering built-in elements, and loading standard plugins.\nThis function will return False if GStreamer could not be initialized\nfor some reason.  If you want your program to fail fatally,\nuse Gst.init instead.\nParameters:\npointer to application's argc\npointer to application's argv\nReturns a tuple made of:\nTrue if GStreamer could be initialized.\nTrue if GStreamer could be initialized.\nTrue if GStreamer could be initialized.\n"});