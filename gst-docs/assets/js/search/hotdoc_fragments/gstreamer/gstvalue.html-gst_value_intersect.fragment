fragment_downloaded_cb({"url":"gstreamer/gstvalue.html#gst_value_intersect","fragment":"\n\tgst_value_intersect\n\n\n\tGst.prototype.value_intersect\n\n\n\tGst.value_intersect\n\nCalculates the intersection of two values.  If the values have\na non-empty intersection, the value representing the intersection\nis placed in dest, unless NULL.  If the intersection is non-empty,\ndest is not modified.\nParameters:\n(out caller-allocates) (transfer full) (allow-none):\na uninitialized GValue that will hold the calculated\nintersection value. May be NULL if the resulting set if not\nneeded.\na value to intersect\nanother value to intersect\nTRUE if the intersection is non-empty\nCalculates the intersection of two values.  If the values have\na non-empty intersection, the value representing the intersection\nis placed in dest, unless null.  If the intersection is non-empty,\ndest is not modified.\nParameters:\na value to intersect\nanother value to intersect\nReturns a tuple made of:\ntrue if the intersection is non-empty\ntrue if the intersection is non-empty\nCalculates the intersection of two values.  If the values have\na non-empty intersection, the value representing the intersection\nis placed in dest, unless None.  If the intersection is non-empty,\ndest is not modified.\nParameters:\na value to intersect\nanother value to intersect\nReturns a tuple made of:\nTrue if the intersection is non-empty\nTrue if the intersection is non-empty\n"});