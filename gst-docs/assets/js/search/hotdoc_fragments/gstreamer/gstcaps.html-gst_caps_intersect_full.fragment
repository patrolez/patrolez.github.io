fragment_downloaded_cb({"url":"gstreamer/gstcaps.html#gst_caps_intersect_full","fragment":"\n\tgst_caps_intersect_full\n\n\n\tGst.Caps.prototype.intersect_full\n\n\n\tGst.Caps.intersect_full\n\nCreates a new GstCaps that contains all the formats that are common\nto both caps1 and caps2, the order is defined by the GstCapsIntersectMode\nused.\nParameters:\na GstCaps to intersect\na GstCaps to intersect\nThe intersection algorithm/mode to use\nthe new GstCaps\nCreates a new Gst.Caps that contains all the formats that are common\nto both caps1 and caps2, the order is defined by the Gst.CapsIntersectMode\nused.\nParameters:\na Gst.Caps to intersect\na Gst.Caps to intersect\nThe intersection algorithm/mode to use\nthe new Gst.Caps\nCreates a new Gst.Caps that contains all the formats that are common\nto both caps1 and caps2, the order is defined by the Gst.CapsIntersectMode\nused.\nParameters:\na Gst.Caps to intersect\na Gst.Caps to intersect\nThe intersection algorithm/mode to use\nthe new Gst.Caps\n"});