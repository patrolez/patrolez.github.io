fragment_downloaded_cb({"url":"gstreamer/gstbus.html#gst_bus_get_pollfd","fragment":"\n\tgst_bus_get_pollfd\n\n\n\tGst.Bus.prototype.get_pollfd\n\n\n\tGst.Bus.get_pollfd\n\nGets the file descriptor from the bus which can be used to get notified about\nmessages being available with functions like g_poll, and allows integration\ninto other event loops based on file descriptors.\nWhenever a message is available, the POLLIN / G_IO_IN event is set.\nWarning: NEVER read or write anything to the returned fd but only use it\nfor getting notifications via g_poll or similar and then use the normal\nGstBus API, e.g. gst_bus_pop.\nParameters:\nA GstBus\nA GPollFD to fill\n\n\tSince\t\t\t: 1.14\n\t\t\nGets the file descriptor from the bus which can be used to get notified about\nmessages being available with functions like GLib.prototype.poll, and allows integration\ninto other event loops based on file descriptors.\nWhenever a message is available, the POLLIN / GLib.IOCondition.IN event is set.\nWarning: NEVER read or write anything to the returned fd but only use it\nfor getting notifications via GLib.prototype.poll or similar and then use the normal\nGstBus API, e.g. Gst.Bus.prototype.pop.\nParameters:\nA Gst.Bus\n\n\tSince\t\t\t: 1.14\n\t\t\nGets the file descriptor from the bus which can be used to get notified about\nmessages being available with functions like GLib.poll, and allows integration\ninto other event loops based on file descriptors.\nWhenever a message is available, the POLLIN / GLib.IOCondition.IN event is set.\nWarning: NEVER read or write anything to the returned fd but only use it\nfor getting notifications via GLib.poll or similar and then use the normal\nGstBus API, e.g. Gst.Bus.pop.\nParameters:\nA Gst.Bus\n\n\tSince\t\t\t: 1.14\n\t\t\n"});