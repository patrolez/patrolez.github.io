fragment_downloaded_cb({"url":"gstreamer/gstpoll.html#gst_poll_restart","fragment":"\n\tgst_poll_restart\n\n\n\tGst.Poll.prototype.restart\n\n\n\tGst.Poll.restart\n\nRestart any gst_poll_wait that is in progress. This function is typically\nused after adding or removing descriptors to set.\nIf set is not controllable, then this call will have no effect.\nThis function only works for non-timer GstPoll objects created with\ngst_poll_new.\nParameters:\na GstPoll.\nRestart any Gst.Poll.prototype.wait that is in progress. This function is typically\nused after adding or removing descriptors to set.\nIf set is not controllable, then this call will have no effect.\nThis function only works for non-timer Gst.Poll objects created with\ngst_poll_new (not introspectable).\nParameters:\na Gst.Poll.\nRestart any Gst.Poll.wait that is in progress. This function is typically\nused after adding or removing descriptors to set.\nIf set is not controllable, then this call will have no effect.\nThis function only works for non-timer Gst.Poll objects created with\ngst_poll_new (not introspectable).\nParameters:\na Gst.Poll.\n"});