fragment_downloaded_cb({"url":"gstreamer/gstbuffer.html#gst_buffer_find_memory","fragment":"\n\tgst_buffer_find_memory\n\n\n\tGst.Buffer.prototype.find_memory\n\n\n\tGst.Buffer.find_memory\n\nFinds the memory blocks that span size bytes starting from offset\nin buffer.\nWhen this function returns TRUE, idx will contain the index of the first\nmemory block where the byte for offset can be found and length contains the\nnumber of memory blocks containing the size remaining bytes. skip contains\nthe number of bytes to skip in the memory block at idx to get to the byte\nfor offset.\nsize can be -1 to get all the memory blocks after idx.\nParameters:\na GstBuffer.\nan offset\na size\npointer to index\npointer to length\npointer to skip\nTRUE when size bytes starting from offset could be found in\nbuffer and idx, length and skip will be filled.\nFinds the memory blocks that span size bytes starting from offset\nin buffer.\nWhen this function returns true, idx will contain the index of the first\nmemory block where the byte for offset can be found and length contains the\nnumber of memory blocks containing the size remaining bytes. skip contains\nthe number of bytes to skip in the memory block at idx to get to the byte\nfor offset.\nsize can be -1 to get all the memory blocks after idx.\nParameters:\na Gst.Buffer.\nan offset\na size\nReturns a tuple made of:\ntrue when size bytes starting from offset could be found in\nbuffer and idx, length and skip will be filled.\ntrue when size bytes starting from offset could be found in\nbuffer and idx, length and skip will be filled.\ntrue when size bytes starting from offset could be found in\nbuffer and idx, length and skip will be filled.\ntrue when size bytes starting from offset could be found in\nbuffer and idx, length and skip will be filled.\nFinds the memory blocks that span size bytes starting from offset\nin buffer.\nWhen this function returns True, idx will contain the index of the first\nmemory block where the byte for offset can be found and length contains the\nnumber of memory blocks containing the size remaining bytes. skip contains\nthe number of bytes to skip in the memory block at idx to get to the byte\nfor offset.\nsize can be -1 to get all the memory blocks after idx.\nParameters:\na Gst.Buffer.\nan offset\na size\nReturns a tuple made of:\nTrue when size bytes starting from offset could be found in\nbuffer and idx, length and skip will be filled.\nTrue when size bytes starting from offset could be found in\nbuffer and idx, length and skip will be filled.\nTrue when size bytes starting from offset could be found in\nbuffer and idx, length and skip will be filled.\nTrue when size bytes starting from offset could be found in\nbuffer and idx, length and skip will be filled.\n"});