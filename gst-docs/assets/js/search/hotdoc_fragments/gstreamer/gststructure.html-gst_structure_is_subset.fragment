fragment_downloaded_cb({"url":"gstreamer/gststructure.html#gst_structure_is_subset","fragment":"\n\tgst_structure_is_subset\n\n\n\tGst.Structure.prototype.is_subset\n\n\n\tGst.Structure.is_subset\n\nChecks if subset is a subset of superset, i.e. has the same\nstructure name and for all fields that are existing in superset,\nsubset has a value that is a subset of the value in superset.\nParameters:\na GstStructure\na potentially greater GstStructure\nTRUE if subset is a subset of superset\nChecks if subset is a subset of superset, i.e. has the same\nstructure name and for all fields that are existing in superset,\nsubset has a value that is a subset of the value in superset.\nParameters:\na Gst.Structure\na potentially greater Gst.Structure\ntrue if subset is a subset of superset\nChecks if subset is a subset of superset, i.e. has the same\nstructure name and for all fields that are existing in superset,\nsubset has a value that is a subset of the value in superset.\nParameters:\na Gst.Structure\na potentially greater Gst.Structure\nTrue if subset is a subset of superset\n"});