fragment_downloaded_cb({"url":"gstreamer/gstquery.html#gst_query_parse_allocation","fragment":"\n\tgst_query_parse_allocation\n\n\n\tGst.Query.prototype.parse_allocation\n\n\n\tGst.Query.parse_allocation\n\nParse an allocation query, writing the requested caps in caps and\nwhether a pool is needed in need_pool, if the respective parameters\nare non-%NULL.\nPool details can be retrieved using gst_query_get_n_allocation_pools and\ngst_query_parse_nth_allocation_pool.\nParameters:\na GstQuery\nThe GstCaps\nWhether a GstBufferPool is needed\nParse an allocation query, writing the requested caps in caps and\nwhether a pool is needed in need_pool, if the respective parameters\nare non-%NULL.\nPool details can be retrieved using Gst.Query.prototype.get_n_allocation_pools and\nGst.Query.prototype.parse_nth_allocation_pool.\nParameters:\na Gst.Query\nParse an allocation query, writing the requested caps in caps and\nwhether a pool is needed in need_pool, if the respective parameters\nare non-%NULL.\nPool details can be retrieved using Gst.Query.get_n_allocation_pools and\nGst.Query.parse_nth_allocation_pool.\nParameters:\na Gst.Query\n"});