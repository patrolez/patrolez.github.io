fragment_downloaded_cb({"url":"gstreamer/gstobject.html#gst_object_has_ancestor","fragment":"\n\tgst_object_has_ancestor\n\n\n\tGst.Object.prototype.has_ancestor\n\n\n\tGst.Object.has_ancestor\n\nCheck if object has an ancestor ancestor somewhere up in\nthe hierarchy. One can e.g. check if a GstElement is inside a GstPipeline.\nParameters:\na GstObject to check\na GstObject to check as ancestor\nTRUE if ancestor is an ancestor of object.\n\n\tdeprecated\t\t\t: Use gst_object_has_as_ancestor() instead.\n\n MT safe. Grabs and releases @object's locks.\n\t\t\nCheck if object has an ancestor ancestor somewhere up in\nthe hierarchy. One can e.g. check if a Gst.Element is inside a Gst.Pipeline.\nParameters:\na Gst.Object to check\na Gst.Object to check as ancestor\ntrue if ancestor is an ancestor of object.\n\n\tdeprecated\t\t\t: Use gst_object_has_as_ancestor() instead.\n\n MT safe. Grabs and releases @object's locks.\n\t\t\nCheck if object has an ancestor ancestor somewhere up in\nthe hierarchy. One can e.g. check if a Gst.Element is inside a Gst.Pipeline.\nParameters:\na Gst.Object to check\na Gst.Object to check as ancestor\nTrue if ancestor is an ancestor of object.\n\n\tdeprecated\t\t\t: Use gst_object_has_as_ancestor() instead.\n\n MT safe. Grabs and releases @object's locks.\n\t\t\n"});