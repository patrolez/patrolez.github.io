fragment_downloaded_cb({"url":"gstreamer/gststructure.html#gst_structure_get_double","fragment":"\n\tgst_structure_get_double\n\n\n\tGst.Structure.prototype.get_double\n\n\n\tGst.Structure.get_double\n\nSets the double pointed to by value corresponding to the value of the\ngiven field.  Caller is responsible for making sure the field exists\nand has the correct type.\nParameters:\na GstStructure\nthe name of a field\na pointer to a gdouble to set\nTRUE if the value could be set correctly. If there was no field\nwith fieldname or the existing field did not contain a double, this\nfunction returns FALSE.\nSets the double pointed to by value corresponding to the value of the\ngiven field.  Caller is responsible for making sure the field exists\nand has the correct type.\nParameters:\na Gst.Structure\nthe name of a field\nReturns a tuple made of:\ntrue if the value could be set correctly. If there was no field\nwith fieldname or the existing field did not contain a double, this\nfunction returns false.\ntrue if the value could be set correctly. If there was no field\nwith fieldname or the existing field did not contain a double, this\nfunction returns false.\nSets the double pointed to by value corresponding to the value of the\ngiven field.  Caller is responsible for making sure the field exists\nand has the correct type.\nParameters:\na Gst.Structure\nthe name of a field\nReturns a tuple made of:\nTrue if the value could be set correctly. If there was no field\nwith fieldname or the existing field did not contain a double, this\nfunction returns False.\nTrue if the value could be set correctly. If there was no field\nwith fieldname or the existing field did not contain a double, this\nfunction returns False.\n"});