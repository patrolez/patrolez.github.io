fragment_downloaded_cb({"url":"gstreamer/gstregistry.html#gst_registry_add_plugin","fragment":"\n\tgst_registry_add_plugin\n\n\n\tGst.Registry.prototype.add_plugin\n\n\n\tGst.Registry.add_plugin\n\nAdd the plugin to the registry. The plugin-added signal will be emitted.\nplugin's reference count will be incremented, and any floating\nreference will be removed (see gst_object_ref_sink)\nParameters:\nthe registry to add the plugin to\nthe plugin to add\nTRUE on success.\nMT safe.\nAdd the plugin to the registry. The plugin-added signal will be emitted.\nplugin's reference count will be incremented, and any floating\nreference will be removed (see gst_object_ref_sink (not introspectable))\nParameters:\nthe registry to add the plugin to\nthe plugin to add\ntrue on success.\nMT safe.\nAdd the plugin to the registry. The plugin-added signal will be emitted.\nplugin's reference count will be incremented, and any floating\nreference will be removed (see gst_object_ref_sink (not introspectable))\nParameters:\nthe registry to add the plugin to\nthe plugin to add\nTrue on success.\nMT safe.\n"});