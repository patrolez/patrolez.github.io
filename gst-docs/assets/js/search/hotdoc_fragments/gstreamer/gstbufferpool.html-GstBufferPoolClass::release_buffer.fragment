fragment_downloaded_cb({"url":"gstreamer/gstbufferpool.html#GstBufferPoolClass::release_buffer","fragment":"\n\trelease_buffer\n\n\n\tvfunc_release_buffer\n\n\n\tdo_release_buffer\n\nRelease a buffer back in the pool. The default implementation\nwill put the buffer back in the queue and notify any\nblocking acquire_buffer calls when the\nGST_BUFFER_FLAG_TAG_MEMORY is not set on the buffer.\nIf GST_BUFFER_FLAG_TAG_MEMORY is set, the buffer will be freed with\nfree_buffer.\nParameters:\nthe GstBufferPool\nthe GstBuffer to release\nRelease a buffer back in the pool. The default implementation\nwill put the buffer back in the queue and notify any\nblocking vfunc_acquire_buffer calls when the\nGst.BufferFlags.TAG_MEMORY is not set on the buffer.\nIf Gst.BufferFlags.TAG_MEMORY is set, the buffer will be freed with\nvfunc_free_buffer.\nParameters:\nthe Gst.BufferPool\nthe Gst.Buffer to release\nRelease a buffer back in the pool. The default implementation\nwill put the buffer back in the queue and notify any\nblocking do_acquire_buffer calls when the\nGst.BufferFlags.TAG_MEMORY is not set on the buffer.\nIf Gst.BufferFlags.TAG_MEMORY is set, the buffer will be freed with\ndo_free_buffer.\nParameters:\nthe Gst.BufferPool\nthe Gst.Buffer to release\n"});