fragment_downloaded_cb({"url":"gstreamer/gsttaglist.html#gst_tag_list_insert","fragment":"\n\tgst_tag_list_insert\n\n\n\tGst.TagList.prototype.insert\n\n\n\tGst.TagList.insert\n\nInserts the tags of the from list into the first list using the given mode.\nParameters:\nlist to merge into\nlist to merge from\nthe mode to use\nInserts the tags of the from list into the first list using the given mode.\nParameters:\nlist to merge into\nlist to merge from\nthe mode to use\nInserts the tags of the from list into the first list using the given mode.\nParameters:\nlist to merge into\nlist to merge from\nthe mode to use\n"});