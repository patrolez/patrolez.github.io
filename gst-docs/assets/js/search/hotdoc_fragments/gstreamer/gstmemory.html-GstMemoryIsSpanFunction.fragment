fragment_downloaded_cb({"url":"gstreamer/gstmemory.html#GstMemoryIsSpanFunction","fragment":"\n\tGstMemoryIsSpanFunction\n\n\n\tGst.MemoryIsSpanFunction\n\n\n\tGst.MemoryIsSpanFunction\n\nCheck if mem1 and mem2 occupy contiguous memory and return the offset of\nmem1 in the parent buffer in offset.\nParameters:\na GstMemory\na GstMemory\na result offset\nTRUE if mem1 and mem2 are in contiguous memory.\nCheck if mem1 and mem2 occupy contiguous memory and return the offset of\nmem1 in the parent buffer in offset.\nParameters:\na Gst.Memory\na Gst.Memory\na result offset\ntrue if mem1 and mem2 are in contiguous memory.\nCheck if mem1 and mem2 occupy contiguous memory and return the offset of\nmem1 in the parent buffer in offset.\nParameters:\na Gst.Memory\na Gst.Memory\na result offset\nTrue if mem1 and mem2 are in contiguous memory.\n"});