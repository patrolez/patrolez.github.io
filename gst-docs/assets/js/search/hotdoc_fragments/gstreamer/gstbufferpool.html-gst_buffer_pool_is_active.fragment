fragment_downloaded_cb({"url":"gstreamer/gstbufferpool.html#gst_buffer_pool_is_active","fragment":"\n\tgst_buffer_pool_is_active\n\n\n\tGst.BufferPool.prototype.is_active\n\n\n\tGst.BufferPool.is_active\n\nChecks if pool is active. A pool can be activated with the\ngst_buffer_pool_set_active call.\nParameters:\na GstBufferPool\nTRUE when the pool is active.\nChecks if pool is active. A pool can be activated with the\nGst.BufferPool.prototype.set_active call.\nParameters:\na Gst.BufferPool\ntrue when the pool is active.\nChecks if pool is active. A pool can be activated with the\nGst.BufferPool.set_active call.\nParameters:\na Gst.BufferPool\nTrue when the pool is active.\n"});