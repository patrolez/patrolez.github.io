fragment_downloaded_cb({"url":"gstreamer/gstsample.html#gst_sample_get_buffer_list","fragment":"\n\tgst_sample_get_buffer_list\n\n\n\tGst.Sample.prototype.get_buffer_list\n\n\n\tGst.Sample.get_buffer_list\n\nGet the buffer list associated with sample\nParameters:\na GstSample\nthe buffer list of sample or NULL\nwhen there is no buffer list. The buffer list remains valid as long as\nsample is valid.  If you need to hold on to it for longer than\nthat, take a ref to the buffer list with gst_mini_object_ref ().\n\n\tSince\t\t\t: 1.6\n\t\t\nGet the buffer list associated with sample\nParameters:\na Gst.Sample\nthe buffer list of sample or null\nwhen there is no buffer list. The buffer list remains valid as long as\nsample is valid.  If you need to hold on to it for longer than\nthat, take a ref to the buffer list with gst_mini_object_ref ().\n\n\tSince\t\t\t: 1.6\n\t\t\nGet the buffer list associated with sample\nParameters:\na Gst.Sample\nthe buffer list of sample or None\nwhen there is no buffer list. The buffer list remains valid as long as\nsample is valid.  If you need to hold on to it for longer than\nthat, take a ref to the buffer list with gst_mini_object_ref ().\n\n\tSince\t\t\t: 1.6\n\t\t\n"});