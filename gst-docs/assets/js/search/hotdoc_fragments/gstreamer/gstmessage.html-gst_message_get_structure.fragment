fragment_downloaded_cb({"url":"gstreamer/gstmessage.html#gst_message_get_structure","fragment":"\n\tgst_message_get_structure\n\n\n\tGst.Message.prototype.get_structure\n\n\n\tGst.Message.get_structure\n\nAccess the structure of the message.\nParameters:\nThe GstMessage.\nThe structure of the message. The\nstructure is still owned by the message, which means that you should not\nfree it and that the pointer becomes invalid when you free the message.\nMT safe.\nAccess the structure of the message.\nParameters:\nThe Gst.Message.\nThe structure of the message. The\nstructure is still owned by the message, which means that you should not\nfree it and that the pointer becomes invalid when you free the message.\nMT safe.\nAccess the structure of the message.\nParameters:\nThe Gst.Message.\nThe structure of the message. The\nstructure is still owned by the message, which means that you should not\nfree it and that the pointer becomes invalid when you free the message.\nMT safe.\n"});