fragment_downloaded_cb({"url":"gstreamer/gsttaskpool.html#gst_shared_task_pool_set_max_threads","fragment":"\n\tgst_shared_task_pool_set_max_threads\n\n\n\tGst.SharedTaskPool.prototype.set_max_threads\n\n\n\tGst.SharedTaskPool.set_max_threads\n\nUpdate the maximal number of threads the pool may spawn. When\nthe maximal number of threads is reduced, existing threads are not\nimmediately shut down, see g_thread_pool_set_max_threads.\nSetting max_threads to 0 effectively freezes the pool.\nParameters:\na GstSharedTaskPool\nMaximum number of threads to spawn.\n\n\tSince\t\t\t: 1.20\n\t\t\nUpdate the maximal number of threads the pool may spawn. When\nthe maximal number of threads is reduced, existing threads are not\nimmediately shut down, see GLib.ThreadPool.prototype.set_max_threads.\nSetting max_threads to 0 effectively freezes the pool.\nParameters:\na Gst.SharedTaskPool\nMaximum number of threads to spawn.\n\n\tSince\t\t\t: 1.20\n\t\t\nUpdate the maximal number of threads the pool may spawn. When\nthe maximal number of threads is reduced, existing threads are not\nimmediately shut down, see GLib.ThreadPool.set_max_threads.\nSetting max_threads to 0 effectively freezes the pool.\nParameters:\na Gst.SharedTaskPool\nMaximum number of threads to spawn.\n\n\tSince\t\t\t: 1.20\n\t\t\n"});