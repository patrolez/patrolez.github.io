fragment_downloaded_cb({"url":"gstreamer/gstbufferpool.html#GstBufferPoolClass::stop","fragment":"\n\tstop\n\n\n\tvfunc_stop\n\n\n\tdo_stop\n\nStop the bufferpool. the default implementation will free the\npreallocated buffers. This function is called when all the buffers are\nreturned to the pool.\nParameters:\nthe GstBufferPool\nwhether the pool could be stopped.\nStop the bufferpool. the default implementation will free the\npreallocated buffers. This function is called when all the buffers are\nreturned to the pool.\nParameters:\nthe Gst.BufferPool\nwhether the pool could be stopped.\nStop the bufferpool. the default implementation will free the\npreallocated buffers. This function is called when all the buffers are\nreturned to the pool.\nParameters:\nthe Gst.BufferPool\nwhether the pool could be stopped.\n"});