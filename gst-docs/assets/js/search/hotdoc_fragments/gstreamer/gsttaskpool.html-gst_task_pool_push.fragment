fragment_downloaded_cb({"url":"gstreamer/gsttaskpool.html#gst_task_pool_push","fragment":"\n\tgst_task_pool_push\n\n\n\tGst.TaskPool.prototype.push\n\n\n\tGst.TaskPool.push\n\nStart the execution of a new thread from pool.\nParameters:\na GstTaskPool\nthe function to call\ndata to pass to func\nreturn location for an error\na pointer that should be used\nfor the gst_task_pool_join function. This pointer can be NULL, you\nmust check error to detect errors. If the pointer is not NULL and\ngst_task_pool_join is not used, call gst_task_pool_dispose_handle\ninstead.\nStart the execution of a new thread from pool.\nParameters:\na Gst.TaskPool\nthe function to call\ndata to pass to func\na pointer that should be used\nfor the gst_task_pool_join function. This pointer can be null, you\nmust check error to detect errors. If the pointer is not null and\nGst.TaskPool.prototype.join is not used, call Gst.TaskPool.prototype.dispose_handle\ninstead.\nStart the execution of a new thread from pool.\nParameters:\na Gst.TaskPool\nthe function to call\ndata to pass to func\na pointer that should be used\nfor the gst_task_pool_join function. This pointer can be None, you\nmust check error to detect errors. If the pointer is not None and\nGst.TaskPool.join is not used, call Gst.TaskPool.dispose_handle\ninstead.\n"});