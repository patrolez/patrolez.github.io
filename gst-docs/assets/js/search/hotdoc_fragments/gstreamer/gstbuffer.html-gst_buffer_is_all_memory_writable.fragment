fragment_downloaded_cb({"url":"gstreamer/gstbuffer.html#gst_buffer_is_all_memory_writable","fragment":"\n\tgst_buffer_is_all_memory_writable\n\n\n\tGst.Buffer.prototype.is_all_memory_writable\n\n\n\tGst.Buffer.is_all_memory_writable\n\nChecks if all memory blocks in buffer are writable.\nNote that this function does not check if buffer is writable, use\ngst_buffer_is_writable to check that if needed.\nParameters:\na GstBuffer.\nTRUE if all memory blocks in buffer are writable\n\n\tSince\t\t\t: 1.4\n\t\t\nChecks if all memory blocks in buffer are writable.\nNote that this function does not check if buffer is writable, use\ngst_buffer_is_writable (not introspectable) to check that if needed.\nParameters:\na Gst.Buffer.\ntrue if all memory blocks in buffer are writable\n\n\tSince\t\t\t: 1.4\n\t\t\nChecks if all memory blocks in buffer are writable.\nNote that this function does not check if buffer is writable, use\ngst_buffer_is_writable (not introspectable) to check that if needed.\nParameters:\na Gst.Buffer.\nTrue if all memory blocks in buffer are writable\n\n\tSince\t\t\t: 1.4\n\t\t\n"});