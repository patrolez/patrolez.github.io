fragment_downloaded_cb({"url":"gstreamer/gststructure.html#gst_structure_map_in_place","fragment":"\n\tgst_structure_map_in_place\n\n\n\tGst.Structure.prototype.map_in_place\n\n\n\tGst.Structure.map_in_place\n\nCalls the provided function once for each field in the GstStructure. In\ncontrast to gst_structure_foreach, the function may modify but not delete the\nfields. The structure must be mutable.\nParameters:\na GstStructure\na function to call for each field\nprivate data\nTRUE if the supplied function returns TRUE For each of the fields,\nFALSE otherwise.\nCalls the provided function once for each field in the Gst.Structure. In\ncontrast to Gst.Structure.prototype.foreach, the function may modify but not delete the\nfields. The structure must be mutable.\nParameters:\na Gst.Structure\na function to call for each field\nprivate data\ntrue if the supplied function returns true For each of the fields,\nfalse otherwise.\nCalls the provided function once for each field in the Gst.Structure. In\ncontrast to Gst.Structure.foreach, the function may modify but not delete the\nfields. The structure must be mutable.\nParameters:\na Gst.Structure\na function to call for each field\nprivate data\nTrue if the supplied function returns True For each of the fields,\nFalse otherwise.\n"});