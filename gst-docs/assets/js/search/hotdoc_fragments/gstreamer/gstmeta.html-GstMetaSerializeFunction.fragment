fragment_downloaded_cb({"url":"gstreamer/gstmeta.html#GstMetaSerializeFunction","fragment":"\n\tGstMetaSerializeFunction\n\n\n\tGst.MetaSerializeFunction\n\n\n\tGst.MetaSerializeFunction\n\nSerialize meta into a format that can be stored or transmitted and later\ndeserialized by GstMetaDeserializeFunction.\nBy default version is set to 0, it should be bumped if incompatible changes\nare made to the format so GstMetaDeserializeFunction can deserialize each\nversion.\nParameters:\na GstMeta\nGstByteArrayInterface to append serialization data\nversion of the serialization format\nTRUE on success, FALSE otherwise.\n\n\tSince\t\t\t: 1.24\n\t\t\nSerialize meta into a format that can be stored or transmitted and later\ndeserialized by Gst.MetaDeserializeFunction.\nBy default version is set to 0, it should be bumped if incompatible changes\nare made to the format so Gst.MetaDeserializeFunction can deserialize each\nversion.\nParameters:\na Gst.Meta\nGst.ByteArrayInterface to append serialization data\nReturns a tuple made of:\ntrue on success, false otherwise.\ntrue on success, false otherwise.\n\n\tSince\t\t\t: 1.24\n\t\t\nSerialize meta into a format that can be stored or transmitted and later\ndeserialized by Gst.MetaDeserializeFunction.\nBy default version is set to 0, it should be bumped if incompatible changes\nare made to the format so Gst.MetaDeserializeFunction can deserialize each\nversion.\nParameters:\na Gst.Meta\nGst.ByteArrayInterface to append serialization data\nReturns a tuple made of:\nTrue on success, False otherwise.\nTrue on success, False otherwise.\n\n\tSince\t\t\t: 1.24\n\t\t\n"});