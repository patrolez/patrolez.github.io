fragment_downloaded_cb({"url":"gstreamer/gstelementfactory.html#gst_element_factory_get_metadata_keys","fragment":"\n\tgst_element_factory_get_metadata_keys\n\n\n\tGst.ElementFactory.prototype.get_metadata_keys\n\n\n\tGst.ElementFactory.get_metadata_keys\n\nGet the available keys for the metadata on factory.\nParameters:\na GstElementFactory\n(transfer full) (element-type utf8) (array zero-terminated=1) (nullable):\na NULL-terminated array of key strings, or NULL when there is no\nmetadata. Free with g_strfreev when no longer needed.\nGet the available keys for the metadata on factory.\nParameters:\na Gst.ElementFactory\n(transfer full) (element-type utf8) (array zero-terminated=1) (nullable):\na null-terminated array of key strings, or null when there is no\nmetadata. Free with GLib.prototype.strfreev when no longer needed.\nGet the available keys for the metadata on factory.\nParameters:\na Gst.ElementFactory\n(transfer full) (element-type utf8) (array zero-terminated=1) (nullable):\na None-terminated array of key strings, or None when there is no\nmetadata. Free with GLib.strfreev when no longer needed.\n"});