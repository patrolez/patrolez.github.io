fragment_downloaded_cb({"url":"gstreamer/gsttaglist.html#gst_tag_list_get_sample","fragment":"\n\tgst_tag_list_get_sample\n\n\n\tGst.TagList.prototype.get_sample\n\n\n\tGst.TagList.get_sample\n\nCopies the first sample for the given tag in the taglist into the variable\npointed to by sample. Free the sample with gst_sample_unref when it is\nno longer needed. You can retrieve the buffer from the sample using\ngst_sample_get_buffer and the associated caps (if any) with\ngst_sample_get_caps.\nFree-function: gst_sample_unref\nParameters:\na GstTagList to get the tag from\ntag to read out\naddress of a GstSample\npointer variable to store the result into\nTRUE, if a sample was returned, FALSE if the tag didn't exist in\nthe given list or if it was NULL.\nCopies the first sample for the given tag in the taglist into the variable\npointed to by sample. Free the sample with gst_sample_unref (not introspectable) when it is\nno longer needed. You can retrieve the buffer from the sample using\nGst.Sample.prototype.get_buffer and the associated caps (if any) with\nGst.Sample.prototype.get_caps.\nFree-function: gst_sample_unref\nParameters:\na Gst.TagList to get the tag from\ntag to read out\nReturns a tuple made of:\ntrue, if a sample was returned, false if the tag didn't exist in\nthe given list or if it was null.\ntrue, if a sample was returned, false if the tag didn't exist in\nthe given list or if it was null.\nCopies the first sample for the given tag in the taglist into the variable\npointed to by sample. Free the sample with gst_sample_unref (not introspectable) when it is\nno longer needed. You can retrieve the buffer from the sample using\nGst.Sample.get_buffer and the associated caps (if any) with\nGst.Sample.get_caps.\nFree-function: gst_sample_unref\nParameters:\na Gst.TagList to get the tag from\ntag to read out\nReturns a tuple made of:\nTrue, if a sample was returned, False if the tag didn't exist in\nthe given list or if it was None.\nTrue, if a sample was returned, False if the tag didn't exist in\nthe given list or if it was None.\n"});