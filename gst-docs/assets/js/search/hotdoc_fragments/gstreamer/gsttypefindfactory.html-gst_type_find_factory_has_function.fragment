fragment_downloaded_cb({"url":"gstreamer/gsttypefindfactory.html#gst_type_find_factory_has_function","fragment":"\n\tgst_type_find_factory_has_function\n\n\n\tGst.TypeFindFactory.prototype.has_function\n\n\n\tGst.TypeFindFactory.has_function\n\nCheck whether the factory has a typefind function. Typefind factories\nwithout typefind functions are a last-effort fallback mechanism to\ne.g. assume a certain media type based on the file extension.\nParameters:\nA GstTypeFindFactory\nTRUE if the factory has a typefind functions set, otherwise FALSE\nCheck whether the factory has a typefind function. Typefind factories\nwithout typefind functions are a last-effort fallback mechanism to\ne.g. assume a certain media type based on the file extension.\nParameters:\nA Gst.TypeFindFactory\ntrue if the factory has a typefind functions set, otherwise false\nCheck whether the factory has a typefind function. Typefind factories\nwithout typefind functions are a last-effort fallback mechanism to\ne.g. assume a certain media type based on the file extension.\nParameters:\nA Gst.TypeFindFactory\nTrue if the factory has a typefind functions set, otherwise False\n"});