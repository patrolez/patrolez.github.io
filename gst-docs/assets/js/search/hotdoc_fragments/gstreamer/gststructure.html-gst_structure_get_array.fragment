fragment_downloaded_cb({"url":"gstreamer/gststructure.html#gst_structure_get_array","fragment":"\n\tgst_structure_get_array\n\n\n\tGst.Structure.prototype.get_array\n\n\n\tGst.Structure.get_array\n\nThis is useful in language bindings where unknown GValue types are not\nsupported. This function will convert the GST_TYPE_ARRAY into a newly\nallocated GValueArray and return it through array. Be aware that this is\nslower then getting the GValue directly.\nParameters:\na GstStructure\nthe name of a field\na pointer to a GValueArray\nTRUE if the value could be set correctly. If there was no field\nwith fieldname or the existing field did not contain a GST_TYPE_ARRAY,\nthis function returns FALSE.\n\n\tSince\t\t\t: 1.12\n\t\t\nThis is useful in language bindings where unknown GObject.Value types are not\nsupported. This function will convert the GST_TYPE_ARRAY (not introspectable) into a newly\nallocated GObject.ValueArray and return it through array. Be aware that this is\nslower then getting the GObject.Value directly.\nParameters:\na Gst.Structure\nthe name of a field\nReturns a tuple made of:\ntrue if the value could be set correctly. If there was no field\nwith fieldname or the existing field did not contain a GST_TYPE_ARRAY (not introspectable),\nthis function returns false.\ntrue if the value could be set correctly. If there was no field\nwith fieldname or the existing field did not contain a GST_TYPE_ARRAY (not introspectable),\nthis function returns false.\n\n\tSince\t\t\t: 1.12\n\t\t\nThis is useful in language bindings where unknown GObject.Value types are not\nsupported. This function will convert the GST_TYPE_ARRAY (not introspectable) into a newly\nallocated GObject.ValueArray and return it through array. Be aware that this is\nslower then getting the GObject.Value directly.\nParameters:\na Gst.Structure\nthe name of a field\nReturns a tuple made of:\nTrue if the value could be set correctly. If there was no field\nwith fieldname or the existing field did not contain a GST_TYPE_ARRAY (not introspectable),\nthis function returns False.\nTrue if the value could be set correctly. If there was no field\nwith fieldname or the existing field did not contain a GST_TYPE_ARRAY (not introspectable),\nthis function returns False.\n\n\tSince\t\t\t: 1.12\n\t\t\n"});