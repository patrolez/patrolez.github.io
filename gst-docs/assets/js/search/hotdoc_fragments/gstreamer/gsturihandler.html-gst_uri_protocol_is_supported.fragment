fragment_downloaded_cb({"url":"gstreamer/gsturihandler.html#gst_uri_protocol_is_supported","fragment":"\n\tgst_uri_protocol_is_supported\n\n\n\tGst.prototype.uri_protocol_is_supported\n\n\n\tGst.uri_protocol_is_supported\n\nChecks if an element exists that supports the given URI protocol. Note\nthat a positive return value does not imply that a subsequent call to\ngst_element_make_from_uri is guaranteed to work.\nParameters:\nWhether to check for a source or a sink\nProtocol that should be checked for (e.g. \"http\" or \"smb\")\nTRUE\nChecks if an element exists that supports the given URI protocol. Note\nthat a positive return value does not imply that a subsequent call to\nGst.Element.prototype.make_from_uri is guaranteed to work.\nParameters:\nWhether to check for a source or a sink\nProtocol that should be checked for (e.g. \"http\" or \"smb\")\ntrue\nChecks if an element exists that supports the given URI protocol. Note\nthat a positive return value does not imply that a subsequent call to\nGst.Element.make_from_uri is guaranteed to work.\nParameters:\nWhether to check for a source or a sink\nProtocol that should be checked for (e.g. \"http\" or \"smb\")\nTrue\n"});