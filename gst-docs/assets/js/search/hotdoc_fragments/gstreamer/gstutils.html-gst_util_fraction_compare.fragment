fragment_downloaded_cb({"url":"gstreamer/gstutils.html#gst_util_fraction_compare","fragment":"\n\tgst_util_fraction_compare\n\n\n\tGst.prototype.util_fraction_compare\n\n\n\tGst.util_fraction_compare\n\nCompares the fractions a_n/@a_d and b_n/@b_d and returns\n-1 if a < b, 0 if a = b and 1 if a > b.\nParameters:\nNumerator of first value\nDenominator of first value\nNumerator of second value\nDenominator of second value\n-1 if a < b; 0 if a = b; 1 if a > b.\nCompares the fractions a_n/@a_d and b_n/@b_d and returns\n-1 if a < b, 0 if a = b and 1 if a > b.\nParameters:\nNumerator of first value\nDenominator of first value\nNumerator of second value\nDenominator of second value\n-1 if a < b; 0 if a = b; 1 if a > b.\nCompares the fractions a_n/@a_d and b_n/@b_d and returns\n-1 if a < b, 0 if a = b and 1 if a > b.\nParameters:\nNumerator of first value\nDenominator of first value\nNumerator of second value\nDenominator of second value\n-1 if a < b; 0 if a = b; 1 if a > b.\n"});