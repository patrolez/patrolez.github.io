fragment_downloaded_cb({"url":"gstreamer/gstcaps.html#gst_caps_normalize","fragment":"\n\tgst_caps_normalize\n\n\n\tGst.Caps.prototype.normalize\n\n\n\tGst.Caps.normalize\n\nReturns a GstCaps that represents the same set of formats as\ncaps, but contains no lists.  Each list is expanded into separate\nGstStructure.\nThis function takes ownership of caps and will call gst_caps_make_writable\non it so you must not use caps afterwards unless you keep an additional\nreference to it with gst_caps_ref.\nParameters:\na GstCaps to normalize\nthe normalized GstCaps\nReturns a Gst.Caps that represents the same set of formats as\ncaps, but contains no lists.  Each list is expanded into separate\nGst.Structure.\nThis function takes ownership of caps and will call gst_caps_make_writable (not introspectable)\non it so you must not use caps afterwards unless you keep an additional\nreference to it with gst_caps_ref (not introspectable).\nParameters:\na Gst.Caps to normalize\nthe normalized Gst.Caps\nReturns a Gst.Caps that represents the same set of formats as\ncaps, but contains no lists.  Each list is expanded into separate\nGst.Structure.\nThis function takes ownership of caps and will call gst_caps_make_writable (not introspectable)\non it so you must not use caps afterwards unless you keep an additional\nreference to it with gst_caps_ref (not introspectable).\nParameters:\na Gst.Caps to normalize\nthe normalized Gst.Caps\n"});