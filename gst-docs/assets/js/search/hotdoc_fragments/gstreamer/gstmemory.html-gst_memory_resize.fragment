fragment_downloaded_cb({"url":"gstreamer/gstmemory.html#gst_memory_resize","fragment":"\n\tgst_memory_resize\n\n\n\tGst.Memory.prototype.resize\n\n\n\tGst.Memory.resize\n\nResize the memory region. mem should be writable and offset + size should be\nless than the maxsize of mem.\nGST_MEMORY_FLAG_ZERO_PREFIXED and GST_MEMORY_FLAG_ZERO_PADDED will be\ncleared when offset or padding is increased respectively.\nParameters:\na GstMemory\na new offset\na new size\nResize the memory region. mem should be writable and offset + size should be\nless than the maxsize of mem.\nGst.MemoryFlags.ZERO_PREFIXED and Gst.MemoryFlags.ZERO_PADDED will be\ncleared when offset or padding is increased respectively.\nParameters:\na Gst.Memory\na new offset\na new size\nResize the memory region. mem should be writable and offset + size should be\nless than the maxsize of mem.\nGst.MemoryFlags.ZERO_PREFIXED and Gst.MemoryFlags.ZERO_PADDED will be\ncleared when offset or padding is increased respectively.\nParameters:\na Gst.Memory\na new offset\na new size\n"});