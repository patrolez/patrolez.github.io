fragment_downloaded_cb({"url":"gstreamer/gstbus.html#GstBus:enable-async","fragment":"\n\tenable-async\n\n\n\tenable-async\n\n\n\tenable_async\n\nEnables async message delivery support for bus watches,\ngst_bus_pop and similar API. Without this only the\nsynchronous message handlers are called.\nThis property is used to create the child element buses\nin GstBin.\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tConstruct Only\n\t\t\t\n\nEnables async message delivery support for bus watches,\nGst.Bus.prototype.pop and similar API. Without this only the\nsynchronous message handlers are called.\nThis property is used to create the child element buses\nin Gst.Bin.\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tConstruct Only\n\t\t\t\n\nEnables async message delivery support for bus watches,\nGst.Bus.pop and similar API. Without this only the\nsynchronous message handlers are called.\nThis property is used to create the child element buses\nin Gst.Bin.\n\n\tFlags :\t\n\t\n\t\t\n\t\t\t\tRead\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tWrite\n\t\t\t\n\t\t\t\t/\t\t\n\t\t\t\tConstruct Only\n\t\t\t\n\n"});