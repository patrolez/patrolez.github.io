fragment_downloaded_cb({"url":"gstreamer/gstmessage.html#gst_message_take","fragment":"\n\tgst_message_take\n\n\n\tGst.prototype.message_take\n\n\n\tGst.message_take\n\nModifies a pointer to a GstMessage to point to a different GstMessage. This\nfunction is similar to gst_message_replace except that it takes ownership\nof new_message.\nParameters:\npointer to a pointer to a GstMessage\nto be replaced.\npointer to a GstMessage that\nwill replace the message pointed to by old_message.\nTRUE if new_message was different from old_message\n\n\tSince\t\t\t: 1.16\n\t\t\nModifies a pointer to a Gst.Message to point to a different Gst.Message. This\nfunction is similar to gst_message_replace (not introspectable) except that it takes ownership\nof new_message.\nParameters:\npointer to a pointer to a Gst.Message\nto be replaced.\npointer to a Gst.Message that\nwill replace the message pointed to by old_message.\nReturns a tuple made of:\ntrue if new_message was different from old_message\ntrue if new_message was different from old_message\n\n\tSince\t\t\t: 1.16\n\t\t\nModifies a pointer to a Gst.Message to point to a different Gst.Message. This\nfunction is similar to gst_message_replace (not introspectable) except that it takes ownership\nof new_message.\nParameters:\npointer to a pointer to a Gst.Message\nto be replaced.\npointer to a Gst.Message that\nwill replace the message pointed to by old_message.\nReturns a tuple made of:\nTrue if new_message was different from old_message\nTrue if new_message was different from old_message\n\n\tSince\t\t\t: 1.16\n\t\t\n"});