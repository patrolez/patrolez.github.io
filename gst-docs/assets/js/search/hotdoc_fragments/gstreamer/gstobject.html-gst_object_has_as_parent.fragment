fragment_downloaded_cb({"url":"gstreamer/gstobject.html#gst_object_has_as_parent","fragment":"\n\tgst_object_has_as_parent\n\n\n\tGst.Object.prototype.has_as_parent\n\n\n\tGst.Object.has_as_parent\n\nCheck if parent is the parent of object.\nE.g. a GstElement can check if it owns a given GstPad.\nParameters:\na GstObject to check\na GstObject to check as parent\nFALSE if either object or parent is NULL. TRUE if parent is\nthe parent of object. Otherwise FALSE.\nMT safe. Grabs and releases object's locks.\n\n\tSince\t\t\t: 1.6\n\t\t\nCheck if parent is the parent of object.\nE.g. a Gst.Element can check if it owns a given Gst.Pad.\nParameters:\na Gst.Object to check\na Gst.Object to check as parent\nfalse if either object or parent is null. true if parent is\nthe parent of object. Otherwise false.\nMT safe. Grabs and releases object's locks.\n\n\tSince\t\t\t: 1.6\n\t\t\nCheck if parent is the parent of object.\nE.g. a Gst.Element can check if it owns a given Gst.Pad.\nParameters:\na Gst.Object to check\na Gst.Object to check as parent\nFalse if either object or parent is None. True if parent is\nthe parent of object. Otherwise False.\nMT safe. Grabs and releases object's locks.\n\n\tSince\t\t\t: 1.6\n\t\t\n"});