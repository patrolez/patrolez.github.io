fragment_downloaded_cb({"url":"gstreamer/gstelement.html#GstStateChangeReturn","fragment":"\n\tGstStateChangeReturn\n\nMembers\n\n\tGst.StateChangeReturn\n\nMembers\n\n\tGst.StateChangeReturn\n\nMembers\nThe possible return values from a state change function such as\ngst_element_set_state. Only GST_STATE_CHANGE_FAILURE is a real failure.\nthe state change failed\nthe state change succeeded\nthe state change will happen asynchronously\nthe state change succeeded but the element\ncannot produce data in GST_STATE_PAUSED.\nThis typically happens with live sources.\nThe possible return values from a state change function such as\nGst.Element.prototype.set_state. Only GST_STATE_CHANGE_FAILURE is a real failure.\nthe state change failed\nthe state change succeeded\nthe state change will happen asynchronously\nthe state change succeeded but the element\ncannot produce data in Gst.State.PAUSED.\nThis typically happens with live sources.\nThe possible return values from a state change function such as\nGst.Element.set_state. Only GST_STATE_CHANGE_FAILURE is a real failure.\nthe state change failed\nthe state change succeeded\nthe state change will happen asynchronously\nthe state change succeeded but the element\ncannot produce data in Gst.State.PAUSED.\nThis typically happens with live sources.\n"});