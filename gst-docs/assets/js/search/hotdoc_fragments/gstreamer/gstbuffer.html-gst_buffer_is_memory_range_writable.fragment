fragment_downloaded_cb({"url":"gstreamer/gstbuffer.html#gst_buffer_is_memory_range_writable","fragment":"\n\tgst_buffer_is_memory_range_writable\n\n\n\tGst.Buffer.prototype.is_memory_range_writable\n\n\n\tGst.Buffer.is_memory_range_writable\n\nChecks if length memory blocks in buffer starting from idx are writable.\nlength can be -1 to check all the memory blocks after idx.\nNote that this function does not check if buffer is writable, use\ngst_buffer_is_writable to check that if needed.\nParameters:\na GstBuffer.\nan index\na length, should not be 0\nTRUE if the memory range is writable\n\n\tSince\t\t\t: 1.4\n\t\t\nChecks if length memory blocks in buffer starting from idx are writable.\nlength can be -1 to check all the memory blocks after idx.\nNote that this function does not check if buffer is writable, use\ngst_buffer_is_writable (not introspectable) to check that if needed.\nParameters:\na Gst.Buffer.\nan index\na length, should not be 0\ntrue if the memory range is writable\n\n\tSince\t\t\t: 1.4\n\t\t\nChecks if length memory blocks in buffer starting from idx are writable.\nlength can be -1 to check all the memory blocks after idx.\nNote that this function does not check if buffer is writable, use\ngst_buffer_is_writable (not introspectable) to check that if needed.\nParameters:\na Gst.Buffer.\nan index\na length, should not be 0\nTrue if the memory range is writable\n\n\tSince\t\t\t: 1.4\n\t\t\n"});