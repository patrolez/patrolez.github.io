fragment_downloaded_cb({"url":"rtplib/gstrtcpbuffer.html#gst_rtcp_buffer_map","fragment":"\n\tgst_rtcp_buffer_map\n\n\n\tGstRtp.RTCPBuffer.prototype.map\n\n\n\tGstRtp.RTCPBuffer.map\n\nOpen buffer for reading or writing, depending on flags. The resulting RTCP\nbuffer state is stored in rtcp.\nParameters:\na buffer with an RTCP packet\nflags for the mapping\nresulting GstRTCPBuffer\nOpen buffer for reading or writing, depending on flags. The resulting RTCP\nbuffer state is stored in rtcp.\nParameters:\na buffer with an RTCP packet\nflags for the mapping\nresulting GstRtp.RTCPBuffer\nOpen buffer for reading or writing, depending on flags. The resulting RTCP\nbuffer state is stored in rtcp.\nParameters:\na buffer with an RTCP packet\nflags for the mapping\nresulting GstRtp.RTCPBuffer\n"});