fragment_downloaded_cb({"url":"rtplib/gstrtcpbuffer.html#gst_rtcp_buffer_new_copy_data","fragment":"\n\tgst_rtcp_buffer_new_copy_data\n\n\n\tGstRtp.RTCPBuffer.prototype.new_copy_data\n\n\n\tGstRtp.RTCPBuffer.new_copy_data\n\nCreate a new buffer and set the data to a copy of len\nbytes of data and the size to len. The data will be freed when the buffer\nis freed.\nParameters:\ndata for the new buffer\nthe length of data\nA newly allocated buffer with a copy of data and of size len.\nCreate a new buffer and set the data to a copy of len\nbytes of data and the size to len. The data will be freed when the buffer\nis freed.\nParameters:\ndata for the new buffer\nthe length of data\nA newly allocated buffer with a copy of data and of size len.\nCreate a new buffer and set the data to a copy of len\nbytes of data and the size to len. The data will be freed when the buffer\nis freed.\nParameters:\ndata for the new buffer\nthe length of data\nA newly allocated buffer with a copy of data and of size len.\n"});