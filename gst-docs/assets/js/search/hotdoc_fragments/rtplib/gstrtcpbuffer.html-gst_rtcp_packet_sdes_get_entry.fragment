fragment_downloaded_cb({"url":"rtplib/gstrtcpbuffer.html#gst_rtcp_packet_sdes_get_entry","fragment":"\n\tgst_rtcp_packet_sdes_get_entry\n\n\n\tGstRtp.RTCPPacket.prototype.sdes_get_entry\n\n\n\tGstRtp.RTCPPacket.sdes_get_entry\n\nGet the data of the current SDES item entry. type (when not NULL) will\ncontain the type of the entry. data (when not NULL) will point to len\nbytes.\nWhen type refers to a text item, data will point to a UTF8 string. Note\nthat this UTF8 string is NOT null-terminated. Use\ngst_rtcp_packet_sdes_copy_entry to get a null-terminated copy of the entry.\nParameters:\na valid SDES GstRTCPPacket\nresult of the entry type\nresult length of the entry data\nresult entry data\nTRUE if there was valid data.\nGet the data of the current SDES item entry. type (when not NULL) will\ncontain the type of the entry. data (when not NULL) will point to len\nbytes.\nWhen type refers to a text item, data will point to a UTF8 string. Note\nthat this UTF8 string is NOT null-terminated. Use\nGstRtp.RTCPPacket.prototype.sdes_copy_entry to get a null-terminated copy of the entry.\nParameters:\na valid SDES GstRtp.RTCPPacket\nresult of the entry type\nReturns a tuple made of:\ntrue if there was valid data.\ntrue if there was valid data.\ntrue if there was valid data.\nGet the data of the current SDES item entry. type (when not NULL) will\ncontain the type of the entry. data (when not NULL) will point to len\nbytes.\nWhen type refers to a text item, data will point to a UTF8 string. Note\nthat this UTF8 string is NOT null-terminated. Use\nGstRtp.RTCPPacket.sdes_copy_entry to get a null-terminated copy of the entry.\nParameters:\na valid SDES GstRtp.RTCPPacket\nresult of the entry type\nReturns a tuple made of:\nTrue if there was valid data.\nTrue if there was valid data.\nTrue if there was valid data.\n"});