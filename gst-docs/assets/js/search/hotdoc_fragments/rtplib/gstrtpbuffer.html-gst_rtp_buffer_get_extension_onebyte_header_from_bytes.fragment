fragment_downloaded_cb({"url":"rtplib/gstrtpbuffer.html#gst_rtp_buffer_get_extension_onebyte_header_from_bytes","fragment":"\n\tgst_rtp_buffer_get_extension_onebyte_header_from_bytes\n\n\n\tGstRtp.RTPBuffer.prototype.get_extension_onebyte_header_from_bytes\n\n\n\tGstRtp.RTPBuffer.get_extension_onebyte_header_from_bytes\n\nSimilar to gst_rtp_buffer_get_extension_onebyte_header, but working\non the GBytes you get from gst_rtp_buffer_get_extension_bytes.\nParses RFC 5285 style header extensions with a one byte header. It will\nreturn the nth extension with the requested id.\nParameters:\nGBytes\nThe bit-pattern. Anything but 0xBEDE is rejected.\nThe ID of the header extension to be read (between 1 and 14).\nRead the nth extension packet with the requested ID\n(out) (array length=size) (element-type guint8) (transfer none):\nlocation for data\nthe size of the data in bytes\nTRUE if bytes had the requested header extension\n\n\tSince\t\t\t: 1.18\n\t\t\nSimilar to gst_rtp_buffer_get_extension_onebyte_header, but working\non the GLib.Bytes you get from gst_rtp_buffer_get_extension_bytes.\nParses RFC 5285 style header extensions with a one byte header. It will\nreturn the nth extension with the requested id.\nParameters:\nGLib.Bytes\nThe bit-pattern. Anything but 0xBEDE is rejected.\nThe ID of the header extension to be read (between 1 and 14).\nRead the nth extension packet with the requested ID\nReturns a tuple made of:\nTRUE if bytes had the requested header extension\nTRUE if bytes had the requested header extension\nTRUE if bytes had the requested header extension\n\n\tSince\t\t\t: 1.18\n\t\t\nSimilar to gst_rtp_buffer_get_extension_onebyte_header, but working\non the GLib.Bytes you get from gst_rtp_buffer_get_extension_bytes.\nParses RFC 5285 style header extensions with a one byte header. It will\nreturn the nth extension with the requested id.\nParameters:\nGLib.Bytes\nThe bit-pattern. Anything but 0xBEDE is rejected.\nThe ID of the header extension to be read (between 1 and 14).\nRead the nth extension packet with the requested ID\nReturns a tuple made of:\nTRUE if bytes had the requested header extension\nTRUE if bytes had the requested header extension\nTRUE if bytes had the requested header extension\n\n\tSince\t\t\t: 1.18\n\t\t\n"});