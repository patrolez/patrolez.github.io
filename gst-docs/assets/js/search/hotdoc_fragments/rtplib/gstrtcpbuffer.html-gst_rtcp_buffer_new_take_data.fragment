fragment_downloaded_cb({"url":"rtplib/gstrtcpbuffer.html#gst_rtcp_buffer_new_take_data","fragment":"\n\tgst_rtcp_buffer_new_take_data\n\n\n\tGstRtp.RTCPBuffer.prototype.new_take_data\n\n\n\tGstRtp.RTCPBuffer.new_take_data\n\nCreate a new buffer and set the data and size of the buffer to data and len\nrespectively. data will be freed when the buffer is unreffed, so this\nfunction transfers ownership of data to the new buffer.\nParameters:\ndata for the new buffer\nthe length of data\nA newly allocated buffer with data and of size len.\nCreate a new buffer and set the data and size of the buffer to data and len\nrespectively. data will be freed when the buffer is unreffed, so this\nfunction transfers ownership of data to the new buffer.\nParameters:\ndata for the new buffer\nthe length of data\nA newly allocated buffer with data and of size len.\nCreate a new buffer and set the data and size of the buffer to data and len\nrespectively. data will be freed when the buffer is unreffed, so this\nfunction transfers ownership of data to the new buffer.\nParameters:\ndata for the new buffer\nthe length of data\nA newly allocated buffer with data and of size len.\n"});